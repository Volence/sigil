# t23 loop pass 1 — boot.emp (full 3(a)/3(b)/4/5 interrogation record)

Ran after checkpoint (a) countersign under the three overseer rulings.
Pass-1 headline actions: the P3 demanded feature (ruling 1), five zero-byte
3(b) fixes both twins, the ruling-3 hoist, six ledger rows, and the ruling-2
wave (clr.w, −2 both shapes, full re-pin + row-1257 fixture migration).

## P3 feature (ruling 1) — SHIPPED first

moveq link-imm8 via the dedicated `ImmSigned8` fixup (opcode-word low byte,
SIGNED window [-128, 127] — distinct from the unsigned `Value8` ext-word
class per the ruling's no-shared-union bar; quick/shift refusals stand,
messages updated). TDD: `moveq_link_folded_imm8_in_window_lowers` +
`moveq_link_imm8_out_of_window_is_loud` (fails the link NAMING the moveq
window). The deferred gates landed with it:
`boot_port::boot_sound_off_twin_parity_{plain,debug}` (oracle-fed
self-consistent worlds — the boot region is SHAPE-DIVERGENT at sound-off,
moveq 2 bytes shorter, so bases derive from the oracle, never pins) and
`boot_port::boot_hotkeys_shape_twin_parity` (the gameBootHook (1,1) drift
matrix: the oracle assembles the REAL games/sonic4/config/game.asm macro
expansion — a macro-body edit the mirror doesn't follow fails it). Kill row
45's P3 rider resolved same-commit.

## Step 3(a) — every line run

- **Ceremony scan**: no sfx_bank-class ratio; the 6× controller-init run and
  the two keyoff loops are hardware-protocol listings (step-4 adjudications
  below). Nothing beyond them.
- **Comment-as-compensation**: the cursor protocol's FIVE what-comments
  (register meanings the table assigns) are the recurring shape — demand
  data attached to the typed-table/data-DSL roadmap (ledger row; kill row
  46's kill condition is the structural fix).
- **Escape-hatch census (by shape)**: drift-ensure ×1 (PSG_PORT; the shadow
  offsets moved to the shared twin), macro-mirror transliteration ×1
  (gameBootHook). Cleanest census of the campaign so far; no recurring
  new shape.
- **Domain-type scan**: GAME_ENTRY_ID → GameStateId candidate (item-13,
  logged at step 2); SONG_MOVINGTRUCKS is SongId-family — an `as SongId`
  bless at the mirror's moveq is LOGGED as a candidate for when hook-block
  typing lands (macro-mirror block; cross-module call-slot enforcement
  doesn't bite today), not forced.
- **Noticing**: the `A-B(aN)` displacement-expression parse gap → ledgered
  as a grammar ask (demand 1); NOT proposed as a checklist line (single
  occurrence, and the named-derive workaround reads better).

## Step 3(b) — every line run

- **Comment-claim audit**: TWO false/imprecise claims found and FIXED both
  twins (zero-byte): "word count" on the Z80 copy (it is a BYTE count;
  rewritten naming the real moveq-range reason) and "d2 = $3FFF (longword
  count)" (it is the dbf count; rewritten "→ $4000 longs = 64KB"). Verified
  true: the ~264-cycle YM delay (250 taken + 14 exit), the wrap arithmetic,
  the bus-grant bit-0 note, the HBlank slot rte contract, keyoff $04-$06,
  the auto-increment=1 rationale.
- **Contract audit**: `clobbers(d0-d7/a0-a6/sr)` exhaustive ✓ (never
  returns; every callee's license is a subset).
- **Name audit**: locals conform; PSG_DATA_OFF names its derivation.
- **Magic-number audit**: THREE uncommented literals found and commented
  both twins ($F TMSS version nibble; $C0 region mask + bit 6 = PAL; VDP
  status bit 1 = DMA busy at the warm-boot wait).
- **Cold-reader test**: the movem preload was the wall — a cold reader
  couldn't know the a0-a4 assignments without opening boot_data.asm. Fixed:
  the register-map comment at the preload site (mirrors the table's head
  comments; the assert wall + byte gates hold the two in lockstep).
- **Codename audit**: clean (spec §refs, kill/ledger rows only).

## Step 4 — every adjudication named

- **Ruling-3 hoist EXECUTED**: VDP_MODE1_OFF/VDP_MODE2_OFF/VDP_HINT_OFF →
  engine.vdp shadow-offset block, ensures riding; hblank.emp + boot.emp
  import; byte-neutral (all gates green vs unchanged canonical). Ripple:
  hblank ambient arm in mixed_dac_rom, vdp.emp prepend in hblank_port +
  hblank_negative_probes, +3 equ pairs in the 10 checking port tests. Kill
  row 47 amended; the test-side vdp-equ-list duplication ledgered
  (at-next-touch consolidation jot).
- **Row 1482(d) typed RAM-cell decl**: RE-ADJUDICATED NOT-BUILD (extern-data
  decl grammar unchanged since the t21 probe); demand +2 (boot's VInt_Ptr +
  Game_State init stores — the class now has full .emp producer/consumer
  pairs). Ledgered, surfaced not silent.
- **set_vdp_reg helper**: NOT BUILT at pass 1 — RECORD CORRECTED at the
  panel (B1): the write-through shape has THREE .emp sites (hblank.emp:56-57
  is write-through, not RMW as this record first claimed; parallax.emp:230
  already ships with set_vdp_reg demand data in its site comment; boot is
  the third). Past the 2-consumer clock → at-next-touch ADOPTION DEBT
  ledgered; overseer adjudicates build-now vs debt at the merge gate.
- **6× controller-init structural clone**: NOT ADOPTED — a rep-class
  emitter saves nothing at 6 lines and loses the per-line protocol comments
  (adoption-over-cleverness taste gate).
- **vdp_reg/bytes_to_lcnt typing (macro-port rule re-check)**: UPGRADED to
  refinement params (`reg: int where 0..$17`, `val: int where 0..$FF`,
  `n: int where 4..$10000` — the vram_art wrong-input class); byte-neutral,
  gates green. The divisibility edge on bytes_to_lcnt is documentary (the
  AS function shares it).
- **New mirrors**: none created this pass (the hoist DELETED two).
- **align 2 adjudication (step-0 queued item — recorded here per the A1-4
  panel catch, the call itself shipped at step 1)**: the twin KEEPS its
  mid-code `align 2` (a no-op layout guard at an even address); the .emp
  carries a site comment naming the twin-lockstep fact instead of an align
  construct (none exists in .emp; if the align ever emitted a pad, the byte
  gate fires — that IS the lockstep mechanism).

## Step 5 — full interrogation, per hot proc (ONE proc: EntryPoint; heat: COLD, runs once pre-display)

- **Invariant ladder**: every loop body is protocol-minimal (delay, copy,
  fill); nothing sits below its scope. NO finding.
- **Counter/cache audit**: d1/d2 are loop-local dbf counters; no guarded
  resource exists. NOT-APPLICABLE.
- **Guard-coverage audit**: the two DMA-busy waits and the bus-grant wait
  are each the SOLE guard on their following access, and the single linear
  path passes through all of them — load-bearing, no uncovered path.
- **Hardware cross-check**: statically consistent (TMSS handshake, fill
  auto-increment=1, YM delay 264 ≥ 192, Z80 handshake order). TWO items
  named for the C3 panel lens rather than assumed: (1) the PSG-silence
  writes land at $C00011 INSIDE the DMA-fill parallelism window — PSG is a
  separate write path from the VDP data port, but the window invariant's
  comment says "nothing touches the VDP data port", so C3 rules whether the
  claim needs sharpening; (2) the YM2612 key-off writes back-to-back with
  no busy-wait between register writes (boot-once, silence-only; the
  original twin ships the same) — C3 adjudicates whether a site comment
  naming the compromise is owed.
- **Silent-tradeoff comments**: fill-parallelism is commented; the keyoff
  busy-wait compromise is the C3 question above.
- **Noticing**: none new.
- **Threshold ruling**: boot is COLD — zero steady-state cycles; NO cut.
  The ONE byte-changing item (clr.w Frame_Accumulator, −2, size-only) ran
  as THE WAVE per ruling 2 (sole-item addendum): twin lockstep, 77 pins −2,
  30 engine.inc orgs, REGION_A_LMA, repin_pins baselines, and the row-1257
  fixture sweep — which caught TWO stale hardcoded classes and MIGRATED
  them to pins permanently (vdp_init_port's BootData_VDPRegs carrier;
  mixed_tranche3's vdp_init windows + pc-rel disp). CRCs after the wave:
  plain `01832b1a`/421157 · debug `154076f8`/429232 (debug +30 = convsym
  appendix re-encoding; ASSEMBLED_LEN both shapes unchanged). Paired strict
  2588/0 against the rebuilt references.

## Pass 2 (post-wave re-circuit)

3(a) EMPTY (the wave/hoist opened no ceremony, census, or type item);
3(b) EMPTY (new comments re-audited: the clr.w I/O-hazard note and the
register-map comment verified against code and table); step 4 EMPTY (no new
emission patterns; inventory walked against the wave's output); step 5
EMPTY (the interrogation re-run finds the wave took the only candidate;
no-cut stands with the two C3 questions carried to the panel). → DRY CLAIM;
panel dispatched (A1+B1+C1+C2+C3, read-only, one round, synchronous).
