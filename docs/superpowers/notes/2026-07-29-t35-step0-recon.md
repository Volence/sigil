# 2026-07-29 — t35 step-0 recon (P2+P3: player_ground / player_air / player_spindash)

Porter: Opus subagent (direct-dispatch). Brief: `2026-07-29-t35-p2-player-states-brief.md`.
Masters: sigil `7b725b1` / aeon `84d23c8`. Branches `port-tranche35` in BOTH repos,
worktrees `.worktrees/port-tranche35`.

## Baseline verification (worktree, editor-rsynced)

- Editor data rsync'd from aeon main → aeon worktree (39 entries present).
- Dual rebuild in the aeon worktree:
  - plain `s4.bin`  = **37dd2bb2 / 421041** ✓ canonical
  - debug `s4.debug.bin` = **bbb822f6 / 429102** ✓ canonical
- Strict suite from the sigil worktree, `AEON_DIR` = aeon worktree:
  `SIGIL_STRICT_GATE=1 cargo test --workspace --release` = **2803 passed / 0 failed / 1 ignored** ✓ baseline.

## Census / brief confirmation (NO contradictions → proceed; not a STOP)

1. **Combined-link stale-fold fix LANDED** — brief §2 claim verified: `03d29cd` IS an
   ancestor of sigil master; merge message confirms "t34 mixed whole-ROM gates UN-IGNORED
   and green 0-diff both shapes … P2-P4 UNGATED". The gap-ledger row 1759 dup-locals
   hypothesis was a RED HERRING (root cause = cross-seam JmpJsrSym growth vs baked
   constants). So P2 files must pass FULL mixed byte-identity from the start (not
   windowed-only) — confirmed feasible.

2. **Frontend accepts literal `jmp Name` / `jsr Name` (absolute)** — `code.rs:144-159`:
   a bare-`Sym` operand to jmp/jsr lowers via `lower_jmp_jsr_sym` → `JmpJsrSym`
   (linker-chosen abs.w/abs.l width). Verified in the ROM: `jmp PState_Spindash` at
   `$10476` emits `4EF9 0001 0B74` (jsr/jmp abs.l → $10B74). So a FAITHFUL, ZERO-DELTA
   step-1 transliteration is possible with LITERAL mnemonics — `jsr`/`jmp Name`,
   `bsr.w`/`bra.w`/`bra.s`/`bsr.s Name`, explicit-width `Bcc.s`/`.w` — no jbra/jbsr
   conversion (that is step-2 house-format, deferred). `game_loop.emp:38` already carries
   a literal absolute `jsr Name` as a structural precedent.

3. **The keystone provides the imports** (brief §2) — `player_common.emp` exports:
   `mask_opposing_lr` (comptime fn — the surviving consumers are ground ×2 / air ×1),
   `dist_to_fix` (= `pixels_to_coord` splice), `set_standing_size`/`set_ball_size`,
   `PlayerV` overlay (`ground_speed/player_state/move_lock/spindash_charge/stick_convex`
   + the reserved fields), `Player_SetState`, `Player_SnapToSurface`, and the const
   mirrors. State files IMPORT these, never redefine.

4. **Stale `(sonic.asm)` comment CONFIRMED** — `player_ground.asm` header ("PState_Spindash
   lives in sonic.asm") + line 65 + the `Player_States` table comment
   ("PSTATE_SPINDASH (sonic.asm)") are stale: spindash relocated to `player_spindash.asm`.
   The `.emp` side (`player_common.emp:542`) is already correct ("player_spindash.asm").
   → step-3 comment-accuracy items for the AS twins if touched, else noted here.

## Region geometry (shape-INVARIANT — identical in s4.lst & s4.debug.lst)

| file            | base    | end (next label)     | len    | gate                     | else-org |
|-----------------|---------|----------------------|--------|--------------------------|----------|
| player_ground   | `$10448`| `$108A2` (PState_Air)| `$45A` | `SIGIL_EMP_PLAYER_GROUND`| `$108A2` |
| player_air      | `$108A2`| `$10B74` (PState_Spindash)| `$2D2`| `SIGIL_EMP_PLAYER_AIR` | `$10B74` |
| player_spindash | `$10B74`| `$10C14` (Sonic_InitAssets)| `$A0`| `SIGIL_EMP_PLAYER_SPINDASH`| `$10C14` |

The object bank base is `org $10000` (fixed) and the bank layout up to these files is
shape-invariant, so ONE org both shapes each (the sonic.asm gate precedent). main.asm
arms plain `ifndef` blocks at 46-48 (pure code — NOT the keystone internal-gate pattern).

## Symbol resolution ladder (per step-2 item 8 / the demanded-features law)

Field access: SST fields as BARE accessors (`angle(a0)`, `status(a0)`, `x_vel(a0)`, …)
via `use engine.objects.sst.{Sst}` (player_common.emp precedent); overlay via
`PlayerV.field(aN)` imported from player_common; `Sst.field` only inside comptime
`asm{}` templates. AS `SST_angle`/`_pl_gsp` equates do NOT exist in .emp scope → the
field-access form is DEMANDED at step 1 (byte-identical displacement).

- **`import` (module-to-module)** from `player_common` — PlayerV, the 4 macros,
  Player_SetState, Player_SnapToSurface, the PSTATE_*/ANIM_* etc. consts; from
  `engine.constants` — the shared engine block; from `engine.objects.sst` — Sst;
  from `engine.coords` — via dist_to_fix.
- **HOIST DUE (step-0 trip-check hit — kill-list rows 76/77):** player_ground is the
  **2nd `.emp` consumer** of the engine physics/constants block → rows 76/77's kill
  condition ("engine block HOISTS to the engine.constants twin at the 2nd consumer
  P2/player_ground") TRIPS. The shared engine consts player_common.emp declared
  file-local as 1st consumer (`PHYS_SKID_MIN/JUMP_BUFFER/FALL_CAP`, player/ball radii,
  `CURL_Y_SHIFT`, `ST_ROLLING/PUSHING`, `COLLISION_NONE`, `EDGE_*`, `BUTTON_*`) PLUS the
  NEW physics consts the state files need (`PHYS_ROLL_START_MIN`, `PHYS_SLOPE_WALK/
  ROLL_DOWN/ROLL_UP/STAND_MIN`, `PHYS_GSP_CAP`, `PHYS_JUMP_HEADROOM`, `PHYS_SLIP_ANGLE/
  SPEED/NUDGE`, `PHYS_FALL_ANGLE`, `PHYS_MOVE_LOCK_TIME`, `PHYS_ROLL_DECEL`,
  `PHYS_UNROLL_MAX`, `PHYS_KEEP_ROLL_MIN`, `PUSH_RADIUS`, `SPINDASH_*`, `SFXID_*`) move to
  `engine.constants.emp` (pub const + drift guard each), and player_common.emp +
  sonic.emp switch to `use engine.constants`. Byte-neutral (consts fold to immediates).
  Kill rows 76/77 conditions updated same-commit. (The physics table OFFSETS `PPHYS_*`
  are a4-relative displacement equates — resolve as consts/offsetof, engine.constants
  mirror or file-local.)
- **`extern proc` boundary decls** — the surviving player_sensors callees
  (`Player_SensorFloor/Ceiling/WallDir/WallAt`) are P4-close; each carries a DIE-AT-PORT
  kill row (the `Player_AtLedgeEdge` precedent, kill row already open). Cross-seam
  callees `GetSineCosine`/`ObjectMove`/`Sound_PlaySFX` resolve bare link.

## Guarded-field survival re-derivation (brief §2)

The 5 PlayerV guards (gsp/state/move_lock/spindash/stick_convex) exist because ground/air/
spindash read `_pl_*` from AS. As these three files PORT, they stop reading `_pl_*` from
AS — but **player_sensors (P4) still reads them**, so the guards SURVIVE. Verified: grep
the AS `games/` tree for surviving `_pl_*` readers after P2/P3 — sensors is the last one.
Kill rows 72/74/75 stay P4-CLOSE kills; do NOT retire early. No guard row retires this
tranche; the Player_States extern() entries CONVERT to local defs (report the count at
step 1: 4 of the 7 — Ground/Roll/Spindash/Air/Jump/RollJump/AirBall become real .emp
defs; the `extern()` term drops for all 7 once P2+P3 land, but the `offsets` construct
adoption stays LEDGERED, kill = ADOPT AT P4 CLOSE per t34).

## Ledger hazards swept (file-name + symbol grep)

- gap-ledger §17 (player_ground/air/spindash) = the master step-5 optimization backlog
  index — PULL at this tranche's step-5 (deferred; step-1 only this run).
- rows 261/287 (player_ground six `jsr GetSineCosine`) — the JmpJsrSym deferral that
  SHIPPED in tranche 2; the .emp `jsr GetSineCosine` uses that same path. Not a blocker.
- row 1759 (whole-ROM link blocker) — CLOSED by `03d29cd` (see confirmation 1).

## Infrastructure plan (step-1)

Per-file gates `SIGIL_EMP_PLAYER_{GROUND,AIR,SPINDASH}`; ONE test file
`test_p2_player_states_port.rs` (region byte gates BOTH shapes + t24 positive-control /
negative-probe per file + guard-pass); ONE mixed fn `build_mixed_tranche35_*` (all 3
gates, whole-ROM both shapes). New pins: 3 region pins + cross-seam callees not yet
pinned (`PLAYER_SET_STATE`, `PLAYER_SENSOR_CEILING/WALL_DIR/WALL_AT`,
`PLAYER_SNAP_TO_SURFACE`). Most cross-seam pins already exist from t34 (GET_SINE_COSINE,
P_STATE_* addresses, OBJECT_MOVE, SOUND_PLAY_SFX, CTRL_1_*, PLAYER_PHYS/QUADRANT/
JUMP_BUFFER, CAMERA_HOLD_FRAMES). Wave-ripple checklist applies only if step-2/5 moves
bytes (step-1 delta = ZERO).

## Panel (from brief §3): A1 + B1 + C2 + C1 ACTIVE (hot per-frame physics); C3 inactive
unless VDP claims appear. Dry by panel — deferred to the loop (post-checkpoint-a).
