# Phase-2.5 (D7 + Tier-C) + item-9 riders — Stage-0 existence census + design gate

**Author:** Opus (Phase-2.5/item-9 lead). **Status:** ✅ MERGED + PUSHED + CLOSED (2026-07-22).
Design gate (§0–§9) ruled; all six commits (c1–c6) + a post-clear correctness fix landed, verified,
and merged. §0–§9 remain the historical design reference; the RESUME BRIEF below is the audit trail.

**MERGED masters (pushed, origin==local):** aeon `033865f` / sigil `c0b6354` (merge `9b89d67` +
arc-close confirm). Was: aeon `5c975af` / sigil `08b3110`. Branch `pass3-phase25-item9` + worktrees
aeon-p25/sigil-p25 deleted post-merge.
**NEW CANONICAL:** plain `406c773b`/421122 · debug `5752c2e3`/429107 (ASSEMBLED_LEN `0x5DB60`/`0x5F65A`
unchanged). Merged aeon master rebuilds these EXACTLY both invocations; full paired strict on merged
masters **2457/0/1**; overseer attack-the-diff PASS + re-attack PASS.

**THE CATCH (arc's headline):** the overseer cleared merge pending a "$1A→$16 comment fix";
hand-auditing that comment surfaced a REAL byte-changing bug — `EntityWindow_MigrateMasks`
hand-computed the EntityScanState stride via a shift decomposition (`×26 = $1A`), NOT `sizeof`, so
c6's `$1A→$16` left it stale → wrong mask migration on window slides. **Invisible to BOTH the byte
gate** (both twins carried the identical wrong `×26` → sigil==asl held; the gate proves twin-agreement,
not correctness) **and the boot** (wrong mask ≠ crash). Fixed `lsl#3`→`lsl#2` (`×22 = $16`),
length-preserving (aeon `04ff7e5` / sigil `4cfb18c`). Banked as a gap-ledger blind-spot row (bar:
struct-size change ⇒ grep hand-strides across both twins, not just `sizeof`/`offsetof`).
**Non-blocking rider carried forward:** observe one live window-slide mask migration next runtime session.

Row-1239 `.culled_loop` sweep RAN → no remainder (CLOSED). ew#1 cross-note (ledger 1085+1218) +
worktree-seed jot + c5 upstream-fixture-ripple bar recorded. **NEXT: Phase 3 (G5 + domain-type pass)
opens with its own design gate — G5 spec first, Volence-taste questions separated from mechanical.**

Census method: four parallel read-only Explore/general sweeps over aeon `5c975af` (clean), each
symbol/site proven at CURRENT line numbers across BOTH `.emp` and `.asm` twins + game code +
the sigil harness. Deadness = **zero READ sites** (clears/writes don't count); every writer listed.

---

## ★ RESUME BRIEF (START HERE) — fresh session, no chat memory

### R0 · Gate ruling (what the overseer decided)

- **Committed order re-grouped from §5.** The gate ordered byte-NEUTRAL + isolated-low-risk work
  FIRST (c1–c3), and DEFERRED the RAM-layout surgery (the address-error failure class) to c4–c6.
- **HOLD ruled after c3.** Correctness-critical RAM surgery never ships from the tail of a deep
  session. The anchor (c1–c3) is committed + verified; nothing is lost by stopping. **c4 opens fresh
  against the committed anchor.**
- **Parks ratified** (each gets a gap-ledger row w/ reason + reopen condition at close):
  Tile_Cache_GetTile (pin-anchor churn, low value), aabb #1 (no live consumer), dplc D3 (guard-hoist
  maintainability regression), Tier-C A/B/C evaporated + C′ un-ported `.asm`, and **Hscroll_Dirty_Start/_End
  → parked to t18** (dropped from this arc's byte-changing set).
- **PHYS_ROLL_FRICTION EXCLUDED (kept), RATIFIED.** It is a live-cluster **tuning value**, NOT a dead
  guard — do not delete it in the c1 byte-neutral purge or anywhere in this arc. (Corrects §1's
  "companions" row, which listed it as a 0-reader deletion candidate.) See R6.

### R1 · Current state (verified live this session)

| | aeon | sigil |
|---|---|---|
| branch | `pass3-phase25-item9` | `pass3-phase25-item9` |
| c1 (D7 byte-neutral consts) | `7ccb791` | — (no sigil-side repin needed) |
| c2 (engine.vdp module, row 1073) | `c076bcf` | `3bb09dd` (register engine.vdp in port harnesses) |
| c3 (vdp_init M1 — byte-CHANGING) | `214c245` | `e74ef67` (repin ripple, +2 all downstream pins) |
| master (UNTOUCHED) | `5c975af` | `08b3110` |
| worktree | `/home/volence/sonic_hacks/aeon-p25` | `/home/volence/sonic_hacks/sigil-p25` (clean) |

- **Branch ROMs @ c3 tip (built in aeon-p25, both shapes present):** plain `d1ca0340`/421134 ·
  debug `5ebc8dc3`/429151. (Master baseline for parity ref: plain `00222415`/421134 · debug
  `fffc0179`/429151 — same padded sizes, CRCs differ by the c3 code shift.)
- **Worktrees aeon-p25 / sigil-p25 already exist, are SEEDED, and c1–c3 are built.** If you reuse them,
  **do NOT re-seed.** Only re-seed if you create a brand-new worktree (see R2).

### R2 · Level-data-blob seeding requirement (the reproducibility trap — must NOT re-fire)

A fresh `git worktree add` does **not** check out the git-IGNORED generated artifacts a build needs
(editor per-section working data, generated OJZ `entity_data.asm`, collision/sprite binaries,
engine/debug blobs). Without them `build.sh` either errors (`cannot include .../entity_data.asm`) or —
**worse, silently** — falls back to air-baseline collision and builds a ROM diverging **~130 KB with no
error**. The seed step closes this:

```
aeon/tools/seed-worktree.sh <worktree-path> [<source-tree>]     # run ONCE, right after `git worktree add`
```

It copies the whole gitignored artifact set from a known-good tree (default: the main worktree
containing the script) and builds both shapes. **`tools/seed-worktree.sh` is tracked on the branch
(commit `923ba5d`)** so a fresh checkout has it. **The existing aeon-p25 is already seeded — reuse it
and skip this.** (build.sh emits `s4.debug.*` natively under `DEBUG=1`, so shapes don't overwrite.)

### R3 · Remaining plan — VERBATIM (c4–c6 + close)

Each byte-changing commit stays independently buildable/bisectable by doing the **full ripple in-commit**
(see R4 for exactly which sites — c3 established this: it touched pins.rs + mixed_dac_rom + repin_pins +
engine.inc ORGs all in one commit; the note's older "hand-edits once at close" phrasing in §8 is
superseded by that observed cadence).

- **c4 · Spawn_Count** — RAM cell (ram.asm:240) + **2 stores**: RunObjects (core.emp:421 / core.asm:375)
  + InitObjectRAM (core.emp:89 / core.asm:49). `.emp`+`.asm` twin. **+ `.culled_loop` `declared∖effective`
  clobber sweep SAME COMMIT** (adjudication (b), gap-ledger row 1239: `ProcNode.declared_clobbers −
  Closure.effective.regs`, non-`out()` remainder = over-declared → tighten byte-neutrally). This is the
  **FIRST RAM-deleting commit → a PARITY STATEMENT is required** in the commit message. Pins to touch:
  pins.rs:422 + core_port.rs:95/123 + core_negative_probes.rs:58 + repin.toml:695.
- **c5 · CROSS_RESET_MAGIC** — const (constants.asm:107) + equates (ram.asm:518–520) + **store** in
  Cold_Boot (boot.asm:197). `.asm`-only. RAM equates are **fixed-addr → no RAM shift**; the store
  removal is byte-changing. Sigil hits are **lexer/repin TEST FIXTURES, not byte-pins**.
- **c6 · ess_ring_left_idx / ess_obj_left_idx** — WIDEST row. Fields structs.asm:246/248 +
  entity_window.emp:59/61; clrs in EntityWindow_InitSection (emp:656/658, asm:560/562). **MID-STRUCT:**
  shifts every EntityScanState field ≥`$04`, struct len `$1A`→`$16` (−4 bytes), drift-locks emp:98/100,
  harness offset table entity_window_port.rs:180–191. `.emp`+`.asm` twin. **+ ew#1 CROSS-NOTE** (adjudication
  (a), gap-ledger rows 1085 + 1218: reword so a reopened ew#1 no longer "reuses two dead ess_*_left_idx
  fields" — it re-adds its own scratch). **PARITY STATEMENT required.**
- **CLOSE** — (1) final **shared 5-site ripple reconciliation** (repin does pins.rs; HAND-edit engine.inc
  ORG table + `mixed_dac_rom` + `repin_pins`; repin.toml only if a region is ADDED — none of c4–c6 add a
  region, and engine.vdp is source-level, so **no repin.toml touch expected**); (2) **PROVENANCE
  re-baseline** (crc32+size, FINAL bytes both shapes → golden/PROVENANCE.md); (3) **full paired strict,
  failures-first, both shapes** from clean tips (never `grep|head`); (4) **RUNTIME BOOT BOTH shapes in
  oracle — FOREGROUND only, never a subagent** — drive the OJZ scroll scene **+ ObjectTest/Churn
  population** (for the core/RunObjects Spawn_Count touch), confirm **no address error and Lag
  unchanged**, frame-anchored on Frame_Counter; (5) **HOLD for overseer attack-the-diff before merge.**

### R4 · Reusable scripts (org-apply / pins-sync) — where they live, how to invoke

- **pins-sync = the `repin` tool** (authoritative). From sigil-p25:
  `cargo run --release --bin repin -- --aeon /home/volence/sonic_hacks/aeon-p25 --verbose`
  (or `target/release/repin --aeon /home/volence/sonic_hacks/aeon-p25 --verbose`). It (a) **writes
  pins.rs**, (b) prints the pin-change list, (c) prints the rerun-hint affected-binary list, and (d)
  prints a paste-able **"engine.inc / main.asm gate resume orgs"** else-arm block. Run it after each
  byte-changing edit; it tells you exactly which pins shifted and by how much. **Prereq: both shapes
  built in aeon-p25 first** (repin verifies bytes against `s4.bin`/`s4.debug.bin`).
- **org-apply = `apply_orgs.py`.** Durable copy: **`docs/superpowers/notes/phase2.5-apply_orgs.py`**
  (source also embedded in R7 below; the original lived in a session scratchpad that will be cleaned).
  It bumps every gate-resume org in aeon `engine/engine.inc` + `games/sonic4/main.asm` by a fixed
  `DELTA` and **self-verifies** the bumped set against repin's printed authoritative new-value block,
  refusing (`MISMATCH`, exit 1) if they diverge. **Invoke:** `python3 phase2.5-apply_orgs.py <DELTA>`
  (default +2). **Two things to re-point before reuse:** (1) the hardcoded `REPIN` path near the top
  (point it at the fresh repin dump), (2) the `DELTA` arg. **LIMITATION:** it applies ONE uniform delta
  to ALL gate orgs — correct only for a uniform ripple like c3's `+0x2`. c4–c6 store removals produce a
  **non-uniform** ripple (regions upstream of the edit don't move; downstream shift by a negative delta),
  so apply_orgs will (correctly, safely) **refuse** — then **HAND-apply repin's printed gate-resume org
  block** instead. Use apply_orgs only if repin reports a single uniform delta.

### R5 · Per-commit ripple sites (observed from c3 — do the SAME for each of c4–c6)

c3 (byte-changing) touched, IN THE SAME COMMIT, both repos:
- **aeon:** the code twin (`.emp` + `.asm`) + **`engine/engine.inc`** gate ORGs (via apply_orgs / hand).
- **sigil:** **`crates/sigil-harness/src/pins.rs`** (repin) + **`tests/mixed_dac_rom.rs`** +
  **`tests/repin_pins.rs`** (offset test fixtures that shift with the pins).
- **NOT touched:** `repin.toml` (no region added), `repin_pins.rs`'s region list (unchanged set).
Do the full set per c4/c5/c6 so each commit builds. PROVENANCE re-baseline + final reconciliation +
full strict + runtime boot are the CLOSE-only steps (R3).

### R6 · Open riders inherited at close

- **worktree-seeding ledger jot** — record the R2 trap (fresh-worktree silent ~130 KB air-baseline
  divergence) + `seed-worktree.sh` as the fix, in the campaign gap-ledger.
- **PHYS_ROLL_FRICTION decision note** — logged in R0 + §6-decisions here: EXCLUDED as a live tuning
  value, not a dead guard. Ensure the c1 purge did NOT delete it (verify at resume; c1 = `7ccb791`).

### R7 · `apply_orgs.py` source (embedded backup)

```python
#!/usr/bin/env python3
"""Bump gate-resume orgs by a fixed delta, then self-verify against repin's
authoritative new-value set. Gate orgs = `org` followed by >=2 spaces then $HEX.
The lone `org $10000` (ObjCodeBase, single space) is a fixed boundary and skipped."""
import re, sys

AEON = "/home/volence/sonic_hacks/aeon-p25"
FILES = [f"{AEON}/engine/engine.inc", f"{AEON}/games/sonic4/main.asm"]
REPIN = "<RE-POINT ME: path to the fresh `repin --verbose` dump>"   # was a session scratchpad file
DELTA = int(sys.argv[1], 0) if len(sys.argv) > 1 else 2

# authoritative new values from repin's printed block
auth = []
inblk = False
for line in open(REPIN):
    if "gate resume orgs" in line:
        inblk = True
    if inblk:
        m = re.search(r'^\s+org\s{2,}\$([0-9A-Fa-f]+)', line)
        if m:
            auth.append(int(m.group(1), 16))
auth_set = sorted(auth)

# bump gate orgs in the files
GATE = re.compile(r'^(\s+org\s{2,}\$)([0-9A-Fa-f]+)(.*)$')
produced = []
newtext = {}
for path in FILES:
    out = []
    for line in open(path):
        m = GATE.match(line)
        if m:
            old = int(m.group(2), 16)
            new = old + DELTA
            produced.append(new)
            out.append(f"{m.group(1)}{new:X}{m.group(3)}\n")
        else:
            out.append(line)
    newtext[path] = "".join(out)

produced_set = sorted(produced)
if produced_set != auth_set:
    print("MISMATCH — bumped set != repin authoritative set")
    print("  in-file-bumped not in repin:", [hex(x) for x in sorted(set(produced_set)-set(auth_set))])
    print("  repin not in in-file-bumped:", [hex(x) for x in sorted(set(auth_set)-set(produced_set))])
    print(f"  counts: bumped={len(produced_set)} repin={len(auth_set)}")
    sys.exit(1)

for path, txt in newtext.items():
    open(path, "w").write(txt)
print(f"OK — {len(produced_set)} gate orgs bumped by {DELTA:+#x}, all match repin authoritative set")
```

---

## 0 · Headline — two big evaporations, one big blast-radius

1. **Tier-C movem batch has EVAPORATED at its three named `.emp` targets.** Perform_DPLC,
   Load_Object, and AllocDynamic are **already minimized** to their live∩clobber sets — the exact
   register the 2026-07-16 review flagged as deletable is already omitted at each site. There is
   nothing to delete in the ported twins. The net + prior port-loop passes already retired this
   target (the [[stage-0-every-pass3-parcel]] pattern again). The only surviving Tier-C
   redundancies live in **un-ported `.asm`-only files** (children.asm) → recommend PARK (§4).
2. **`Tile_Cache_GetTile` (D7) is a pin ANCHOR, not a clean drop** — deleting it re-anchors
   pins.rs + repin.toml + engine.inc ORGs. Low value (zero-caller leaf) vs real anchor churn →
   recommend PARK (§3, gate call).
3. **`ess_*_left_idx` (D7) is a MID-STRUCT deletion** — shifts the whole EntityScanState tail +
   struct len + drift-locks + harness offset table. Genuinely dead, doable, but the highest
   blast-radius row in the batch. Fires the ew#1 cross-note (§ adjudication a).

Net: the surviving arc is **D7 dead-code (mostly) + two clean item-9 riders**. Tier-C, and several
item-9/D7 items, dissolve or park. "Expect evaporations — record them, don't force them" — this is
that outcome.

---

## 1 · D7 dead-code census — all 7 RAM symbols + all consts/flags confirmed DEAD

### 1a · Byte-NEUTRAL (unreferenced `=`/`equ`; deletion emits no byte change, no re-pin)

| Symbol | Def | Notes |
|---|---|---|
| `MAX_SPAWNS_PER_FRAME` | constants.asm:227 | 0 readers (the never-built spawn-throttle guard) |
| `HEIGHT_MAP_SIZE` | constants.asm:399 | 0 readers; **cascade**: kills sole readers of `NUM_COLLISION_PROFILES` (:397) + `HEIGHT_PROFILE_SIZE` (:398) → those go too |
| `CTYPE_FLAT_SOLID` | constants.asm:404 | 0 readers. **KEEP sibling `CTYPE_AIR` (:403) — LIVE** |
| `SF_HAS_WATER/_UNDERGROUND/_NO_Y_WRAP/_PRESERVE_STATE` | constants.asm:436-439 | 0 readers (dead surface-flag block) |
| `ST_P1_PUSHING` / `ST_P2_PUSHING` | constants.asm:214-215 | 0 readers. **KEEP `ST_PUSHING` (:206) — LIVE** (player_common/air/ground). The `ST_P*_STANDING` bits already gone (tranche-7b). |
| companions | `ANGLE_TABLE_SIZE` :400, `MAX_OBJECT_TYPES` :489 | 0 readers → delete. **`PHYS_ROLL_FRICTION` :251 EXCLUDED at gate (RATIFIED) — it is a live-cluster TUNING VALUE, not a dead guard; DO NOT delete.** See R0. |
| `DEBUG_ALL/_DMA/_VRAM/_OBJECTS/_COLLISION` | macros.asm:373-377 | self-contained dead cluster, dead in BOTH shapes. **KEEP `DEBUG_FLY_SPEED[_FAST]` (test_player.asm:35-36) — LIVE** |

No `.emp` twin, no harness/pins exposure on any of these. → **one bisectable commit, verified
byte-IDENTICAL (canonical CRCs unchanged).**

### 1b · Byte-CHANGING (RAM cell and/or store removal → gate move + re-pin)

| Symbol | Def | Writer(s) (proc) | Reads | Blast radius |
|---|---|---|---|---|
| `Spawn_Count` | ram.asm:240 | **RunObjects** (core.emp:421/core.asm:375) **+ InitObjectRAM** (core.emp:89/core.asm:49) | 0 | RAM cell + 2 stores, **.emp+.asm twin**; pins pins.rs:422 + core_port.rs:95/123 + core_negative_probes.rs:58 + repin.toml:695. **Fires the `.culled_loop` rider — adjudication (b).** |
| `CROSS_RESET_MAGIC` | constants.asm:107 + ram.asm:518-520 | **Cold_Boot** (boot.asm:197) | 0 | 1 store, **.asm-only** (boot.asm); const+equates byte-neutral, store byte-changing. Sigil hits are lexer/repin TEST FIXTURES, not byte-pins. |
| `Hscroll_Dirty_Start` / `_End` | ram.asm:136-137 | **Parallax_Step4_Fill** (parallax.asm:440/441/445/446) | 0 | 2 RAM cells + 4 stores, **.asm-only** (parallax.asm, no `.emp` twin, no pins) |
| `ess_ring_left_idx` / `ess_obj_left_idx` | structs.asm:246/248 + entity_window.emp:59/61 | **EntityWindow_InitSection** clr only (emp:656/658, asm:560/562) | 0 (right-idx siblings ARE live-read) | **MID-STRUCT** — shifts every field ≥`$04`, struct len `$1A`→`$16`, drift-locks emp:98/100, harness offset table entity_window_port.rs:180-191. **.emp+.asm twin.** Adjudication (a). |
| `Tile_Cache_GetTile` | tile_cache.emp:135 / asm:13 | (routine, 0 callers) | 0 callers | body removal + **PIN ANCHOR** (pins.rs:87/90, repin.toml:143/147/158, engine.inc:316/330 ORGs). Not a clean drop → §3. |

---

## 2 · Item-9 (Parcel E) riders — all four still exist; two clean, two purpose-drifted

| # | Rider | Byte? | State | Recommend |
|---|---|---|---|---|
| **VDP shared module (row 1073)** | neutral | typed-VDP interface still file-local in section.emp:58-122 (none `pub`); plane_buffer.emp:448-451/463-466 keep the inline `lsl.l#2/addq/ror/swap` shuffle at 2 drain heads; no `engine.vdp` module exists | **DO** — real dedup, LIVE 2nd consumer (plane_buffer), matches 2nd-consumer-consolidation doctrine, byte-neutral |
| **vdp_init M1** | changing | `Flush_VDP_Shadow` (vdp_init.emp:44) does a full `dbf`-walk of all shadow regs, no shift-out early-exit; per-VBlank (callers vblank.asm:58/124) | **DO** — genuine ~600c on register-dirty VBlank frames, isolated, no consumer/maintainability problem |
| **aabb #1 (3-piece split)** | neutral | still one template (aabb.emp:46 `aabb_axis_test`); consumers rings.emp:289/296 + collision.emp:57/67 | **PARK (gate call)** — byte-neutral but its purpose (unblock rings pre-combined-dims + collision coarse-reject variants) has **no live consumer**: rings R3 is a dissolved/parked Bar-A row. Substrate-ahead-of-consumer → "no net without a consumer." |
| **dplc D3 (hoist frame-check)** | changing | frame-unchanged check in the callee template (dplc.emp:42-44); callers **all game-side `.asm`**: Sonic_LoadArt (sonic.asm:28), test_player.asm:259, test_animated.asm:46; **Perform_DPLC_Deferrable has ZERO callers** | **PARK (gate call)** — hoisting duplicates the guard into 3 callers AND makes it a future-caller obligation (maintainability regression) for ~65c/frame on the player-art path in a 48%-idle scene. Callee-side guard reads cleaner. |

---

## 3 · Adjudications

**(a) `ess_*_left_idx` — genuinely dead; ew#1 reuse rider is MOOT.** The left-edge scan ratchet is
dead: only the `_left_idx` pair is unread, while the `_right_idx` siblings are live-read
(entity_window.emp:1035/1242/1321/1347) — so this is dead scaffolding, not a whole-mechanism cut.
The old base-census rider "**D7 must NOT delete the fields entity_window #1 repurposes**"
(parcelD-rescope-census.md §iii + master step-5 backlog row 1085) was written when ew#1 was SURVIVE;
**ew#1 dissolved in the Bar-A sweep** (gap-ledger row 1218), so the rider is moot and the fields are
free to delete. **CROSS-NOTE owed at close** (ledger rows 1085 + 1218): a reopened ew#1 can no longer
"reuse two dead ess_*_left_idx fields" — it re-adds whatever scratch it needs. Correct the phrasing
so a future reader doesn't hunt for fields that no longer exist.

**(b) `Spawn_Count` deletion is a `core.emp` touch → fires the `.culled_loop` sweep.** Confirmed two
writers: RunObjects (core.emp:421) + InitObjectRAM (core.emp:89). Deleting the RunObjects store edits
`core.emp`, which per gap-ledger row 1239 **rides the banked `.culled_loop` `declared∖effective`
clobber sweep in the same commit** (`ProcNode.declared_clobbers − Closure.effective.regs`, non-`out()`
remainder = over-declared → tighten byte-neutrally). Scoped into the Spawn_Count commit.

---

## 4 · Tier-C movem — evaporated in scope; `.asm`-only remainder

- **A · Perform_DPLC(_Deferrable)** (dplc.emp:76-78 / asm:55-57,110-112): `movem.l d2-d4/a2` around
  `QueueDMA_*` (`clobbers(d0-d4/a1-a2)`). All four saved regs CLOBBERED + live-across → **fully
  load-bearing**. The deletable reg (a3) is **already omitted** (preserved by QueueDMA). Nothing to delete.
- **B · Load_Object** (load_object.emp:39-42,87 / asm:28-31,74): `movem.l d0/a1` around AllocDynamic
  (`clobbers(d0) out(a1 if eq) preserves(a0)`). d0 clobbered, a1 overwritten → **fully load-bearing**;
  d1/d2 **already omitted** on AllocDynamic's `preserves`. Nothing to delete.
- **C · AllocDynamic** (core.emp:108-158): **makes no calls** → no Tier-C frame exists. Its internal
  `move.l a0` single-reg saves honor its own `preserves(a0)` — load-bearing, not candidates.
- **C′ · the real redundancies** are `.asm`-ONLY, no `.emp` twin: children.asm:49/:122/:198 (partial —
  d3/d4/a0 dead-saved around `jsr AllocDynamic`), **:293 fully redundant** (`movem.l d1-d5/a0` — all 6
  preserved by AllocDynamic), + object_test/test_churn a0 saves.
  → **Recommend PARK** to the children.asm port: the whole Tier-C batch was premised on the `.emp`
  per-callee-union proof (gap-ledger row 1111), which does not apply to un-ported `.asm`; editing an
  un-ported file's register frames is pre-port churn the port will re-derive. **Optional single
  opportunistic win**: children.asm:293 (6 dead-saved regs, provable against AllocDynamic's `.emp`
  contract) as one `.asm`-only commit if the gate wants a concrete Tier-C artifact. Gate call.

---

## 5 · Proposed batch (surviving rows) — bisectable commit order, smallest blast-radius first

1. **D7 byte-neutral purge** (1 commit, no gate move) — §1a consts + DEBUG flags. Verify byte-IDENTICAL.
2. **CROSS_RESET_MAGIC** (byte-changing, `.asm`-only boot.asm) — store + const + equates.
3. **Hscroll_Dirty_Start/_End** (byte-changing, `.asm`-only parallax.asm) — 4 stores + 2 RAM cells.
4. **Spawn_Count** (byte-changing, `.emp`+`.asm` twin core) — RAM cell + 2 stores; **+ `.culled_loop`
   declared∖effective sweep same commit** (adjudication b).
5. **ess_*_left_idx** (byte-changing, `.emp`+`.asm` twin, mid-struct) — 2 fields + 2 clrs + tail shift +
   struct len + drift-locks + harness offset table; **+ ew#1 cross-note** (adjudication a).
6. **VDP shared module (row 1073)** (byte-neutral) — new `engine.vdp` module; section.emp + plane_buffer.emp adopt.
7. **vdp_init M1** (byte-changing) — Flush_VDP_Shadow early-exit shift-walk; `.emp`+`.asm` twin.
8. **CLOSE: shared 5-site ripple + PROVENANCE re-baseline** (1 commit).

**PARKED with ledger rows (gate to confirm):** Tile_Cache_GetTile (pin-anchor churn, low value),
aabb #1 (no live consumer), dplc D3 (guard-hoist maintainability regression), Tier-C C′ (un-ported
`.asm`). Each gets a row with reason + reopen condition.

**Re-pin cadence:** repin (does pins.rs) runs per byte-changing commit so each stays buildable/
bisectable; the 3 HAND-edited sites (engine.inc ORGs / mixed_dac_rom / repin_pins) + PROVENANCE
re-baseline happen ONCE at close (step 8) per the arc's verification floor. **Gate to confirm**
per-commit-strict vs strict-at-close cadence.

---

## 6 · Scaffolding approach (propose one, with cost)

D7's amendment: ratified-keep scaffolding that is dead-but-intentional (e.g. Plane_Buffer_Reset's
forward reset hook) must be ANNOTATED not deleted. **Recommend comment annotation** (`// @scaffolding:
<reason>` at each kept-dead site), NOT pulling a `@scaffolding` lint forward.
- **Cost of annotation:** ≈0 — documentary lines, no tooling, no gate.
- **Cost of the lint:** a whole dead-symbol/dead-write diagnostic must be BUILT FIRST (D7 was a review
  analysis, never a built lint — it isn't even on the s4lint-absorption Tier-B list), then the attribute
  exemption. Disproportionate for a deletion batch, and there is no lint to exempt from today →
  violates "no net without a consumer" (mirrors row 1111's per-callee-union export deferral).
- **Ledger a row** linking the annotated sites as the ready regression corpus for the future
  dead-symbol lint (diagnostics-tier / s4lint-absorption Tier-B slot).

---

## 7 · Byte / RAM-layout impact estimate

- **RAM:** −1 word (Spawn_Count) −2 bytes (Hscroll_Dirty ×2) −4 bytes (ess ×2 words) = small RAM
  shrink; every RAM symbol after each deletion shifts (address-error risk if any odd/word-align
  interaction — **RAM boot test mandatory**, §8).
- **Code/ROM:** store removals (2 Spawn_Count + 1 CROSS_RESET + 4 Hscroll + 2 ess clrs) shrink code;
  vdp_init M1 early-exit changes Flush_VDP_Shadow length; every region ORG downstream of each edit
  moves → full 5-site ripple. Byte-neutral rows (§1a, VDP module hoist) do NOT move gates.
- **Struct:** EntityScanState `$1A`→`$16` (−4 bytes), all displacements ≥`$04` shift — the largest
  single-row change.

---

## 8 · Verification plan (arc floor)

- **Full paired strict, failures-first**, both shapes, from clean tips (never `grep|head` — surfaces
  FAILED first + explicit pass/fail counts). Baseline gate snapshot re-captured at open.
- **ONE shared 5-site ripple** at close: repin does pins.rs; **HAND-edit** engine.inc ORG table +
  mixed_dac_rom + repin_pins; repin.toml only if a region is added (the `engine.vdp` module is
  source-level, not a new pinned region — confirm no repin.toml touch).
- **ONE PROVENANCE re-baseline** (crc32+size, FINAL bytes both shapes) at close; new canonical CRCs
  recorded in golden/PROVENANCE.md.
- **Attack-the-diff from clean state** — this batch removes RAM + moves a struct (not preserve
  surgery, but RAM-layout motion); attack the diff for any store whose removal was actually live, and
  the EntityScanState shift for a missed displacement.
- **RUNTIME BOOT TEST (mandatory)** — RAM-layout shifts can produce address-error crashes no build
  gate catches. Boot BOTH shapes in oracle (foreground only — never from a subagent), drive the OJZ
  scroll scene (+ the ObjectTest/Churn population for the core/RunObjects Spawn_Count touch), confirm
  no address error and Lag unchanged. Frame-anchored on Frame_Counter.

---

## 9 · Design-gate questions for the overseer

1. **Tier-C:** ratify PARK of A/B/C (evaporated) + C′ (un-ported `.asm`)? Or take children.asm:293 as
   one opportunistic `.asm`-only win?
2. **Tile_Cache_GetTile:** PARK (pin-anchor churn, low value), or delete + re-anchor pins to
   Tile_Cache_GetCollision same commit?
3. **aabb #1:** PARK (no live consumer) or do the byte-neutral split anyway?
4. **dplc D3:** PARK (guard-hoist regression) or hoist for the ~65c/frame player-art saving?
5. **Re-pin cadence:** per-commit-strict (repin per byte-changing commit) vs strict-only-at-close?
6. Confirm the surviving batch + commit order (§5) and the comment-annotation scaffolding call (§6).

**No code until this gate rules.**
