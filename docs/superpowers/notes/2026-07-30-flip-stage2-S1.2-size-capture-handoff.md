# 2026-07-30 — FLIP STAGE 2 · S1.2 size-capture + the driver handoff (overseer-blessed)

Status: **the overseer BLESSED the declared-size approach (3 conditions). The two
time-critical pieces are DONE and committed: the generalized chainer (proven, sonic4
both shapes 0-diff) and the PERISHABLE off-canonical size captures (frozen while asl is
live, CRC-tied to all four goldens). The three off-canonical DRIVERS + the 6×2 matrix
remain — the multi-session lift the overseer's valve anticipated ("honest stops with
committed size-table captures FIRST; the driver code can always be rebuilt, the
captures cannot after the flip"). This note is the driver build plan + the standing of
all three conditions.**

Sigil `flip-stage2` tip after this work; aeon read-only `bcb8f64`. Strict **2919/0
(1 ignored)** both environments; asl default byte-identical; zero aeon commits.

## The three conditions — standing

**Condition 1 (sizes are frozen asl-derived constants, captured now, committed with
provenance): DONE.** `crates/sigil-harness/golden/offcanonical_sizes/{demo,demo_debug,
config_a,config_b}.txt` — each a frozen `LABEL 0xADDR` table with a provenance header
tying it to the golden CRC it reproduces. Captured by `capture_offcanonical_sizes.sh`
(anti-stale: fresh positional build → parse via the authoritative `repin::parse_listing`
→ assert-newer → record). `capture_offcanon` (`src/bin/`) is the parser reuse. CRCs
verified == the brief: demo 18c64002 · demo.debug b0475a59 · config_a b4a6756d ·
config_b 92776720. A region's declared SIZE = `addr[end] − addr[start]`; absent labels
(sound-off `sound_api`, the `games.sonic4.*` game modules in demo) are omitted.

**Condition 2 (post-flip semantics — stated plainly): here.** The committed sizes are
**golden provenance** — they exist to reproduce the FROZEN goldens, and they are **the
last asl-derived constants in the system**. They are NOT permanent pins:
- Pre-flip and immediately post-flip they reproduce the asl-derived goldens byte-for-byte.
- When a post-flip byte-changing parcel deliberately re-baselines a golden (the §17
  optimization sweep WILL), the sizes **re-derive from SIGIL'S OWN layout** as part of
  the same ruled re-baseline — sigil's relaxer settling at a tighter fixpoint than asl
  is then LEGITIMATE (arguably better code). The re-derivation is mechanical: build the
  target natively, read each emp region's resolved `image_len`, freeze that as the new
  size (exactly the sonic4 pinned-pass-1 mechanism, now sourced from sigil not asl).
- **Retirement path:** once every target's goldens have been re-baselined off asl at
  least once, the asl-derived captures are fully superseded; nothing ever requires asl
  again. The capture header wording ("PERISHABLE… reproduce the golden below") and this
  note are the anti-fossilization record — these tables must never become mysterious
  pins.

**Condition 3 (t24 control on the size-table plumbing): SPEC'd, lands with the drivers.**
Each off-canonical driver gate must carry a doctored-size negative control: mutate one
region's size in the loaded table (e.g. +2) and assert the resulting ROM DIVERGES from
the golden (the byte gate goes red loudly) — proving the gate actually consumes the
committed size and would catch a corrupted table. This mirrors the sonic4
declaration-matches-reality re-proof (`native_declared_chain` re-proves the computed
layout against the frozen golden). Pattern: a `#[test]` that calls the driver with a
`size_override(region, delta)` hook and `assert_ne!` on the header-neutral diff count.

## The driver build plan (the remaining lift — demo → Config-A/B → matrix)

The generalized chainer (`native::build_native_rom_chained`) is the placement engine.
The per-target work is a parameterized sibling of the sonic4 native driver that (a)
builds the target's AS residual + emp set, (b) sources declared sizes from the committed
table instead of a pinned pass-1, (c) chains, (d) proves vs the frozen golden.

### Refactor needed first (sonic4-hardcodings to parameterize)

`build_native_emp` / `assemble_native_all_gates_as_side` / `registry` are sonic4-shaped.
Extract a `GameProfile` carrying: the game root (`games/<g>/main.asm`), the emp registry
(engine-only for demo; sonic4 keeps its game modules), `SOUND_DRIVER_ENABLED` (0 for
demo — no DAC/MT/SFX BINCLUDE, `ensure_generated` a no-op), the AS_OWNED_KEYSTONES set
(demo: `demo_box`/`demo_state`, NOT player_common/test_player/test_enemy), the
comptime defines (demo's `config/game.asm` may not define `GAME_CAMERA_JUMP_LOCK` —
verify), the OBJDEFS `"text"`-guard applicability (demo has no objdefs.emp → the
"exactly one non-empty text" guard must relax/skip), and the drift-guard allowlist
(per-target NAMED, both-direction — demo/Config gate off the same engine `.asm`, so
likely the same VRAM_PLANE_B_BYTES / CAM_SCREEN_HALF set — VERIFY, don't assume).

### Demo specifics (from `2026-07-30-flip-stage1-demo-*` + this capture)

- Demo is **sound-OFF**. Watch the `demo-config-native-blocked` note's second
  consequence: sound-off drops sound-provided symbols (`SFXID_*`). Any engine `.emp`
  that references a sound symbol must resolve it in the demo joint link the SAME way the
  demo golden's `.asm` does (the demo golden builds sound-off cleanly, so the twins
  already handle it — the emp side must match). This is the most likely byte-identity
  fight; probe it first.
- Demo `gameEngineBlockIncludes` is EMPTY (sonic4 put `player_sensors` there) →
  `player_sensors` is a sonic4 game module, ABSENT from the demo registry.
- The demo emp set = the pure `engine.*` modules present in demo's layout (38 boundary
  labels captured — cross-check the emp registry against them; `sound_api` absent, the
  `games.sonic4.*` game modules absent).
- Declared sizes: for each demo engine region, `size = demo.txt[end] − demo.txt[start]`
  (the fixed-len regions — vectors 0x100, bg_anim, etc. — keep their shape-invariant
  len). AS island sizes stay `image_len` (address-free). Genuine anchors: object bank
  `ObjCodeBase` 0x10000 (from the table), no phased sound bank (sound-off).
- Prove demo plain/debug native == 18c64002/90776 (anchor cfda98d3) · b0475a59/91584
  (anchor 20c5571d), + the S1.4 split-golden machinery (determinism, de-b2 presence,
  convsym spot-check of demo symbols) + condition-3 t24. Freeze the sigil-canonical full
  files as new provenance.

### Config-A/B specifics

- The `mixed_offcanonical_rom` defines: CONFIG_A = `__DEBUG__` + SOUND_DRIVER_ENABLED +
  SOUND_DEBUG_HOTKEYS + SOUND_DBG_MIRROR (game_debug + sound_debug NON-empty); CONFIG_B
  = SOUND_DRIVER_ENABLED=0 (z80_init no-sound else-arm). Both are SONIC4 (full game emp
  set) — the registry is sonic4's, PLUS game_debug/sound_debug (Config-A) / z80_init
  (Config-B) as their own placed modules with their own gates.
- Declared sizes from `config_a.txt` / `config_b.txt` (69 / 66 labels). The off-canonical
  region sizes (game_debug `Debug_MusicToggle..Section_Init`, sound_debug
  `Sound_DebugMirror..CarrierMaskTableZ`, z80_init `Z80_IdleProgram..Z80_IdleProgram_End`)
  are in the tables.
- Per-config NAMED drift-guard allowlists (both-direction hard-fail), like the Stage-1
  `STAGE1_INAPPLICABLE_GUARDS`.
- Prove vs config_a.bin b4a6756d/421898 (anchor 3d9bac53) · config_b.bin 92776720/304961
  (anchor fd3f7f8e); freeze sigil-canonical full files.

## The 6×2 matrix (pre-flip)

| target | assembled anchor | full file | size source |
|---|---|---|---|
| sonic4 plain/debug | ✅ `native_rom` + ✅ `native_declared_chain` | ✅ `native_full_rom` | pinned pass-1 |
| demo plain | ⏳ driver | ⏳ | ✅ `demo.txt` (frozen) |
| demo debug | ⏳ driver | ⏳ | ✅ `demo_debug.txt` (frozen) |
| config_a | ⏳ driver | ⏳ | ✅ `config_a.txt` (frozen) |
| config_b | ⏳ driver | ⏳ | ✅ `config_b.txt` (frozen) |

## The valve

The perishable asl truth (the size tables) is SECURED and committed — the one thing
that could not be recovered after the flip. The chainer is proven. The drivers are
rebuildable code with a proven engine, frozen data, and a written plan; they are the
next session. asl default byte-identical throughout; zero aeon commits; the flip commit
remains the overseer's.
