# t22 loop pass 1 — full 3(a)/3(b)/4/5 interrogation record (compression cluster)

Per campaign-port-loop; every line walked, outcome named. Files: s4lz_decompress.emp ·
zx0_decompress.emp · compression_selftest.emp (+ .asm twins).

## Step 3(a) — language/format asks (each line run)

- **Ceremony scan**: (1) the two 14-entry unrolled `move.w (aN)+, (a1)+` blocks
  (s4lz) — high lines-per-intent BUT they are computed-jump LANDING SLOTS
  (`jmp .lit_end(pc,d1.w)` enters mid-block; the 2-byte stride IS the
  `add.w d1,d1` displacement math) → adjudicated at step 4, NOT an ask on its
  own. (2) TileDelta's 8× `move.l`/`eor.l` pairs — the corpus's FOURTH
  spelled-inline-because-no-rept site (core.emp:36, entity_window.emp:114,
  parallax.emp:320 name the same absence) → **ASK (class (c), ledgered as a
  demand increment): an emit-time `rept(n) { code }` construct** — big
  (grammar+lowering), no stopgap hand-built per the verb-(c) rule.
  (3) selftest's 3 per-vector blocks — step-4 adjudicated.
- **Comment-as-compensation**: "count * 2 bytes per move.w instruction" — the
  language cannot state the stride/displacement coupling; folds into the same
  rept/unroll construct family (no separate ask). zx0's per-line bit-threading
  comments are inherited algorithm documentation (Marty), not compensation.
- **Escape-hatch census**: ZERO extern()/call-expr escapes; ONE ensure
  (TILE_SIZE drift lock — the standard mirror class); zero transliteration
  blocks. Cleanest census of any tranche to date.
- **Domain-type scan**: d3/d4 (size/dict-len) + zx0's d1 bit-queue / d2
  negative-offset — all shift/add-chain values, A4-i-gated → LOG (no existing
  newtype covers a moved-AND-compared value here; no new candidate earns a row).
- **Noticing**: the twin-side `ifdef`-width spelling for shape-divergent
  relaxations is now at its THIRD instance (t19 bg_anim, t21 vblank sound-off,
  t22 s4lz ×2) — proposed as an explicit step-2 checklist item-1 sub-line
  ("shape-divergent relaxations ride twin ifdef widths; the .emp stays bare")
  rather than precedent-by-memory. For the packet/ratification.

## Step 3(b) — reads-wrong (each line run)

- **Comment-claim audit**: ALL header claims verified against the code as it
  stands — the ≤32766 suba.w sign-extension bound; d4 ≤ 2304 positive; the
  dict-rebase trace (both endpoints re-derived); EOS-word-consumed evenness;
  odd-size pad landing (a1 = declared+1); "a4/d4 untouched" vs load_art's
  LIVE d4-after-call reliance (Level_LoadArt reads d4 into d7 AFTER
  Art_Decompress — the claim is load-bearing and true); zx0 "88-byte" = $58
  region ✓; selftest poison rationale ✓. **ONE REAL FINDING**: the
  `out(a1)` contract on the tile-delta path survives only because
  TileDelta_Undo's XOR walk ENDS at buffer_end = the decode's own a1
  endpoint (size is a multiple of 32) — a coincidence-by-construction that
  neither twin named. FIXED zero-byte: site comment at the call, BOTH twins.
- **Contract audit**: every header license re-derived including transitive
  callees (TileDelta ⊆ shared-body license with the a0 save/restore bracket;
  selftest's union covers both callees' licenses; zx0 §5-machine-verified).
  The DEBUG dict-range assert reads d4 on the shared body's dict-hit branch —
  garbage-d4-on-plain-entry documented by the header's "branch never fires on
  valid no-dict streams" ✓.
- **Name audit**: labels say what they do; zx0's `.got_elias`-as-shared-exit
  is named in the new preserves header comment. No renames.
- **Magic-number audit**: every literal commented (#15 escape, #$0F masks,
  #$A5A5 POISON, #-128 queue init, #-2/$fe, version #1). **ONE FINDING**:
  `lsr.w #5` hardcoded log2(TILE_SIZE) with no relationship comment — FIXED
  zero-byte both twins ("bytes/TILE_SIZE; 32 = 1<<5, drift-locked by the
  ensure").
- **Cold-reader test**: one decode traced on headers+comments alone — the
  computed-unroll entry is the only point needing the site comment, which is
  present. PASS.
- **Codename-reference audit**: no session codenames in any shipped comment;
  anchors are spec §5, the corpus gate, and twin-spelling facts. PASS.

## Step 4 — construct pass (all adjudications named)

- **NOT ADOPTED/NOT BUILT — the 14-entry unroll blocks ×2**: the rows are
  stride-locked jump-table landing slots (the bra.w-table class transposed to
  data-movement); a construct would hide the 2-byte stride the `add/neg/jmp`
  arithmetic depends on, and the twin spells them inline (byte-lockstep is
  line-for-line readable today). Kept explicit with entry comments.
- **ASK not build — `rept(n) { code }`** (TileDelta 8× pairs): 4th corpus
  demand site; class (c) — grammar-level, its own design; no stopgap.
  Ledgered (demand increment on the emit-time-repeat family).
- **NOT BUILT — selftest `check_vector` template**: 3 sites, ONE debug-only
  boot-cold file; the poison/decode/checksum/assert/compare sequence IS the
  test's documented protocol — a template would hide the protocol from the
  cold reader of the test itself (protocol-visibility beats dedup at this
  size; t20 size-bar discipline). Logged as demand data for the
  skeleton-with-holes loop-template family.
- **DELETE pass**: none (TileDelta internal-but-called; no orphans).
- **Kill rows**: no new mirrors this pass (TILE_SIZE = row 44, born at s1).

## Step 5 — FULL interrogation (per hot proc; heat: S4LZ shared body =
tile_cache streaming path (~3.4k cyc/f warm incl., cold-crossing spikes —
rows 1057/1074 FENCE: the unified-prefetch charter owns it); S4LZ plain entry
+ ZX0 = load-time (Level_LoadArt, blanked-display window); TileDelta =
per-decompress tail; selftest = boot-cold debug-only)

- **Invariant ladder** (shared body): every per-token instruction consumes
  the token (rol/andi extraction, the two #15 escapes); a3/a4 held in
  registers across the loop (nothing recomputed); the unrolled copies ARE the
  iteration-scope optimization. ZX0: bit-queue (d1) and rep-offset (d2)
  register-resident for the whole call — nothing below its scope. NO HOIST.
- **Counter/cache audit**: no counters/budgets/caches in any of the three
  files (d3 size written once, read once at stream end). N/A.
- **Guard-coverage audit**: the $0000 EOS token is the SOLE stream
  terminator (load-bearing — named in the header's token map); the DEBUG
  dict-range assert covers exactly the dict-hit emission path; zx0's EOD
  marker (offset high byte -1) is the sole exit — all on their only paths ✓.
- **Hardware cross-check**: RAM/ROM-only code, no VDP/DMA/IRQ surface in any
  of the three files. The streaming-context interplay (VBlank window,
  Tile_Cache_Fill) is the PANEL C3 named input, not a step-5 line here.
- **Silent-tradeoff comments**: (a) +16 cy/match dict-check shared with the
  plain entry — header-commented as CHOSEN ✓; (b) the a1/TileDelta endpoint
  coincidence — NOW commented (pass-1 fix); (c) zx0 byte-granular dbf copies
  — the load-time tier choice, header-commented ✓. No uncommented compromise.
- **Threshold ruling: NO CUT — measured no-cut expectation confirmed by
  inspection; nothing within reach of ≥1k cyc/f outside the fenced
  streaming charter** (the only ≥1k figure in play is DecompressBlock's
  ~3.4k warm inclusive, which the t16 ruling assigns to prefetch
  amortization, not decode micro-opt). NO named oracle probes required —
  no candidate needs live numbers.
- **Noticing**: none new.

## Pass-1 shipped output (byte-neutral, CRCs unchanged
06290799/e280a49b): 2 comment fixes both twins (a1-coincidence,
lsr-#5/TILE_SIZE) + the step-0 note flip-sketch correction (overseer ruling
2). Ledger: rept-construct demand increment + the noticing proposal go in
the close packet's rows.

## Pass 2 (fresh re-walk after pass-1 output): EMPTY at all three steps —
pass-1's output was comments and ledger rows only (no code motion, no new
constructs, no contract changes to re-audit; the 3(a)/3(b) lines re-run
against the amended files surface nothing new; step 4 has no new patterns;
step 5's ruling unchanged). → DRY CLAIM; panel dispatched (A1+B1+C1+C2+C3
per the countersign's named inputs).
