# 2026-07-29 — t27 brief: Z80 ladder rung 0+1 — the satellite ports

Status: **DISPATCH BRIEF** (overseer: Fable; porter: Opus subagent, direct-dispatch per the
standing amendment). Target ruled by the overseer from the §5 ladder in
`2026-07-28-z80-recon-emp-design.md` (the ladder as ruled 2026-07-28) and the T1 design note
`2026-07-28-z80-t1-operand-model.md`. T1 is MERGED (sigil `c712f17`); the split streams meet
here — rung 1 is unblocked and is the next tranche.

## 0. Bars (verified by the overseer 2026-07-29 before dispatch)

- Masters: aeon **`6e25b6e`** / sigil **`c712f17`**, origin==local both, working trees clean.
- Canonical: plain **`c51342d0`/421041** · debug **`992d9e7d`/429102** (`s4.bin` / `s4.debug.bin`
  — hash the right file). EndOfRom `0x5DB60`/`0x5F65A`.
- Strict baseline: **2654/0** paired (AEON_DIR at the matching tree), failures-first output,
  explicit pass/fail counts — never `grep|head` the run.
- **EXPECTED BYTE MOVEMENT: ZERO at every commit.** The blob precedes the engine — one Z80
  byte slides the whole corpus. Any nonzero delta is **STOP-and-report to the overseer**,
  never absorb (the t25 rule).
- Branches: `port-tranche27` in BOTH repos, worktrees under `.worktrees/port-tranche27`,
  off the masters above. Editor-dir rsync (`games/sonic4/data/editor/`) into the aeon
  worktree BEFORE the first build; verify CRC before trusting anything. One shape per build
  invocation (plain vs `DEBUG=1`). Never `git add -u` in a main checkout; explicit paths;
  check branch before every commit; never chain two repos' git commands in one `cd`-prefixed
  compound.
- Checkpoint discipline: **steps 0-2 ONLY, then STOP** for checkpoint (a) — the overseer
  countersigns with its OWN dual rebuild + OWN strict run before the loop is authorized.
  Checkpoint (b) after passes+panel; merge gate (c) opens only on overseer instruction.
- Canonical loop text: `notes/campaign-port-loop.md` (including the $8000 bank-shift bar,
  the positive-control rule for negative probes, the step-2 house-spelling checklist —
  jbra/jbsr, raise_exception, dc.l label — and the brace-indent style). Comments describe
  function, never change history.

## 1. Scope ruling (LEAN, three lanes + the rung-0 probes folded in)

t27 = **rung 0 + rung 1** of the Z80 ladder. The rung-0 probes are not a separate tranche;
they are the step-0/step-1 proof machinery of these ports.

| Lane | File | Port class |
|---|---|---|
| A | `engine/system/z80_init.asm` (38 L) → `z80_init.emp` | **FIRST Z80 CODE PORT.** First live `module (cpu: z80)`. Config-shape class (see §2). |
| B | `engine/sound/seq_opcode_tab.asm` (68 L) → `.emp` | Banked data: 32 × `dw <resident-label>` = symbolic LE cells through the $8000 window (`Value16Le`). Canonical bytes. |
| C | `engine/sound/dac_sample_tab.asm` (110 L) → `.emp` | Banked data: `db`/`dw` comptime `SND_*` constants + `if/fatal` size guard → comptime equivalent. Canonical bytes. |

**EXCLUDED, on the record:** `engine/sound/sound_tables_z80.asm` (116 L) — GENERATED by
`tools/gen_sound_tables.py` and included both resident and banked. Porting a generator's
output is the wrong seam; the real conversion is the generator emitting `.emp` (or the
data-table DSL single-sourcing it). **Ledger row owed** (generated-file ownership class);
do not port it in t27. The five resident code files (driver/sequencer/sfx/fm/psg) are
rungs 2-4 — out of scope; touching them is a STOP.

## 2. Overseer recon (verified against the live trees 2026-07-29 — porter re-verifies at step 0)

- **Lane A is the t26 game_debug config-shape class.** `boot_data.asm:47-51`:
  `ifdef SOUND_DRIVER_ENABLED` includes the sound driver blob, **`else`** includes
  `z80_init.asm`. Both canonical shapes have sound enabled → **z80_init emits ZERO canonical
  bytes**. Proof = the t26 lane-B template: canonical-emptiness both shapes + an
  **off-canonical no-sound-shape oracle** (asl vs sigil at the no-sound config, the t21/t26
  class). If whole-ROM mixed placement at the off-canonical shape balloons, take the t26
  deviation shape: module + canonical-emptiness + oracle at the scale that fits, and **bump
  the deferred whole-ROM off-canonical machinery ledger row to DEMAND 3**
  (sound_debug, game_debug, now z80_init) — one shared build serves all three later.
- **Lanes B/C are canonical bytes** inside the banked head: `sound_bank.inc:36-37`, included
  from the `save / cpu z80 / phase 08000h` block (`games/sonic4/main.asm:201-217`,
  `soundBankHead`). The seam design (gate arms, section placement at the banked LMA, cursor
  contract vs the surrounding head) is **step-0 design-note work** — named options: include-site
  ifndef gate + org-advance twin arm (the t23 boot_data cursor-contract class, kill-row
  precedent 46) vs chained-section split. STOP if no clean seam exists.
- **Operand coverage is already proven.** T1's §1.1 demand table was derived from z80_init
  line-by-line (verified: the file matches — `xor a`, `ld bc,(expr)`, symbolic
  `ld de/hl,Label±k`, `ld sp,hl`, `ld (hl),a/$E9`, `pop ix/iy/pairs`, `ld i/r,a`,
  `ex af,af'`, `exx`, `im 1`, `jp (hl)`, `di`, `ldir`). Lanes B/C demand NOTHING from the
  operand model — they prove the banked section + LE-dw + link-value story. Any operand form
  outside T1's wired set is a demanded-feature TDD event or a STOP, not an improvisation.
- **`Z80_IDLE_SIZE`** (`z80_init.asm:38`) is consumed as an `extern()` const from t25's
  sound-off arms — the flip/mirror status of that symbol is a step-0 must-answer
  (check the t25 packet + kill list before inventing anything).

## 3. Named probes (positive controls per the t24 rule — every negative probe proves it CAN fail)

- **P1 — LE `dw` at ROM scale**: `.emp` `data` in a `(cpu: z80)` section emits
  little-endian at full-ROM scale. **Closes gap-ledger row ~825-831** ("unprobed on Z80").
- **P2 — banked section end-to-end**: `(cpu: z80, vma: $8000)` section + `winptr()`/
  `bankid()` link values land byte-identical at the banked LMA.
- **P3 — symbolic imm16 in code at ROM scale**: `ld de, Label+1` → `Value16Le` fixup
  resolves at link against the phase-0 address space (T1 wired it; t27 proves it in a ROM).
- **P4 — pins round-trip**: a `cpu: z80` section survives the pins/repin machinery
  (byte-neutral tranche → no repin wave expected; the probe is the round-trip, not a slide).
- **P5 — the `if/fatal` size guard** (dac_sample_tab) has a comptime `.emp` equivalent that
  FIRES on a doctored size (falsification verified).

## 4. Panel ruling

**A1 + B1 + C2.** C1 inactive-recorded (idle program + cold data tables — no hot path,
no cycle budget). C3 **conditional**: activate ONLY if step 0/2 makes claims about the
banked-window house rule (`no Z80 code executes from the $8000 window — data only`,
`sound_bank.inc:13-17`) or Z80 bus contention; otherwise inactive-recorded. Lens subagents
synchronous (the t22/t23 pin). Panel runs at the end of the pass loop, before checkpoint (b).

## 5. Duties carried

- Kill-list rows born SAME-COMMIT for every twin mirror/scaffold created (org-advance arms,
  size mirrors, gate ifdefs); each with a kill condition.
- Gap-ledger sweep at step 3 per pass; the generated-file row (§1) and any demand-increment
  rows (banked-pointer types, u16be cells if encountered) land there — jot if not implement.
- Close packet ends with per-pass step-3 vs step-5 findings + neither-bucket headlines.
- Two overseer brief errors were caught by porters in t25 — if this brief contradicts the
  tree, **the tree wins; flag it on the record** (it goes in the corrections list).
- Type layer: step-2 item-6 walk applies. Z80-side newtypes (banked-pointer provenance etc.)
  are LOG-ONLY this tranche (LEAN bar; the recon's demand classes land with rungs 2+).

## 6. After t27

Rung 2 (contracts + jr→jp ladder, psg then fm — the module-`invariant` design call lands
there, plus the banked RUNG-2 TEST OBLIGATION from the T1 note §0: the
`Value::Z80Reg`-in-a-68k-section source-level splice test when register params land).
Parallel option: game-side tranches (~18) continue on the proven machinery.
