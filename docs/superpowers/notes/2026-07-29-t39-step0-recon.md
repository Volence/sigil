# 2026-07-29 — t39 step-0 recon (the FINAL three objects)

Porter: Opus subagent (direct-dispatch). Masters: sigil `ea38f7c` / aeon `fa474cd`.
Branches `port-tranche39` both repos, worktrees `.worktrees/port-tranche39`. Editor
data rsync'd. Baseline verified: plain **4b66cace/421041**, debug **1c256b3b/429102**;
strict **2856/0 (1 ignored)**. CANONICAL-BYTES tranche — step-1 delta ZERO expected.

Scope: `test_enemy.asm`, `test_player.asm`, `path_swap.asm`. After t39 the object bank
is ALL-.emp.

## Census corrections (the standing corrections list — census/brief were wrong)

1. **test_enemy cross-seam calls.** Census §3a: "Draw_Sprite, ObjectMove, TouchResponse".
   TREE: `jsr ObjectMoveX` (line 40) + `jmp Draw_Sprite` (line 63) — **NO TouchResponse,
   NO ObjectMove**; it is **ObjectMoveX**. Both callees are `pub proc` in
   engine.objects.core (`ObjectMoveX` core.emp:644 clobbers(d0)) / sprites
   (`Draw_Sprite` sprites.emp:55 clobbers(d0-d3/a1)).

2. **test_player asserts + sound.** Brief §1: "3 asserts → ensure" + "Sound_PlaySFX typed
   call sites". TREE: **ZERO asserts, ZERO Sound_PlaySFX** (grep-confirmed). Cross-seam
   calls are ObjectMove (core.emp:626), Player_SensorFloor (player_sensors.emp:293,
   MODULE-TO-MODULE game→game), AnimateSprite (animate.emp:81), Perform_DPLC (dplc.emp:99),
   Draw_Sprite. The file is SIMPLER than the census claimed (no `ensure` demanded, no
   sound seam). Brief §1 also says "PlayerV overlay import from player_common.emp" —
   **WRONG**: test_player does NOT touch PlayerV / `_pl_*`. It uses its own DplcV/TPlayerV
   overlay (`_dplc_ptr`/`_art_base`/`_debug_flag`) only.

3. **path_swap cross-seam surface.** Census §3a "Draw_Sprite, section calls" + brief §1
   "Section calls = the cross-seam surface". TREE: **NO section calls** (grep-confirmed).
   Surface = `Draw_Sprite` (bare link), `Player_1` (RAM symbol, pinned), `RaiseError`
   (→ `raise_error` construct). Consequently **brief §3's "C3 ACTIVE for path_swap
   (section/camera claims)" has no basis** — path_swap touches no VDP/DMA/bus and its prose
   makes only coordinate-handling BEHAVIORAL claims (teleport rebase, side-tracking), not
   hardware/timing/bank/section/camera claims. Per the codified panel-activation rule
   ("C3 activates whenever ported PROSE makes hardware claims"), C3 is a FLAGGED-inactive
   call for the dry panel, reversible at the gate — recorded here.

## Per-shape addresses (from s4.lst / s4.debug.lst, this worktree)

| Region | start sym | plain base | debug base | plain end sym | plain len | debug len |
|---|---|---|---|---|---|---|
| test_player | `TestPlayer` | $10C92 | $10C92 | `TestEnemy_Init` $10F02 | $270 | $270 |
| test_enemy  | `TestEnemy_Init` | $10F02 | $10F02 | `TestSolid_Init` $10F4A | $48 | $48 |
| path_swap   | `ObjDef_PathSwap` | $111FA | $111FA | `DeformTable_Zero` (plain $1128C / debug $112F4) | $92 | $FA |

- test_player / test_enemy: **shape-INVARIANT** (bases and lens identical both shapes).
- path_swap: **shape-DEPENDENT** — debug +$68 (104 B): the `if DEBUG==1` RaiseError block
  (btst+beq+`raise_error` expansion) + the tail `jmp Draw_Sprite` (4 B debug) vs `rts`
  (2 B plain). `DeformTable_Zero` is the first gameDataIncludes label (ojz_default.asm) =
  path_swap's next placement; it exists in both listings so repin auto-derives per-shape
  lengths (the boot/vblank start/end precedent).

## Overlay reader-sets + gate class (per the guarded-vs-unguarded rule)

- **`_enemy_steps`/`_enemy_direction`/`TEnemyV`** — read ONLY by test_enemy.asm →
  **UNGUARDED** TEnemyV overlay (row-66 single-consumer class). WHOLE-FILE gate.
- **`_ps_half_height`/`_ps_prev_side`/`PathSwapV`** — read ONLY by path_swap.asm →
  **UNGUARDED** PathSwapV overlay. WHOLE-FILE gate (shape-dependent).
- **`_dplc_ptr`/`_art_base`** — read by test_player.asm (owner+user), test_animated.asm
  (gated twin), **AND `test_animated.emp`'s drift guards** (`extern("_dplc_ptr")`,
  `extern("_art_base")`). main.asm:117 records the design fact: with SIGIL_EMP_TEST_ANIMATED
  set, **test_player.asm is the surviving AS provider** of these equates for the .emp guard.
  `_debug_flag` — read ONLY by test_player.asm.

### Row-74 decision: test_player gets an INTERNAL gate (t34 keystone)

Porting test_player.asm with a whole-file gate would REMOVE the AS-side `_dplc_ptr`/
`_art_base` that test_animated.emp's drift guards resolve against (breaking the mixed_tranche29
build). Per the t34 INTERNAL-gate keystone: keep the **zero-byte header** — the DplcV
`struct` (`ifndef _dplc_ptr`), the TPlayerV `struct`, `_debug_flag` equate, `objvarsCheck` —
**always emitted**; gate ONLY the physics constants + `TestPlayer`/`TestPlayer_Main`/
`TestPlayer_Debug` code, with a per-shape-invariant `org $10F02` resume. This preserves the
AS provider so test_animated.emp's guards keep resolving unchanged (no edit to the merged
file). test_player.emp owns a `vars TPlayerV: Sst.sst_custom { dplc_ptr:u32, art_base:u32,
debug_flag:u8 }` overlay and carries its own drift guards for the 3 fields (the surviving AS
twin is its own lockstep partner).

**Reader-set update (close-packet duty):** porting test_player REMOVES its object-code as an
AS `_dplc_ptr`/`_art_base` reader, but its HEADER survives (internal gate), so the AS-side
equate SET is unchanged — test_animated.emp's guards still have a provider. The only AS files
now consuming DplcV are the two surviving headers (test_animated.asm gated, test_player.asm
internal-gated header); both remain until Spec 5.

## Contracts (honest-derivation notes; byte-gate-blind, tune against the closure gate)

- **TestEnemy_Init** falls_into TestEnemy_Main; **TestEnemy_Main** = ObjectMoveX(d0) ∪
  Draw_Sprite(d0-d3/a1) ∪ local(d0) = **clobbers(d0-d3/a1)** (matches the AS header).
- **TestPlayer_Main**: the AS header "Clobbers: d0-d7, a0-a6" **CONTRADICTS the file's own
  comment** (lines 71-73: object routines MUST preserve a0/d7 — RunObjects loop contract;
  d7 is save/restored around Player_SensorFloor). a0/d7 are PRESERVED, a4-a6 untouched.
  Honest union ⊇ d0-d6/a1-a3; d7 tuned against the closure gate's save/restore modelling
  (over-claim = safe direction if the gate cannot see the push/pop). Flagged as a step-3(b)
  contract-audit finding for the loop.

## Constructs / seam facts

- Object template (test_solid/test_particle/test_animated): `use engine.objects.sst.{Sst}`,
  bare field access, `jbra`/`jbsr` (emit jmp/jsr abs when out of bra range — byte-identical
  to the twins' `jmp`/`jsr`), `move.w #Foo_Main - ObjCodeBase, code_addr(a0)` dispatch store,
  UNGUARDED/GUARDED `vars` overlay per reader-set.
- path_swap emits its INLINE objdef: `pub data ObjDef_PathSwap: ObjDef = objdef(code:
  "PathSwap_Init", map: "Map_TestObj", art: vram_art(VRAM_TEST_OBJ), width:4, height:64,
  collision: COLLISION_NONE)` (26-byte record, $111FA..$11214) then the two procs.
  `raise_error "<msg>"` inside `if DEBUG==1 {}` (load_art.emp precedent). Needs
  VRAM_TEST_OBJ ($3E0) + COLLISION_NONE (=0) with drift guards (test_objects.emp pattern).
- No new frontend/language feature demanded; no new extern proc — the game side stays at
  ZERO extern proc (all callees are `pub proc`, module-to-module incl. Player_SensorFloor).

## Harness plan (step 1)

- 3 per-file gates `SIGIL_EMP_TEST_PLAYER` / `SIGIL_EMP_TEST_ENEMY` / `SIGIL_EMP_PATH_SWAP`;
  3 repin.toml regions (path_swap start/end → auto per-shape); pins.rs regenerate.
- One test file `test_g4_final_objects_port.rs` (windowed gates BOTH shapes; path_swap gets
  SEPARATE per-shape length assertions; negative probe + positive control; outbound
  objroutine word for TestEnemy_Init; drift guards asserted).
- One whole-ROM `build_mixed_tranche39_rom` + `assemble_mixed_tranche39_as_side`
  (SIGIL_EMP_TEST_PLAYER + TEST_ENEMY + PATH_SWAP), both shapes, t24 $8000-bar control.
- main.asm: gate test_enemy (org $10F4A) + path_swap (per-shape org) + internal-gate
  test_player (header always-on, code gated, org $10F02).

STOP condition (demanded feature / census contradiction) NOT tripped — corrections are
census OVERSTATEMENTS (simpler files) + surface mislabels, resolved per the ladder. Proceeding
to step 1.
