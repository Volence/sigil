# 2026-07-29 — t27 step-0 design note + STOP-at-step-0 report (Z80 rung 1 satellites)

Status: **STEP-0 RECON COMPLETE → STOP-AND-REPORT to the overseer.** Porter = Opus
subagent, direct-dispatch (Fable). Brief: `2026-07-29-t27-z80-rung1-brief.md`. This note
is committed BEFORE any code (the step-0 discipline) and records the design finding that
re-scopes the tranche: **the tranche premise "T1 is done; the satellites just need porting
(they demand nothing new)" is materially false against the live tree.** Each lane demands a
sigil-side capability that T1 deferred. Two of those deferrals are documented in T1's own
note/code as future work; the brief inherited T1's over-claim. Per the LEAN balloons rule,
"STOP if no clean seam exists," and the demanded-feature-scoping rule ("any operand form
outside T1's wired set is a demanded-feature TDD event or a STOP, not an improvisation"),
the correct action is an overseer scoping decision before building three language features
inside a "byte-neutral LEAN" tranche.

Setup verified before recon (bases, ROMs, strict all match the brief §0):
- Branch `port-tranche27` both repos, off aeon `6e25b6e` / sigil `f43d5fc`.
  (Brief §0 says sigil master `c712f17`; the TREE has sigil at `f43d5fc` = the brief commit
  itself, doc-only — the dispatch message's base. **Discrepancy flagged; tree wins; used
  `f43d5fc`.**)
- Editor-dir rsync'd into the aeon worktree. Plain build `c51342d0`/421041; debug
  `992d9e7d`/429102 (both match). Strict baseline **2654/0** (2 ignored) with `AEON_DIR` at
  the aeon worktree.
- No aeon source changed; no sigil code committed (only doc). Byte-neutrality is trivially
  held (nothing built differently).

---

## 1. The three lanes, mapped to the live tree

| Lane | File | Class | Step-0 verdict |
|---|---|---|---|
| A | `engine/system/z80_init.asm` (38 L) → `.emp` | first Z80 CODE port; canonically empty (no-sound arm) | **BLOCKED on two sigil walls** (§2) |
| B | `engine/sound/seq_opcode_tab.asm` (68 L) → `.emp` | banked `dw <resident-label>` ×32; canonical bytes | **BLOCKED on one sigil wall** (§3) |
| C | `engine/sound/dac_sample_tab.asm` (110 L) → `.emp` | banked `db`/`dw` comptime `SND_*` + `if/fatal` guard; canonical bytes | **data lowers CLEAN; blocked only on the banked-head SEAM** (§4) |

All findings below are EXECUTABLE — each was reproduced with a throwaway probe test
(`crates/sigil-frontend-emp/tests/zzz_t27_probe.rs`, run then deleted). The T1 test harness
(`z80_operands.rs`'s `z80_body`) is the vehicle for the code probes.

Placement facts (verified in the tree):
- Lane A: `boot_data.asm:48-52` — `ifdef SOUND_DRIVER_ENABLED` includes the driver blob,
  `else` includes `z80_init.asm`. Both canonical shapes have sound ENABLED → z80_init emits
  **zero canonical bytes** (the game_debug config-shape class, confirmed).
- Lanes B/C: included by the `soundBankHead` macro (`sound_bank.inc:36-37`) as the last two
  of five includes inside `main.asm:212-217`'s `save / cpu z80 / phase 08000h` block, AFTER
  `sound_tables_z80.asm` (generated, EXCLUDED per brief §1) + the game pitchtable + the SFX
  win-tab. Their labels equal `$8000`-window addresses; their ROM LMA is
  `MovingTrucks_Bank_Start + head_offset`. So they are **phased** (`vma:$8000+off`), not
  `bank:$8000` payload sections like `dac_samples.emp`.

---

## 2. Lane A (z80_init) — two walls

### Wall A1 — compound / expression symbolic imm16 does not lower
z80_init's three symbolic-imm16 lines:
```
ld  bc, (Z80_RAM_END-Z80_RAM)-Z80_IdleProgram_CodeEnd-1   ; const − linklabel − 1
ld  de, Z80_IdleProgram_CodeEnd+1                          ; linklabel + 1
ld  hl, Z80_IdleProgram_CodeEnd                            ; bare linklabel  ← the ONLY form T1 wired
```
T1's symbolic path (`lower/code.rs:1715` gate, `lower_z80_abs16_sym:1751`) matches ONLY a
**bare** `CodeOperand::Sym(name)` and builds a `Value16Le` fixup with target `Expr::Sym(name)`.
Any operand that is an EXPRESSION (`Label+1`, `const-Label-1`) is evaluated comptime by the
operand mapper and **errors `unknown name`** before the symbolic path is reached — proven for
`ld de, Target+1` (same-section) AND `ld de, End+1` (End in a separate `(cpu:z80)` section, the
exact pattern that works for a bare `ld hl, End`). So 2 of z80_init's 3 imm16 lines cannot lower.

- **The 68k defer machinery EXISTS**: `move.l #(Target+1), d0` lowers correctly to
  `20 3C 00 00 12 35` (deferred link fixup with an addend). The Z80 immediate operand path
  simply does not route through it — it folds comptime and errors.
- **Brief/T1-note discrepancy #1 (tree wins):** T1 note §1.1 lists `ld de/hl, Label±k` as a
  demanded, handled form, and §4 says the symbolic imm16 is "the one genuinely new fixup wiring
  in T1 ... corpus-demanded by three z80_init lines." The IMPLEMENTATION handles only the bare
  form; the `±k` forms two of those three lines use do not lower. T1 under-delivered vs its own
  note.
- **Fix shape (bounded, and arguably in-envelope since it is fixing T1 to match its note):** the
  Z80 immediate-operand handling must, when the operand is an unresolved-label EXPRESSION, defer
  it to a `Value16Le` fixup carrying the Expr (not fold-and-error) — the analog of the working
  68k `#(Label+1)` path. `lower_z80_abs16_sym` then builds the fixup from the Expr rather than a
  bare `Expr::Sym`. Medium effort; TDD ladder = the z80_init lines as pins.

### Wall A2 — no end-of-code label mechanism
`Z80_IdleProgram_CodeEnd` is a label at the END of the idle code (referenced 3×). A bare
mid-section / end-of-proc label (`CodeEnd:` after a proc inside a section) is a **parse error**
("expected a declaration, found Ident"). The T1 test only ever put its target label as a `data`
decl in a SEPARATE section with a hardcoded `vma:` — which cannot express "the address right
after this code" (the value depends on the encoded length). So z80_init's internal end label
needs a label-expressing mechanism the grammar lacks in this position (`here()`-bind, an
end-of-section label form, or a zero-size data marker — none verified to work).

Lane A therefore needs BOTH A1 and A2. Its oracle, once unblocked, is the game_debug
windowed template applied to a `(cpu:z80, phase 0)` section (canonically empty → an
off-canonical no-sound-shape oracle; the `Z80_IDLE_SIZE` ownership-flip is NOT needed for the
windowed oracle — it is a full-no-sound-build concern, deferrable exactly as the brief §2
DEMAND-3 machinery row anticipates).

---

## 3. Lane B (seq_opcode_tab) — one wall (a documented T1 deferral)

The 32 cells are `dw <resident-Z80-label>` (`Seq_Op_Vol` … `Seq_Op_End`, `Seq_BadOpcode`) —
RESIDENT phase-0 Z80 code addresses, defined AS-side in `sound_sequencer.asm` (one of the five
resident code files the brief forbids touching), read through the `$8000` window as raw
little-endian data.

- `dc.w Seq_Op_Vol` in a `(cpu:z80)` section → `Cell::SymRef` (`eval/asm.rs:856-857`) →
  `fixup_kind(Cpu::Z80, 2, windowed=false)` → **`[cross-cpu.unwindowed-pointer]`** (the guard at
  `lower/data.rs:169-177`), because the guard assumes any symbolic Z80-section pointer is a
  68k address needing `winptr()`. These are NOT 68k addresses and must NOT be windowed.
- `data [u16;N] = [Seq_Op_Vol, …]` (the array form) → **`unknown name`** (the element evaluator
  folds comptime; the corpus `data` arrays reject bareword link cells — the t26-panel finding).
- The kind these cells need — a resident-Z80-address VALUE cell, little-endian, no masking — is
  **`Value16Le`, which IS the Z80 `Cell::Expr` default** (`lower/data.rs:216`). But nothing in the
  `.emp` surface routes a bareword resident label to a `Cell::Expr`; `dc` produces `SymRef`, the
  array evaluator folds. The code author **explicitly deferred this** (`lower/data.rs:179-185`):
  *"Supporting a genuine Z80-local 16-bit label ref (an `Abs16Le` that does not yet exist) will
  require T6 to SPLIT the `(Z80, _, false)` arm by width, not to route here."*
- **Brief/T1-note discrepancy #2 (tree wins):** T1 note §1.2 and brief §1 lane B state
  seq_opcode_tab "demands NOTHING new … rides the existing `.emp` data path / `Cell::Expr{le:true}`
  / `Value16Le`." The tree shows the resident-label→`Value16Le` routing is unimplemented and
  self-documented as deferred to "T6." (`dac_sample_tab`, comptime consts only, DOES ride the
  path — see §4 — so the "nothing new" claim is true for C, false for B.)
- **Fix shape:** a spelling + lowering that emits a bareword resident-Z80 label as a `Value16Le`
  value cell (the `Abs16Le`/`(Z80,_,false)`-split the comment names). Bounded but a genuine
  design call — it decides how a resident-Z80-address is distinguished from a 68k-address-needing-
  `winptr` at a `dc.w`/data cell. Reversing the author's explicit "defer to T6" is an
  overseer/Fable-owned scope decision, not a porter improvisation.

---

## 4. Lane C (dac_sample_tab) — data CLEAN; only the seam remains

- The body is `db`/`dw` of build-time `SND_*` constants (bank/rate/codec/ptr/len/loop per
  descriptor). Probed: `data [u8;3]=[SND_BLIP_BANK,0,0]` + `data u16 = SND_BLIP_PTR` … lowers to
  the exact little-endian bytes (`0a 00 00 | 00 81 | 00 02 | 00 00`). **P1 (Z80 LE data) holds
  for comptime-constant cells.**
- The `if (End-Start) <> DAC_SAMPLE_COUNT*DacSample_len / fatal` size guard has a clean comptime
  `.emp` equivalent: `ensure(<total> == DAC_SAMPLE_COUNT * DacSample_len, "…")` — verified to
  lower with BOTH `const` operands AND `extern("DAC_SAMPLE_COUNT")` operands. **P5 (comptime size
  guard) is expressible**; its positive control is doctoring the extern so the `ensure` fires.
  (`sizeof(T)` on a data LABEL does NOT work — "unknown type"; use the declared array length /
  an end-minus-start, not `sizeof`.)
- **Remaining blocker = the banked-head SEAM (shared with B).** dac_sample_tab is CANONICAL, so a
  real port must place the `.emp` section at the phased LMA (`vma:$8000+head_offset`, physical LMA
  after the AS-side head content) and gate the include, so `build.sh` emits the canonical ROM from
  the `.emp`. This is the step-0 seam question the brief flagged. Complications:
  1. The `.emp` section is a phased `vma:$8000` table wedged BETWEEN AS-side content
     (`sound_tables_z80` + pitchtable + sfxtab BEFORE; `dephase/restore` + MT song AFTER) inside
     ONE `phase 08000h` block. The `bank:$8000` / VMA==LMA precedent (`dac_samples.emp`) does NOT
     apply — those are payload banks, not phased head tables.
  2. The named options (brief §2): include-site `ifndef` gate + **org-advance twin arm** (the t23
     boot_data cursor-contract class, kill-row precedent 46) — the AS side `org`s forward past the
     `.emp`-placed span so `dephase/restore`/MT-song land unchanged — VS a chained-section split.
     Either needs a region pin (P4) and exact LMA agreement.
  3. Lane C is CO-LOCATED with lane B in the same phase block (B immediately precedes C). Porting C
     alone while B stays AS couples C's placement to B's still-AS span — an awkward isolated seam.

A windowed oracle (game_debug template: compile `.emp` at `vma:$8000`, assemble the `.asm` twin
via the AS front-end with `SND_*` equs, byte-compare) would prove P1+P5 at the "scale that fits"
WITHOUT the seam — but it does NOT wire the file into `build.sh` (the AS twin stays authoritative)
and does NOT exercise P4 (pins round-trip). So it is a rung-0 PROBE, not a port.

---

## 5. Recommendation (overseer decision)

The premise that gated this tranche ("T1 done → satellites just port") does not hold: **every lane
needs a sigil capability T1 deferred**, and two of the three deferrals are documented as future
work in T1's note (`ld de,Label±k`) and in `lower/data.rs:184` ("T6 Abs16Le"). Building three
language features + the banked-head phased-placement seam inside one byte-neutral LEAN tranche is
the balloon the rules forbid; and reversing "defer to T6" / T1's operand scope is a Fable-owned
design call, not a porter improvisation. So I STOP at step 0.

Options for the re-scope, cheapest-correct first:

- **(R1) Sigil-only capability step FIRST, then the satellites.** Land the three deferred
  capabilities as a TDD'd, corpus-scoped sigil sub-tranche — (a) Z80 expression-imm16 defer
  (route `ld rr,<expr>` / `jp <expr>` through the existing defer, Expr-targeted `Value16Le`);
  (b) an end-of-code label mechanism for a Z80 code section; (c) the resident-Z80-address
  `Value16Le` value cell (the `(Z80,_,false)`-split). Then z80_init / seq_opcode_tab port
  trivially against them, and dac_sample_tab needs only the seam. Cleanest; matches "demanded
  feature = its own design + build."
- **(R2) Narrow t27 to lane C + the banked-head seam.** Land dac_sample_tab (data + `ensure`
  guard) with the org-advance twin-arm seam + region pin (proves P1, P5, P2-placement, P4), and
  DEFER A and B to R1. Caveat: C is co-located with B in the same phase block, so C's seam must
  account for B staying AS (an org that skips only C's span, leaving B's AS include in place — the
  order is B then C, so C is the tail of the head; tractable but must be designed).
- **(R3) Rung-0 probes only (no ports).** Land windowed oracles proving P1 (Z80 LE data via
  dac_sample_tab's real cells) + P5 (the `ensure` guard, doctored-extern positive control), close
  gap-ledger row ~825-831, and defer ALL three ports to R1. Smallest; no seam, no `build.sh` wiring.

My read: **R1** is the honest sequence (the campaign's own "demanded feature gets its own design +
build" law), with **R3** as the minimal-now fallback if the overseer wants a landed byte this
tranche. R2 is viable but carries the B/C co-location coupling.

---

## 6. Duties / ledger owed at this checkpoint

- **Gap-ledger rows to add** (jot, per the campaign practice):
  - Z80 expression-imm16 defer (T1 §1.1/§4 over-claim; only bare `ld hl,Label` wired). DEMAND: 2
    z80_init lines.
  - end-of-code label in a Z80 `.emp` code section (grammar gap; z80_init `CodeEnd`). DEMAND: 1.
  - resident-Z80-address `Value16Le` value cell — the `lower/data.rs:184` "T6 Abs16Le" deferral,
    now demand-attached (seq_opcode_tab's 32 cells). DEMAND: 1 file / 32 cells.
  - `sizeof()` on a data LABEL (not a type) — "unknown type"; the table-span guard needs
    end-minus-start / declared length. Minor spelling gap.
  - the generated-file row (brief §1): `sound_tables_z80.asm` is `tools/gen_sound_tables.py` output
    — the real conversion is the generator emitting `.emp`; do not port the output. (Carried, not
    new to t27.)
- **Kill-list rows born:** NONE — no mirror/scaffold was created (no code committed).
- **Twin-scaffolding:** the banked-head org-advance twin arm (R2) would be a kill-list row (t23
  cursor-contract class, precedent 46) IF R2 is chosen — not created here.

## 7. Probe results (P1-P5) at step 0

- **P1 (Z80 LE `dw`/`db`)** — PARTIAL PASS: comptime-constant cells emit correct little-endian
  (lane C). Symbolic resident-label cells (lane B) BLOCKED (§3). Full ROM-scale placement not
  exercised (seam deferred).
- **P2 (banked section + link values)** — NOT REACHED: blocked on B's value-cell wall + the seam.
- **P3 (symbolic imm16 in code at ROM scale)** — BLOCKED: the bare form works (T1), but z80_init's
  ACTUAL lines are compound (§2, wall A1) and do not lower.
- **P4 (pins round-trip)** — NOT REACHED (no section placed).
- **P5 (`if/fatal` size guard → comptime)** — PASS (expressible): `ensure(total == COUNT*len, "…")`
  lowers with consts and externs; the doctored-extern positive control is available. (Not yet
  wired to the real file pending the seam.)

Positive-control status: no negative probe was committed, so the t24 positive-control obligation
is not yet incurred; it attaches to whichever features R1/R2/R3 build.

---

## 8. PHASE-1 ADDENDUM (overseer ruling 2026-07-29 — R1 endorsed, in-tranche)

The overseer countersigned the STOP (both walls re-derived) and ruled **R1 in-tranche**: this
is the standard demanded-feature TDD class (T1's own law is "wire and test only the forms a
landed file demands"; the landed satellites now demand these). Two-phase on the same branch
pair. Phase 1 = three sigil-only capabilities, each failing-test-first, byte-neutral, committed
separately with a paired-strict run.

### (a) Z80 expression symbolic imm16 defer — LANDED (sigil `1c7336a`)
A symbol-bearing operand expression (`Label+1`, `CONST-Label-1`) routes through the SAME
`in_imm_link_ctx` deferral the 68k `#(Label+1)` immediate uses (`map_operand`'s `Imm` arm) →
`CodeOperand::ImmLink { target: Expr }` → the Z80 `Value16Le` fixup carries the residual Expr.
Pure-comptime exprs keep folding (no behavior change); a symbol in imm8 position stays
`[lower.z80-unsupported]`. 7 TDD tests; strict-paired **2661/0** (2654 + 7). 68k frozen.

### (b) End-of-code label — OPTIONS + CHOICE (NO sigil change needed)
z80_init needs `Z80_IdleProgram_CodeEnd` (the address after the last instruction, used by the
three imm16 lines). **How 68k .emp handles the same shape today (probed):** a trailing LOCAL
label inside a proc works — `proc P() { lea .code_end(pc), a0; rts; .code_end: }` lowers and the
pc-relative disp resolves to the code end (`41 fa 00 04 4e 75`). So the shape is ALREADY
supported, not new.

Two candidate spellings (probed, then the probe deleted):
- **C1 — trailing LOCAL label inside the proc** (`proc Z80_IdleProgram() { … ; jp (hl);
  .code_end: }`, referenced `ld hl, .code_end`). **WORKS today** — Z80 bytes `21 04 00 e9`,
  `.code_end` = 4 (correct end address). Byte-identical to the AS twin (a local label resolves to
  the same address as the AS global; `Z80_IdleProgram_CodeEnd` has NO external consumer — the
  three refs are all internal, and `Z80_IDLE_SIZE = End - IdleProgram` is a SEPARATE AS-side
  equate the code bytes do not contain). Least ceremony; existing precedent (the 68k c4 shape).
- **C2 — trailing GLOBAL label inside the proc** (`… jp (hl); Z80_IdleProgram_CodeEnd: }`).
  **PARSE ERROR** ("expected an expression, found Colon") — a bare global label is not a proc-body
  statement. Would need parser surgery (a new global-label-in-body form) — the balloon the ruling
  says to STOP on.

**CHOSEN: C1** (trailing local label). Rationale: it works today, is byte-identical, has an
existing 68k precedent, and needs zero language change — the least-ceremony option consistent with
precedent. No TDD/implementation for (b) beyond using the spelling in the lane-A port (phase 2);
the port's own byte gate proves it. (If a future consumer needs `Z80_IdleProgram_CodeEnd` as a
GLOBAL/exported symbol, that is a separate global-label-in-body ask — ledgered, not built here.)

### (c) Resident-Z80-address 16-bit value cell — TO BUILD
Implement the split the `lower/data.rs:179-185` comment anticipates: split the `(Z80, _, false)`
`fixup_kind` arm by width; a width-2 Z80-local reference → **reuse `Value16Le`** (the code's own
comment: "an `Abs16Le` … no new kind warranted"; same naming call as T1 §9). The
`[cross-cpu.unwindowed-pointer]` guard MUST survive for genuine 68k symbols (width 4, and the
convsym z-filter class) — negative control (a 68k-address symbol still errors) + positive control
per the t24 rule. Corpus-scoped to seq_opcode_tab's `dw <resident-label>` cells. (Design detail
settled at build time: the surface that routes a bareword resident label to the value path — see
phase-1(c) commit.)

---

## 9. PHASE-2 HANDOFF ADDENDUM (context valve invoked 2026-07-29)

Lane A is DONE and committed both repos. Phase 1 (a)(c) landed; (b) resolved as no-change.
Context budget spent on the deep phase-1 capability work + lane A's first-Z80-code-port
integration (incl. the corpus-contract Z80-skip); invoking the overseer's context valve for
lanes B/C rather than rush the banked-head seam under pressure. State + next actions:

### Landed (committed)
- sigil `1c7336a` phase-1(a) Z80 expression imm16 defer (7 tests).
- sigil `c960210` phase-1(b) end-label options + choice (no code).
- sigil `64c2e12` phase-1(c) resident-Z80-address Value16Le cell (4 tests).
- aeon `ee9b51a` + sigil `5c59803` lane A: z80_init.emp + boot_data.asm gate +
  z80_init_port oracle (3 tests) + corpus Z80-skip + kill row 55 + ledger. Strict 2668/0,
  dual build c51342d0/992d9e7d unchanged.

### Remaining: lanes B (seq_opcode_tab) + C (dac_sample_tab) — the banked-head seam
The .emp AUTHORING is now unblocked (phase-1(c) gives lane B its value cell; lane C is
comptime data + an `ensure` guard). What is NOT solved is the WHOLE-ROM CANONICAL placement
seam (both are canonical bytes inside main.asm:212-217's `phase 08000h` head, the LAST two of
five `soundBankHead` includes, AFTER sound_tables_z80 + pitchtable + sfxtab).

**Two proof scales (pick per the brief's "scale that fits"):**
1. **WINDOWED oracle (rung-0 probe, NO seam)** — compile each `.emp` at `vma:$8000` and
   byte-compare against the `.asm` twin assembled via the AS front-end, with the cross-seam
   symbols as equ carriers (lane B: the ~30 `Seq_Op_*`/`Seq_BadOpcode` RESIDENT phase-0
   addresses; lane C: the `SND_*` build-time consts + `DAC_SAMPLE_COUNT`/`DacSample_len`).
   Proves P1 (LE data), P2 (banked link values), P5 (the `ensure` size guard, doctored-extern
   positive control) on the REAL files. This is the z80_init_port template. LANDABLE NOW,
   ~1-2h; does NOT wire into build.sh (the `.asm` stays canonical).
2. **WHOLE-ROM mixed build (real port, THE seam)** — gate the two `soundBankHead` includes
   `ifndef SIGIL_EMP_SEQ_OPCODE_TAB` / `..._DAC_SAMPLE_TAB`, place the `.emp` phased sections
   at their LMA (MovingTrucks_Bank_Start + head_offset, vma:$8000+off), region-pin them (P4).
   The named seam options (brief §2): (i) **include-site ifndef gate + org-advance twin arm**
   — the AS side `org`s forward past the `.emp` span so `dephase/restore`/MT-song land
   unchanged (t23 boot_data cursor-contract class, kill-row precedent 46); (ii) chained-section
   split. COMPLICATIONS: the tables are phased `$8000`-window (NOT `bank:$8000` payload —
   dac_samples.emp does NOT apply); B immediately precedes C in the SAME phase block, so their
   placement couples (an org must skip the whole B+C span or each in turn). This is the
   STOP-if-no-clean-seam item — it needs its own step-0 seam design + a dual-build byte check.

**RECOMMENDATION for the next porter:** land B+C via scale (1) windowed oracles FIRST (proves
every capability on the real files, closes the rung-0 probe value the overseer folded into
phase 2), THEN design + attempt scale (2) as a discrete step with the dual-build byte check;
STOP scale (2) if the phased-mid-head placement + B/C coupling has no clean org-advance.

### Also owed at step 3 (retrospect, not yet run — checkpoint (a) is steps 0-2)
- The leftmost-local-label parser bug (gap-ledgered) — a real correctness catch; needs a
  proper parser fix + positive control (`.x+1` == `1+.x`), deferred as parser surgery.
- The two phase-1 capabilities are demanded-feature landings (step-1 class), not step-3 finds.
