# Pre-t18 Roadmap

**Milestone target:** t18 = the **parallax port** (next lag lever, ~18-22%/frame; parallax is still unported `.asm`).

> **✅ t18 MERGED 2026-07-23** — parallax.emp fresh port (11 procs, full 0→5 loop) + HBlank RAM-jmp trampoline (row 1088) + Hscroll_Dirty PAD + demanded `[lower.abs-sym-operand]` feature. Masters **aeon `6261c29` / sigil `8ab53f8`** (`port-tranche18` --no-ff). **NEW CANONICAL plain `00f609a5`/421089 · debug `80d14183`/429134** (byte-changing re-baseline). Full paired strict on merged masters **2488/0**. Oracle trampoline live-verify 5/5; H2 flat-fill unroll −1924 cyc/f live A/B. **DRY-PANEL DEBUT** (campaign first): caught B6 (gate-blind CC-clobber → post-merge parcel), refuted 1 false positive, 3 folds, ledger rows. Close packet `2026-07-23-t18-close-packet.md`. **Post-merge queue:** item-13 impl brief (7b9afa6) + the boundary-crossing transition parcel (window-slide close + B6/B3/B1/B2, B2-design-first).

> **✅ TRANSITION PARCEL MERGED 2026-07-24 (FIRST in the queue)** — five section-boundary parallax transition fixes: **B6** (promote-frame CC-clobber rebuild-skip, length-neutral reorder), **B2** (active-config mode contract — single `Parallax_Active_Config` accessor, routed HScroll DMA-length + Vscroll), **B3** (frames-remaining `divs.w` ramp, exact convergence, no pop), **B1** (re-cross cancel branch). All latent (no shipped transition fires; configs share mode) — mechanism-hardening, each proven with crossing-drive rig A/B. **Demanded + built `divs.w`/`divu.w` in the sigil ISA** (own commit before consumer). **Window-slide rider CLOSED** (real slide observed, single-axis assert held; per-section VALUE audit ledgered deferred). Kill-list **row 35 live** (OJZ harness mode-register force-write; kill=B2-(ii) ships). Masters **aeon `aa354fa` / sigil `4028bc8`** (`parallax-transition-parcel` --no-ff). **NEW CANONICAL plain `0bfa5b79`/421161 · debug `9d962703`/429204** (byte-changing; `EndOfRom` stable). Full paired strict on merged masters **2490/0**. Close packet `2026-07-23-transition-parcel-close-packet.md`. **Queue now:** item-13 (in flight, re-verifies its byte-neutral bar on this moved canonical).

> **✅ item-13 WAVE-1 MERGED 2026-07-24 (SECOND in the queue) — BYTE-NEUTRAL confirm variant.** The three frozen domain-type families (construct-walk #4 `7b9afa6`): **SongId/SfxId** (sound-API value slots + `SONG_*`/`SFXID_*` const defs + as-blessed construction sites), **AnimId/AnimFrame/MappingFrame** (`FrameId` renamed→`MappingFrame`; `anim_frame` u8→`AnimFrame`), **VramTile/VramAddr** (`vram_art` takes `VramTile`, $1FFF bound preserved; tile-index consts born `VramTile`). Masters **aeon `9fb6fcb` / sigil `4f4f0e2`** (`item13-wave1` --no-ff). **Canonical plain `0bfa5b79`/421161 · debug `9d962703`/429204 UNCHANGED** (confirm variant, NO re-baseline — dual rebuild from merged master exact). Full paired strict on merged masters **2499/0**; zero new clippy. **ENFORCEMENT-TIER FINDING (packet-worthy):** the G5 slice bites only at register call-slots, so only **F1 has live enforcement** (Sound_PlayRing→Sound_PlaySFX corpus swap pin fires); **F2** (SST-memory-resident) and **F3** (comptime-fn loose params) ship DOCUMENTARY + synthetic swap-pins, with the reopen markers (F2 field-store domain-check · F3 comptime-arg newtype-check) making the types retroactively enforceable. `vram_bytes` ledgered-not-created (stage-0: absent in aeon); Option-A optional-param LOG-AND-SPLIT (size-cap). Close packet `2026-07-24-item13-wave1-close-packet.md`. **Queue now EMPTY** — campaign returns to single-lane lean conversion tranches; sprites-hardening stays PARKED (post-conversion).

> **✅ TRANCHE 19 MERGED 2026-07-24 — camera/bg/bg_anim conversion (first LEAN tranche).** `engine/level/` trio → `.emp` (camera 2 procs · bg 1 · bg_anim 2), full 0→6 loop. **Demanded/derived:** the comptime-select game-contract mirror idiom (`-D GAME_CAMERA_JUMP_LOCK`; =0 arm short-circuits before `extern()` — lock-less games link with NO game symbols, proven by the `jump_lock_off_compiles_without_game_symbols` probe); **NEW `engine/z80_bus.emp`** (2nd-consumer lift, sound_api retrofitted); **VDP_DATA/VDP_CTRL hoisted into `engine.vdp`** (3rd consumer; t18 B1 debt executed — parallax/plane_buffer/section retrofitted); `bganim_band` struct (all walk magics derived, sizeof==44 ensure, emitter LOCKSTEP kill row); `clamp_camera_axis` template; 2 DEBUG asserts (band count + piece-1 length — plain zero bytes; the twin carries the campaign's first `ifdef __DEBUG__` shape-dependent branch widths). **Step-5 measured, no cut** (Camera_Update 670/716 cyc/f · BgAnim_Update 124 exit-only; live P3 bonus: both chases parked at the EXACT documented deadzone boundaries). Dry-panel round: DRY STOOD (marquee: partial-retry comment overpromise → truthful comments + ledgered fix sketch; piece-1 assert; GRID_*>=1 ensure). Masters **aeon `3938250` / sigil `f2c4361`** (`port-tranche19` --no-ff). **NEW CANONICAL plain `eab19b3f`/421159 · debug `f1c1aa12`/429204** (`EndOfRom` stable both shapes). Full paired strict on merged masters **2509/0**. Close packet `2026-07-24-t19-close-packet.md`. Remaining engine 68k conversion backlog: ~11 files (bg cluster now DONE; next big rocks: vblank/dma_queue/load_art/boot/buffers).

> **✅ TRANCHE 20 MERGED 2026-07-24 — dma_queue/load_art conversion.** `dma_queue.asm` + `load_art.asm` → `.emp` (9 procs), full 0→6 loop. **THE OWNERSHIP FLIP EXECUTED:** QueueDMA_* re-resolved under dplc.emp + bg_anim.emp via FOUR two-module link tests; all three extern decls deleted same-commit; **kill rows 31+32 DEAD**. **Two demanded features (TDD-first):** `CodeOperand::DispSymInd` (label-as-`d16(An)`, the `jmp .jump_table(a1)` class) + movep-aware field-access widths (movep = alternate-byte 1-byte-per-field access). Also: `resolve_callee_key` closure for `Owner.label` tail contracts; width-4 link value cells → signed∪unsigned union (asl parity; mixed-arm catch, ledgered); NEW independent tranche20 mixed-build arm; `DMAEntry` struct twin (11-field offsetof wall + stride ensure pinning table geometry to layout); load_art contract UPGRADE `preserves(d6/a4-a6)` movem-PROVEN (twin header over-claim fixed). Step-5 no-cut (largest ≈340 cyc/f; Process_DMA_Critical header numbers corrected +12%); panel DRY STOOD (C3 marquee: VBlank budget model uncoupled from physical window — ledgered with numbers as post-conversion budget-parcel input). Masters **aeon `236f959` / sigil `ff0fd30`** (`port-tranche20` --no-ff). **NEW CANONICAL plain `f3e333d3`/421159 · debug `20a1fe4b`/429190** (`EndOfRom` stable). Full paired strict **2531/0**. Close packet `2026-07-24-t20-close-packet.md`. Remaining engine 68k backlog ~9 files (next rocks: vblank/boot/buffers/children/s4lz+zx0/debug×4).

> **✅ TRANCHE 21 MERGED 2026-07-24 — buffers/vblank conversion.** `buffers.asm` + `vblank.asm` → `.emp` (8 procs; buffers FIRST so vblank calls Enqueue_Dirty_Buffers module-to-module with zero extern churn), full 0→6 loop. **THE VSync_Wait OWNERSHIP FLIP EXECUTED:** re-resolved under game_loop.emp + load_art.emp via FOUR two-module link tests; both extern decls deleted same-commit; **kill row 29 DEAD**; contract carried verbatim + lint-mandated `preserves(sr)`. **ZERO demanded features** — all seven step-0 probes passed as-found, including BOTH new mixed-build binding classes (`rte`-terminated .emp procs; `.asm` `dc.l`/`move.l #` references to .emp symbols — the real IRQ6 vector, proven at full-ROM scale by the NEW tranche21 mixed arm). Constructs: **`engine/irq.emp` `sr_masked(code)`** (paired-use-only fence, named exclusions; VSync_Wait + Sound_PostByte + Sound_PlayMusic adopted) + **`out(zero:)` flag-result contracts** (Parallax_Active_Config `out(d0, zero: inert)`; step-6 retrofits Section_GetSecPtrXY + Load_Object incl. the corpus's first `@discards(success)`); `queue_static_dma` entry-only redesign (donor macro's slot/end mismatch class now unrepresentable); DMAEntry at-next-touch ride retired; ledger row 1052 CLOSED (dma_source/dma_length/vdp_comm_delta/plane_loc → engine.vdp); off-canonical **twin-parity gate class** named (AS-side ROM as oracle for comptime arms with no reference ROM). Step-5 no-cut (largest ≈50-100 cyc/f); panel DRY STOOD (C3 marquee: the HScroll "dirty-FLAG gated" safety claim was FALSE — real mechanism is static-dest value-tear-only, self-healing; comments rewritten both twins; C1 settled the mask bracket at exactly 46 cy; C2 zero real bugs + the 112-byte-vs-32-byte-stride latent hazard ledgered). Masters **aeon `0a17462` / sigil `258308a`** (`port-tranche21` --no-ff). **NEW CANONICAL plain `4745cbc3`/421157 · debug `0b7c4804`/429202** (`EndOfRom` stable both shapes). Full paired strict **2553/0**. Close packet `2026-07-24-t21-close-packet.md`. Remaining engine 68k backlog ~7 files (next rocks: boot/children/s4lz+zx0/debug×4).

> **✅ TRANCHE 22 MERGED 2026-07-24 — compression cluster conversion.** `s4lz_decompress.asm` + `zx0_decompress.asm` + `compression_selftest.asm` → `.emp` (4 procs), full 0→6 loop; **byte-identical FIRST COMPILE all three files**. **THREE OWNERSHIP FLIPS EXECUTED (kill rows 30/38/39 DEAD):** S4LZ_Decompress + ZX0_Decompress under load_art.emp, S4LZ_DecompressDict under tile_cache.emp — **load_art.emp now carries ZERO extern decls**. **Two demanded features/fixes (TDD):** repin `debug_only`+`plain_anchor` region class (compression_selftest = the campaign's FIRST debug-only region; plain emptiness proven at full-ROM scale by the mixed plain arm) + **assert/raise_error diag-label module scoping — the t22 mixed arm caught a REAL latent frontend bug** (cross-module `$diagN$` collision; first assert-bearing .emp module pair ever placed in one link). Also: `falls_into` pub-pair spelling (DecompressDict falls into Decompress — passed as-found); **zx0 `preserves(d2/a2)` = spelling-selects-provability** (unprovable at the faithful `bsr.s`, proven at the step-2 `jbsr` — the modernization made the contract enforceable; S2-D6 CFG-unification ask ledgered); bare link-imm arithmetic twin-verbatim; CSELF values parsed from live generated data + doctored-sum probe. Panel DRY STOOD, 5 lenses (A1 marquee: `*DictBase` phantom type — latent since t16, `struct DictBase { }` marker shipped + undeclared-typename enforcement ask ledgered, corpus sweep proved it the ONLY phantom; A1+C2 convergent: the CSelf $8000 growth trap → `ensure` fit-lock + firing probe; C2 zero real bugs + dict-entry bound header correction both twins (dest+dict ≤ 32766); C1 endorse, +16cy/match EXACT). Step-2 checklist gained the ratified shape-divergent-relaxation line (twin ifdef widths; 3rd instance). Masters **aeon `7c97070` / sigil `54e52ed`** (`port-tranche22` --no-ff). **NEW CANONICAL plain `06290799`/421157 · debug `e280a49b`/429202** (`EndOfRom` stable). Full paired strict **2573/0**. Close packet `2026-07-24-t22-close-packet.md`. Remaining engine 68k backlog: **boot(+vectors seam ruling) · children · debug trio (debugger/error_handler/sound_debug)** — t23 = boot (BootData Z80-source seam ruling first; all boot callees now .emp).

> **✅ TRANCHE 23 MERGED 2026-07-24 — boot conversion + the BootData seam.** `boot.asm` → SPLIT (code + `boot_data.asm` data tail; the ruled .asm-data-tail seam — the inline Z80 sound-driver source stays AS-side in BOTH arms) → `boot.emp` (single pub proc `EntryPoint`, ⊤+sr contract; all 9 callees were already .emp — **engine/system code files now ALL .emp**). **FOUR demanded features (TDD): `ImmSigned8`** (moveq link-imm8, DISTINCT signed [-128,127] window — the imm-deferral family is now WIDTH-COMPLETE, kill row 10's condition TRIPPED) + `Value8` `.b` deferral + movem pinned-abs seam + the AS-side cursor-contract assert wall. **The ruling-2 wave** (clr.w, −2 both shapes at the FIRST region) slid every engine base — 77 pins, 30 resume orgs, REGION_A_LMA; the row-1257 sweep's first live run MIGRATED 2 stale fixture classes to pins. Panel DRY STOOD, 5 lenses (B1 completed the ruling-3 VDP_Shadow-offset hoist with parallax's stranded MODE3; C1 zero corrections — 264cy EXACT, clr.w 16=16; C2 zero real bugs — cursor walk re-derived byte-exact; C3 all-clear — PSG-in-fill-window safety re-grounded TEMPORAL, comments sharpened both twins). Oracle-fed parity class grew its first SHAPE-DIVERGENT variant (sound-off boot −2). ZERO ownership flips; kill rows 45-48 born. Masters **aeon `1470af2` / sigil `d129d54`** (`port-tranche23` --no-ff). **NEW CANONICAL plain `01832b1a`/421157 · debug `154076f8`/429232** (`EndOfRom` stable; debug +30 = convsym appendix only). Full paired strict **2588/0**. Close packet `2026-07-24-t23-close-packet.md`. Remaining engine 68k backlog: **children · debug trio (debugger/error_handler/sound_debug)**.

> **✅ TRANCHE 24 MERGED 2026-07-24 — children conversion; the engine 68k backlog is now the DEBUG TRIO ALONE.** `engine/objects/children.asm` → `children.emp` (8 entry points), full 0→6 loop, **byte-identical BOTH shapes at the first linking compile**; ZERO ownership flips (pure-callee — AllocDynamic/AllocEffect/DeleteObject already `.emp`). NEW region `children` = `PopulateSpawnedPieceCount`..`Load_Object` — the hole between two pinned regions, so it SHARES both anchors with its neighbours (proved in both shapes). **THREE BEHAVIOUR FIXES, all live-verified on oracle before merge (byte gates are structurally blind to them):** effects' `parent_ptr` dead store deleted (canonical `$8CBE` → `0000` at the same slot; kills the latent 'effect from an RF_MULTISPRITE parent is never rendered' bug and ~30 cyc/effect/frame in `Draw_Sprite`), children inherit the parent's COORDMODE at spawn (child `render_flags` `$00` → `$08` measured the instant the creator returns — closes a one-frame world-space pop), and the chain-contract DEBUG asserts. **Priority-band inheritance was implemented, caught by the focused re-panel as a REGRESSION, and reverted** (a band is a 3-bit VALUE — or-ing unions it: emitter band 5 + particle band 6 = band 7); deferred to the new **POST-TWIN-RETIREMENT** ledger bucket with the ten-site `ori` precondition, the band-0-cannot-cascade fact and three candidate shapes. **Step 5 took the §19 review wave** (M1-M5: save-set reduction against the exhaustive-licence convention, cascade movem hoist, five dead fail-paths deleted, chain-head write hoisted) + the branchless flip. **Two constructs** — `pixels_to_coord` (new zero-emitting `engine/coords.emp`, proved section-less by a probe) and `refresh_piece_count` (adopted by children AND animate) — plus the demanded feature they exposed (struct-field displacement over a SPLICED base register inside a comptime-fn template). **TWO STANDING BARS BORN:** the **$8000 bank-shift bar** (a DEBUG-only engine growth widens the object bank's `jsr (Sym).w` to abs.l and slides the whole debug bank — it broke four fixture families, now pins-derived per shape; first live run PASSED) and the **positive-control rule** (`tranche3_negative_probes.rs` compared a 36-byte section against a 424-byte slice through stale fixtures — both probes were trivially true and COULD NOT FAIL; migrated to pins + two executable positive controls, falsification verified). **TWO PANEL ROUNDS, TWO CATCHES IN OUR OWN WORK:** the full panel broke the dry claim (8 stale self-claims incl. a false leak-bound; 4 takeables; 2 lens claims REFUTED with evidence) and the focused re-panel caught the band regression. An **OVERSEER-ERROR row** is logged (Fable specified `or` semantics for a 3-bit field — extending a ruling by analogy without re-deriving it for the new data shape). Region `$30E` → `$2D0` plain / `$3DA` debug (first SHAPE-SPLIT region: the shapes move in opposite directions); both `EndOfRom` UNCHANGED, the −90 on both ROM files is the convsym appendix only. Canonical **plain `c51342d0`/421041 · debug `992d9e7d`/429102**; strict **2604/0**. Masters aeon `6dc5a55` / sigil `d4995e6`. **Engine 68k backlog after t24: the debug trio (debugger/error_handler/sound_debug) — the t25 cluster** (+Z80 ~5 Volence-deferred, game-side ~10). Also surfaced: an engine BUG out of scope — children of an entity-window-despawned parent are NEVER freed (the despawn walk skips untagged slots and calls `DeleteObject`, not `DeleteChildren`).

> **✅ TRANCHE 25 MERGED 2026-07-28 — the debug trio; the ENGINE 68K CONVERSION BACKLOG IS EMPTY.** `engine/debug/error_handler.asm` → `error_handler.emp` (12 exception-vector stubs + the vendored MD Debugger v2.6 blob), **the campaign's first fully BYTE-NEUTRAL tranche** — canonical CRCs, sha256s, and `EndOfRom` all UNCHANGED from t24 (the brief's bar was "expected byte movement ZERO; nonzero = STOP" and it held at every commit). **`debugger.asm` RECLASSIFIED, not ported** (zero ROM emission — macro tower + config before `org 0`; retires WITH the twins; POST-TWIN-RETIREMENT row). **`sound_debug.asm` DEFERRED to a mini-tranche** (zero bytes in both canonical shapes; the transliteration exposed a real PARSER INFINITE LOOP — minimized ~26-line repro committed `#[ignore]`d, an `asm_body` zero-progress guard shipped, the deep fix + the `extern`-in-lea-displacement parse gap ledgered demand-1; kill row 42 OPEN). **Two new language capabilities (both TDD'd vs the real AS macros, both now house-format spellings in the step-2 checklist):** `raise_exception` (the `__ErrorMessage` counterpart — a hardware-vectored stub must NOT simulate the exception frame, so no `pea *(pc)`/`move.w sr` prefix; options form carries `address_error` for the two bus/address vectors; flag vocab deliberately minimal) and `dc.l <label>` absolute-symbol pointers (vectors.asm = the one genuine future consumer per the step-6 census). NEW region `error_handler` = `BusError`..`EndOfRom`, **0x10B0 BOTH shapes** (same-size, base-relocated; the region tail abuts the convsym appendix → gates ride `assert_rom_matches_convsym`, windowed + whole-ROM mixed both shapes). The MDDBG__ flip shipped via the NUMERIC-ORG derivation (`mddbg_symbols.asm` always-included, gate-ON arm defines per-shape numeric `ErrorHandler` equs; kill row 52 — sigil lacks equ-off-link-external-base, demand-1 with an executable finding test; kill row 53 — `raise_exception` pinned to donor config). **TWO OVERSEER-ERROR rows** (the brief claimed bare `raise_error` matches an exception stub — +6 bytes of frame simulation says otherwise; and assumed the derived-equ flip was buildable — both caught by porter probes, exactly what step-0 probes are for). Panel A1+B1+C2 (C1 inactive cold-path, C3 inactive lane-deferred), dry stood. **Volence ruling banked: the vendored Vladikcomper handler stays; an own `.emp`-native diagnostics runtime is a POST-TWIN-RETIREMENT row** (sigil-emitted symbol table kills the convsym appendix; the diag construct sheds the third-party format mirror; sized to the used surface — Console/KDebug have ZERO corpus sites). Canonical **plain `c51342d0`/421041 · debug `992d9e7d`/429102** (UNCHANGED); strict **2620/0**. Masters **aeon `915d304` / sigil `a05d4bc`** (`port-tranche25` --no-ff). Close packet `2026-07-28-t25-close-packet.md`. **Remaining conversion: Z80 ~5 (Volence-deferred) · game-side ~10 · the sound_debug mini-tranche.**

> **✅ TRANCHE 26 MERGED 2026-07-29 — vectors + game_debug (two lanes); the FIRST GAME-SIDE .emp CODE MODULE.** `engine/system/vectors.asm` → `vectors.emp` (64-entry vector table, org 0, fixed `[0,$100)`; FIRST .emp→.emp vector reference vs error_handler.emp; P-A1 retired with ZERO new capability — dc.l label comma-lists already supported) + `games/sonic4/debug/game_debug.asm` → `game_debug.emp` (canonically EMPTY both shapes — proven by the off-canonical hotkeys-shape twin-parity oracle + emptiness gates; **kill row 33 KILLED**, row 54 born). **SECOND consecutive byte-neutral tranche** (canonical CRCs/sha256/EndOfRom unchanged). **HEADLINE: the extern decl HID a transitive clobber under-declaration** — game_loop.emp trusted the twin's hand comment (d0-d2/a0/a1) but Sound_PlayMusic leaks d3/d4 through; the honest closure is d0-d4/a0-a1, C2-exact; the step-6 census then verified the corpus's ONE remaining extern (Sound_DebugMirror) honest and fixed ITS stale twin comment (missing d1) — **no extern under-declares today**. The type layer DEMANDED SongId/SfxId blesses at step 1 (slot_type_corpus fired, not deferrable). Panel A1/B1 convergence settled BY PROBE (data-array frictions measured → form kept, typed/label data-table construct ask ledgered with evidence = offset-table roadmap #1). Four panel asks ledgered; whole-ROM hotkeys mixed-placement machinery deferred PAIRED with the sound_debug mini-tranche (one build serves both). Strict **2631/0**. Masters **aeon `6e25b6e` / sigil `981cb4d`** (`port-tranche26` --no-ff). Close packet `2026-07-29-t26-close-packet.md`. **Remaining: Z80 ladder (T1 operand model IMPLEMENTED, merge-queued) · game-side ~18 · sound_debug mini-tranche.**

> **✅ TRANCHE 27 MERGED 2026-07-29 — Z80 ladder rung 0+1: the FIRST Z80 .emp MODULES (satellite ports).** `engine/system/z80_init.asm` → `z80_init.emp` (the first Z80 CODE port and first live `module (cpu: z80)`; canonically EMPTY — the no-sound else-arm of boot_data.asm — proven by the off-canonical `z80_init_port` oracle, 38 B byte-match + t24 positive control) + `engine/sound/seq_opcode_tab.asm` → `seq_opcode_tab.emp` (32 × `dc.w <resident-label>` → `Value16Le`, 64 B oracle-identical) + `engine/sound/dac_sample_tab.asm` → `dac_sample_tab.emp` (10 × 9-byte descriptors, comptime `SND_*` cells + `ensure` size guard that FIRES on a doctored count, 90 B oracle-identical). All scale-(1) WINDOWED oracles — the `.asm` twins stay canonical, so the tranche is **byte-neutral BY CONSTRUCTION** (the FOURTH consecutive byte-neutral landing; CRCs/sha256/EndOfRom unchanged). **THREE T1-DEFERRED CAPABILITIES SHIPPED ON DEMAND** after porter-1's step-0 probes caught the T1 note's over-claims (corrections list in the close packet): the Z80 expression symbolic-imm16 defer (`ld rr, Label±k`), the trailing-local end-of-code label (NO sigil change — house spelling), and the resident-Z80 width-2 value cell (the anticipated `(Z80,2,false)` fixup split; width-4 cross-cpu guard survives). **SOUNDNESS CATCH:** a LEFTMOST local-label leaf in an immediate expression escapes mangling (68k too; census ZERO live instances, loud-fail class; commuted `1 + .x` = house spelling until the parser fix). The scale-(2) whole-ROM canonical banked-head placement **STOPped as its own sub-tranche** (4 blockers; lead = the 30 address-derived `SND_*` comptime-source circularity → the generated-file/DSL row's THIRD convergence; the Value16Le census adds `PsgVolEnv_Ptrs`/`FmVolEnv_Ptrs`, both generated). Panel A1+B1+C2+**C3 ACTIVE** (the table headers make bank-contract claims; C3 verified **10/10** against the resident tree), 4/4 lenses useful, all fixes comment-tier. z80_init joins game_debug + sound_debug in the DEMAND-3 off-canonical machinery family. Kill rows 55/56/57 born. Strict **2675/0**. Masters **aeon `20f1136` / sigil `8b28946`** (`port-tranche27` --no-ff, two-porter relay under the direct-dispatch shape, context valve used once). Close packet `2026-07-29-t27-close-packet.md`. **Remaining: Z80 rung 2 (psg→fm, contracts + jr→jp ladder) · the banked-head seam sub-tranche · game-side ~18 · sound_debug mini-tranche.**

> **✅ TRANCHE 28 MERGED 2026-07-29 — parser debt + the DEMAND-3 off-canonical machinery + sound_debug; THE CORPUS'S LAST `extern proc` IS CLOSED.** Lane P: the three parser-debt items fixed TDD, each root cause DIFFERENT from the brief (the t25 hang = top-level `recover_to_next_decl` spinning on contextual `extern`; the "extern-in-lea" gap = the wrapped-paren `(disp)(An)` form for ANY displacement; the leftmost-local-label bug fixed in shared `eval_path`, both parse forms converging — 5 brief errors caught, probes did their job). Lane M: `mixed_offcanonical_rom.rs` — pure-`assemble_root` references + `assert_rom_matches` EMPTY allowlist (held as a hard bar); Config A (debug+hotkeys+mirror) serves game_debug AND sound_debug in ONE build, Config B (plain no-sound) serves z80_init; **all three DEMAND-3 consumers green by name**. Lane S: `engine/debug/sound_debug.emp` (116 L, canonically empty both shapes, proven at the mirror shape + the Config-A whole-ROM gate; a THIRD blocker `{stop_z80()}` body-splice had been masked by the hang since t25). **KILL ROW 42 KILLED — Sound_DebugMirror's extern deleted; step-6 census: ZERO `extern proc` decls corpus-wide (evidence-grounded).** Rows 58/59 born; sec0-collision harness limitation + cryptic body-splice diagnostic ledgered. Panel A1+B1+C2+C3 4/4 useful, dry stood, all fixes byte-neutral (C3 verified the Z80 bus-hold protocol clean). **FIFTH consecutive byte-neutral landing** (CRCs/sha256/EndOfRom unchanged). Strict **2685/0 (1 ignored)**. Masters **aeon `7051b4d` / sigil `7fd38c9`** (`port-tranche28` --no-ff). Close packet `2026-07-29-t28-close-packet.md`. **Remaining: Z80 rungs 2-4 (rung-2 design RATIFIED `011ee32`) · game-side 17 files (census `c37a164`; G1 = trivial objects first) · the banked-head seam sub-tranche.**

> **✅ TRANCHE 29 MERGED 2026-07-29 — game-side G1: test_static.emp + test_animated.emp (the census-era game front OPENS).** Byte-identical at the FIRST linking compile, zero wave (step-2 flips byte-identical at bank distance). **test_animated = the FIRST game-side SST-overlay-twin port** (census correction — `vars DplcV: Sst.sst_custom` w/ offsetof drift-guards vs test_player.asm; the overlay's `[overlay.window-overflow]` subsumes the AS `objvarsCheck`) + typed `vram_bytes(VramTile)->VramAddr` at first consumer + `vram_art` adopted. Regions `$10C66`/`$4` + `$10C6A`/`$5A`, bases shape-invariant / content compile-twice. Proof = windowed gates + whole-ROM mixed arms (drift guards resolve against the REAL AS tree) + t24 doctored controls. **Per-file object gates = STANDING PATTERN** (one test file + one mixed fn per tranche). Panel A1+B1+C2 (C1 inactive w/ named-site justification); the step-3 comment-claim audit caught the porter's own step-1 false claim. Step-6 censuses: vars-overlay = corpus's ONLY (seeds G3); objroutine-inline demand 3; $FF sentinel demand 4/2 files; vram_bytes 1 consumer (row 63 open). Kill rows 60-63. **SIXTH consecutive byte-neutral landing.** Strict **2691/0 (1 ignored)**. Masters **aeon `c6a89a5` / sigil `557f5b0`**. Close packet `2026-07-29-t29-close-packet.md`; census STATUS-AMENDED (G1 ported). **Remaining: game-side 15 files (G2 next per census) · Z80 rung-2 checker cluster (in flight on `z80-rung2-contracts`) · rungs 3-4 · banked-head seam sub-tranche.**

> **✅ TRANCHE 30 + Z80 RUNG-2 MERGED 2026-07-29 — the effect trio + THE Z80 CONTRACT SYSTEM.** G2 (`test_emitter`/`test_stress_emitter`/`test_churn`) byte-identical both shapes, windowed + whole-ROM; **THREE demanded capabilities: the CALLEE-PRESERVES ORACLE** (test_churn = first object Main WRITING a0 → verify_preserved credits verified callee contracts, per-file defers, closure = final authority — the player cluster's prerequisite; consumer baseline = 1), Value16Be word-imm link-deferral (2 live consumers, census-proven), EffectSpawn1 typed-struct link-expr carrier (`dc`-link-expr ask ledgered). Byte gate caught a census ANCHOR ERROR (TestChildPart, 84-vs-228). Descriptor census → the shared 4-byte SpawnDesc hoist ruling input for G3. Then **z80-rung2-contracts merged** (rebased over the oracle, semantic ProcBuf resolution): CPU-parametric reglists · module `invariant(ix)` + inheritance · the `z80_preserves` SIBLING (conditional two-way edges after the §13.4-E soundness fix — found by the item-2 finisher's PROBE against countersigned code) · Z80 proc reg params (T1 §0 obligation discharged) · bit/set/res · additive flag arms + Z80Cc · latent jr→jp ladder. preserves.rs byte-untouched on the branch. **SEVENTH consecutive byte-neutral landing.** Strict **2757/0 (1 ignored)**. Masters **aeon `b381a83` / sigil `1c08ea7`**. Close packet `2026-07-29-t30-close-packet.md`; design `2026-07-29-z80-rung2-contracts.md`. **Remaining: game-side 12 (G3 next) · Z80 item 9 = the psg/fm PORTS (now a pure port tranche) · rungs 3-4 · banked-head seam.**

> **✅ TRANCHE 31 MERGED 2026-07-29 — G3 test_parent + THE FIRST GAME-SIDE BYTE-MOVING WAVE (first canonical change since t24).** test_parent.emp (first game-side struct-overlay twins; zero-drift-guard single-consumer class; objvarsCheck subsumed) + the SpawnDesc hoist (children.emp format-owner; G2's EffectSpawn1 pair replaced, gates prove byte-identity). **The wave: −12 B/shape** — the dead movem around GetSineCosine (**the callee-preserves oracle's FIRST optimization use**) + the dead andi (internal mask C2-confirmed); downstream −0xC pad-absorbed, sizes unchanged; full ripple hand-checked. **NEW CANONICAL plain `85111814`/421041 · debug `eb5e94be`/429102.** Step-6: the dead-save class is SPARSE (sole confirmed member; the t24 6-alloc-site suspicion resolved LIVE — contract-necessary). **A1 CONFIRMED LATENT DEFECT: the self-destruct/DeleteChildren cascade is UNREACHABLE** (life_timer overloaded; faithful both twins) — comments corrected, **behavior fix = NAMED VOLENCE OVERRIDE** w/ full trace. First slot-demanded `as Angle` bless; Overlay.field idiom feed-forwarded; table-§4.9 adoption ledgered. Process: one cross-worktree contamination self-caught + repaired + overseer-verified clean. Strict **2763/0 (1 ignored)**. Masters **aeon `0b283ef` / sigil `2050a7e`**. Close packet `2026-07-29-t31-close-packet.md`. **Remaining: t32 psg (in flight — contract-gaps finisher + rebase behind t31) · P1 player keystone arc · fm/rungs 3-4 · seam sub-tranche · game-side ~11.**

> **✅ TRANCHE 32 MERGED 2026-07-29 — sound_psg: THE FIRST FULLY-CONTRACTED Z80 MODULE (rung-2 acceptance PASSED, 63→0).** `engine/sound/sound_psg.asm` (526 L, 15 procs) → `sound_psg.emp`, scale-(1) windowed oracles BOTH shapes (444 B; window $1660/$16DE — blob base shape-variant, layout invariant). **The acceptance corpus caught: 2 implementation gaps** (clobbers/out unplumbed; the Z80 sibling lacked the t30 callee-oracle) **+ 1 soundness hole w/ self-catch** (vacuous tail-jp; local end-label as external tail) **+ THREE 15-year-old header lies machine-corrected** + 1 demanded operand feature (Z80 bare-const imm8) + 1 parser sharp-edge. Contract set LIVE: `invariant(ix)` all 15 · out(a)/out(hl,carry:) · honest clobbers/preserves · 3 verified-against-header extern trusts. Panel dry, C3 verified ALL hardware prose vs the resident tree. Step-6: the 4-file coverage census (fm 22 routines next = 9b; sequencer 51 + sfx 23 = rung 3; driver 21 = rung 4) + the trust-surface census (3, all verified). Kill row 70 (seam-sub-tranche condition). Strict **2783/0 (1 ignored)**. Masters **aeon `a4008ca` / sigil `30cb42e`**. Close packet `2026-07-29-t32-close-packet.md`. **Remaining: fm 9b (census-seeded) · P1 player keystone arc · rungs 3-4 · seam sub-tranche · game-side ~11.**

> **✅ TRANCHE 33 MERGED 2026-07-29 — sound_fm: THE RUNG-2 CONTRACT-MODEL GRADUATION (firing arc 0→0).** `engine/sound/sound_fm.asm` (998 L, 21 procs) → `sound_fm.emp`, scale-(1) both shapes (925 B; $12C3/$1341; ZERO shape-dependent seam symbols — fm defines Snd_ChanClass internally). The checker verified fm's honest set DIRECTLY — no finisher, no demanded features, pure-port premise HELD (vs t32's 63→0). **Snd_ChanClass trust → VERIFICATION** at psg's exact equ-carrier addresses (hl confirmed non-preserved — psg's bracket vindicated); **depth-4 LIFO passed** the sibling proof; typed params at the design-named site only (+ the typed-register-out grammar gap ledgered — design §2.3's 2nd over-promise). Panel: C2 caught a THIRD over-claim (Fm_WriteFreq `hl`); C3 caught STALE hardware REASONING (parks-$2A-once vs claimed every-pass; code sound, why stale). **Header-accuracy census: 36 procs, 6 machine-corrected over-claims, 0 unsafe under-claims, 83% — rung 3's 74-proc scoreboard.** Kill row 71. Strict **2788/0 (1 ignored)**. Masters **aeon `5a9d974` / sigil `3a4ef09`**. Close packet `2026-07-29-t33-close-packet.md`. **Remaining: t34 P1 in flight · P2-P4 · 3 objects · 2 harness states · sequencer+sfx (rung 3) · driver (rung 4) · seams · the ruled test_parent fix.**

> **✅ TRANCHE 34 MERGED 2026-07-29 — THE P1 PLAYER KEYSTONE + the second game-side wave (NEW CANONICAL `ee4de2ed`/`b5f76eee`).** `player_common.emp` (the ordering root: PlayerV 13-field GUARDED overlay w/ 5 drift guards; the INTERNAL-gate keystone pattern — zero-byte header always-emitted for the surviving P2-P4 consumers; 4 macro→comptime templates, dist_to_fix→pixels_to_coord) + `sonic.emp`. TWO porters (clean valve handoff). Corpus extern count 0→1 (the Player_AtLedgeEdge boundary decl, dies at P4 — honest asterisk). THE WAVE: bsr.w→.s twin shrink + the census-corrected DEAD ext.l cut (C2-confirmed both axes ×5) = object bank −0x12 pad-absorbed; ripple caught the **act_descriptor SELF-GATE-org class** + a **coincidental-byte-match latent test bug**. THE COMBINED-LINK STALE-FOLD BUG found via the mixed gate (dup-locals hypothesis OVERTURNED by the finisher's minimal repro; partial fix landed on `fix-combined-link-locals`, 3 operand classes remain) — **P2-P4 GATED on the fix**; t34's proof = windowed + gate-off identity. Panel: 2 comment-claim defects fixed; offsets-adoption LEDGERED (kill = P4 close). Kill rows 72-77. Strict **2796/0 (3 ignored)**. Masters **aeon `b0fce19` / sigil `14ce198`**. Close packet `2026-07-29-t34-close-packet.md`. **Remaining: the link-fix completion · the ruled test_parent fix · P2-P4 · 3 objects · 2 harness states · sequencer+sfx · driver · seams · generator.**

> **✅ TRANCHE 36 MERGED 2026-07-29 — sound_sequencer (rung-3a): THE TRUST SURFACE REACHES ZERO.** `engine/sound/sound_sequencer.asm` (2091 L, 51 labels / ~46 code procs — the biggest resident Z80 file) → `sound_sequencer.emp`, scale-(1) BOTH shapes (plain $0565–$0CD7 = 1906 B / debug $0565–$0D55 = 2032 B; SAME base, +$7E = 16 internal `__DEBUG__` blocks — the **FIRST Z80 `.emp` with `if DEBUG == 1` bodies**). Byte movement ZERO; firing arc 0→0. **Mod_ReArm/Mod_Advance become CHECKED defs — the t32 §5.2 3-proc trust table CLOSES.** THE DEMANDED FEATURE: the `ex (sp),hl` trampoline (2/3 pre-built discovery; `Z80IndSp` + loud bail + **the overseer verdict TIGHTENING** — credit past a bail = never-written-locally ∧ module-invariant ONLY, RED-test proven against the pre-fix verdict). **The invariant(ix) adjudicated finding**: Sequencer_Frame's structural walker clobber forbids the module invariant → per-proc preserves(ix) on ~19 leaves, HONEST OMISSION on the 3 loop procs (checker-limitation, not header lie — both callers advance the same ix in fact; Option-B `as dispatch()` table-closure = the ledgered upgrade path). out(carry) cross-proc credit LIVE end-to-end (6 consumers; drop-probes non-vacuous). **The extern-decl-vs-def SILENT DRIFT hazard machine-confirmed** (doctored extern fires NOTHING; ledger row, kill = seam sub-tranche) — and C2 caught a LIVE instance same-tranche (Snd_ChanClass under-declared `out(carry: music)`, fixed). Scoreboard stays 36/6 (first-cut-clean file; Fade_Ramp precision-widened). Kill row 78. Strict **2813/0 (1 ignored)**. Masters **aeon `656115d` / sigil `12fd111`+provenance**. Close packet `2026-07-29-t36-close-packet.md`. **Remaining: t35 P2 in flight (mid-loop) · P4 sensors · 3 objects · 2 harness states · sfx (rung 3b) · driver (rung 4, T-state first) · seams · generator.**

> **✅ TRANCHE 35 MERGED 2026-07-29 — P2/P3 PLAYER STATE MACHINES (census P2+P3 merged) + the −0x20 wave (NEW CANONICAL `4b66cace`/`1c256b3b`).** `player_ground.emp` (783 L) + `player_air.emp` (470 L) + `player_spindash.emp` (119 L) — byte-exact step 1 proven at FULL mixed whole-ROM identity both shapes (the first tranche at full strength under the combined-link fix; no windowed-only concession), then the step-2 control-flow wave (16 shrink sites = −0x20, pad-absorbed; **TWIN-WIDTH FINDING: sigil's AS frontend pins branch width — the AS twins carry EXPLICIT widths from the asl listing**, a bare-twin build fails 208 errors). All 7 `PState_*` dispatch targets = real `.emp` defs (offsets-construct adoption stays P4-ledgered). 4 sensor `extern proc` boundary decls / 7 sites, die at P4 (kill row 80). **`abs_w` comptime fn** (dry-panel A1+B1 consensus) built + step-6 swept: 12 folds (9 step-4 + player_common ×3), 6 engine sites LEDGERED-BLOCKED on the **abs_w engine-hoist parcel** (the wrong-home first-consumer lesson; same blast-radius class as the **engine.constants hoist parcel** — both queued post-t35). **C1 elevated G9: `Ground_Move_Cap` byte-loads d7, uses it as WORD without a clear — benign only under current dispatch, BYTE-GATE-BLIND (shared-twin class), ledgered w/ oracle-A/B rider at P4/post-conversion.** C2 clean-no-divergence; QuadrantId/ButtonMask-vs-Bit item-13 rows. Kill rows 79-82 (renumbered at the overseer rebase; t36 holds 78). Strict **2827/0 (1 ignored)**. Masters **aeon `1cbd4fd` / sigil `37abcac`+provenance**. Packet `2026-07-29-t35-checkpoint-b-packet.md`. **Remaining: the 2 hoist parcels · P4 sensors (retires rows 80/81 + the 5 PlayerV guards) · 3 objects · 2 harness states · sfx (rung 3b) · driver (rung 4, T-state first) · seams · generator.**

> **✅ HOIST-AMBIENT PARCEL MERGED 2026-07-29 (byte-neutral).** 45 engine-truth consts → `engine/system/constants.emp` w/ guards moved-not-dropped (rows 76/77/81 reduced) · `abs_w` → `engine/coords.emp` + the 6 blocked engine folds (row 82 COMPLETE) · BUTTON_*_BIT ×14 adopted. **Correction accepted: PPHYS_*/BUTTON_JUMP_MASK are GAME-truth (player_common.asm) — stay file-local, layering preserved.** Canonical unchanged; strict 2827/0. Masters **aeon `0037db4` / sigil `aa85600`**.

> **✅ TRANCHE 37 MERGED 2026-07-29 — sound_sfx (rung-3b): THE Z80 CODE FRONT IS DRIVER-ONLY.** `engine/sound/sound_sfx.asm` (1627 L) → `sound_sfx.emp`, scale-(1) BOTH shapes (1516 B = $5EC both; shape-VARIANT base $0CD7/$0D55 = the sequencer's +$7E upstream; NO own `__DEBUG__` blocks — the psg t32 case, measured not assumed). Byte movement ZERO; firing arc **3→0 self-corrected** (no finisher). **THE FIRST CLOBBERS UNDER-CLAIM in the sound corpus: Sfx_Frame's header omitted `iy` (its iy-walking callees destroy it) — fixed lockstep both twins, honest superset declared, and PROVEN checker-invisible (clobbers-completeness is enforced LOCALLY, not transitively) → the `[call.clobbers-incomplete]` demanded diagnostic = the symmetric second half of the t36 extern-decl-vs-def drift problem** (extern preserves over-states; proc clobbers under-states; one fixpoint closes both). 13-extern by-hand verification set (11 co-resident conservative-subsets + 2 driver die-at-port); invariant(ix) = the t36-mirror adjudication (Sfx_Frame is the sfx-side walker). Header scoreboard: over-claims 6 / **under-claims 1 (new class)**. B1 tripled the base_plus_idx demand data (4→12 sites). Kill row 83. Strict **2832/0 (1 ignored)**. Masters **aeon `b26efc8` / sigil `abe7f1b`+provenance**. Close packet `2026-07-29-t37-close-packet.md`. **Remaining: t38 P4 sensors in flight (the player-cluster close) · 3 objects · 2 harness states · driver (rung 4, T-state first) · seams (input set COMPLETE) · generator.**

**Current state (2026-07-21):** **PHASE 1 COMPLETE.** All four Phase-1 items merged —
contract-grammar v2 arc is G1+G2+G3+substrate+G4+G4.5(#1–#4). Masters: sigil `871ec7d` /
aeon `ae1de4d` (aeon untouched since #2; #3 and #4 were both pure-sigil, byte-neutral).
Item **#4 (the D1b WARN→ERROR flip) MERGED `871ec7d`** — the verified-out fixpoint is live
and the net is an ERROR gate before pass-3 opens. **Phase 2 (pass-3) is now OPEN.**
Canonical ROMs **RE-BASELINED 2026-07-21** (Deep-Forest-BG art parcel, byte-CHANGING):
plain **`3aa43cb6`/`420749`**, debug **`ce0e83a6`/`428768`** (supersede `8984e510`/`453533` ·
`c80465dc`/`461554`; see `golden/PROVENANCE.md`).
Strict mode **ON for D1b** — `[call.input-undefined]` is now a live ERROR gate under
`SIGIL_STRICT_GATE`, resting on the VERIFIED-out fixpoint (an out() is credited as a
definition only once proven honest). D1c stays observe-only; the out-verify residue (16)
stays WARN.

**Guiding principle:** *Contracts go on stable code.* Finish **and verify** the safety net → optimize under it → delete dead code → type the settled register layout → then port parallax.

> **Ordering note that supersedes the original v2 design doc.** The doc says "pass-3 unblocks after G1+G2." That predates the G4 session. G4 made `out()` labels **load-bearing but unverified**, and pass-3's "trust the contract" hoists now depend on those labels being honest (FindStagedBlock was a real mislabel — proof the trust is currently misplaced). So **Phase 1 (verify the contract system) must land before Phase 2 (pass-3).**

This roadmap covers the three output buckets of the 4-wave optimization review — **optimization**, **bugs**, and **language failings** — each is tagged `[opt]` / `[bug]` / `[lang]` below.

---

## Phase 1 — Complete & verify the register-contract system  *(before any optimization)* — ✅ **COMPLETE (all 4 items merged; D1b is a live ERROR gate)**

1. ~~**out()-verification arc** ("G4.5") `[lang]`~~ — **DONE, merged 2026-07-19** (sigil
   `cd10321`). Caught 2 real allocator mislabels. Residue: Buckets 2/3 (→G5), mutual-callee-out
   fixpoint, conditional-external-tail (grep-proven-absent) — all on the #4 blocker list.

2. ~~**Edge-sensitive conditional-out crediting** `[lang]`~~ — **DONE, merged 2026-07-21**
   (sigil `0d4b529` / aeon `3eba8fb`). Bucket 1 CLEARED (AllocDynamic/AllocEffect honest
   `out(a1 if eq)` + Load_Object cascade), FillColumn D1b FP CLEARED. Includes the Finding-7
   label-join bail (review-proven `valid_edge` hole, fixed pre-merge). D1c FPs now 2,
   documented, deliberately not edge-coupled.

3. **S2-D6 checked-clobbers / preserves lint** `[lang]` ✅ **DONE — merged sigil `3f333d2`
   (2026-07-21, byte-neutral; aeon `ae1de4d` untouched, pure-sigil).** Stage-0 re-census
   confirmed the brief's hypothesis: the transitive `[proc.clobber-undeclared]` residue was
   already 0 (error-gated since G3), the individual-push FP trio retired. Re-scoped to the two
   real defects Stage 0 surfaced — **A** write-detector completeness (dbcc counter + non-stack
   movem-LOAD reglist; the `(sp)+` restore exempt as preserve-discipline) and **B** the local
   `check_clobbers` FP-kill (subtract §5-verified preserves; cleared 25 firings on honestly-
   `preserves()`-declared registers). **Headline catch: A removed a FALSE dead-save
   (`WarmupBelowRow` d6 bracketing `DecompressBlock`) — the dead-save worklist is now 15 rows,
   not 16; d6 is clobbered ONLY by the `movem.l (a0)+, d0-d6/a2` burst the detector was missing,
   so pass-3 would have wrongly deleted a necessary save (see packet
   `2026-07-21-s2d6-packet.md`).** Two attack-diff findings fixed (written_names conditional-dbcc
   over-credit; tripwire gate-compliance). Gaps adjudicated: dbcc → closed (A); movem-pair →
   already closed (substrate §5); s4lint W021 → moot (not a live tool); per-callee union (d) →
   deferred to its Phase-2.5 Tier-C consumer (gap-ledger). D1a transitivity stayed out of scope.
   Snapshot: closure 0 / D1b 0 / D1c 2 / dead-saves 15 / out-verify 15; strict 2445/0/1.

4. **WARN→ERROR flip** (D1b strict) `[lang]` ✅ **DONE — merged sigil `871ec7d` (2026-07-21,
   byte-neutral; aeon `ae1de4d` untouched, pure-sigil).** `[call.input-undefined]` is now a
   HARD ERROR gate, and the credits it rests on are VERIFIED, not declared. Built from the
   overseer brief. **Stage-0a settled it:** predicted new-D1b-firing set = EMPTY at the
   aggressive 17-out bound (every unverified out is in-out — defined upstream, must-def never
   kills — or a pure-out read-consumed / `.asm`-only), so the flip needed **NO G5 pull-forward**;
   Buckets 2/3 stay WARN residue. **Mechanism:** verified-out fixpoint (`compute_verified_outs`
   — extern outs seed verified as §3 axioms; monotone; round-cap panic) grounds the credit; the
   DEFINITION surfaces (D1b must-def + out-verify residue) switch to verified credit, the
   REDEFINE-EXCUSE surfaces (§6 taint-kill, D1c held-value) keep declared — the **define-vs-
   redefine dividing line**, empirically proven (switching D1c would add 11 narrow-width FPs).
   Also awakened four AEON_DIR-gated corpus tests (incl. three shipping ERROR gates) that
   silently skipped under the standard strict invocation. Residue moved 15→16 (fixpoint chain-
   grounding surfaces `Collision_GetType::out(d0)`). Mutual/circular callee-out CLOSED (fixpoint,
   no corpus instance); conditional-external-tail re-confirmed grep-absent, guard stands. §6
   existence-lie exposure deferred to the per-lie-class-credit gap-ledger row, tripwire-guarded.
   Snapshot: closure 0 / D1b 0 (**ERROR**) / D1c 2 / dead-saves 15 / out-verify 16; strict
   2454/0/1. Packet + dividing-line table: `2026-07-19-out-verification-residue.md` (2026-07-21
   addendum).
   *Why before pass-3:* a WARN net doesn't *catch* bad hoists during surgery — an ERROR net does. Net live before you cut. ✅ **The net is live before the cut.**

---

## Phase 2 — Pass-3: register / object-render optimization  *(under the live net)* — 🟢 **OPEN (the net is live; Phase 1 complete)**

*Design-gate first: run the §C trigger checks, then batch the work.*

5. **Dead-save worklist** `[opt]` ✅ **DONE — Parcel A, merged aeon `39faa02` / sigil `015f76e` (2026-07-22, BYTE-CHANGING).** All 15 rows removed (9 length-preserving movem narrows + 2 full movem-pair removals, −16 bytes); attack-the-diff PASSED. NEW canonical plain `748ca5ba`/420749 · debug `d5d8e163`/428768 (EndOfRom unchanged). Merge-phase catch: a stale `s4.debug.lst` mis-repinned debug → shipped a `repin` listing-freshness hard-check as a hardening rider. (was 16: item #3's detector fix exposed the `WarmupBelowRow` d6 row as a FALSE dead-save — deleting it would have corrupted d6; see the s2d6 packet.)
6. **D1c contract-precision (byte-neutral)** `[opt]` ✅ **= Parcel B (branch `pass3-parcelB-hoists`).** Reframed from "caller-side-hoist fuel": the D1c-clear fuel contained **no code hoists** — the sites are TIGHT BY CONSTRUCTION (D1c fires only when a caller reads a reg after a call *without* reloading, so there is nothing to remove), and the freed registers are not hoistable loop-invariants. So B is **4 byte-neutral clobber tightenings** (`Load_Object` d2 · `RescanObjects`/`ScanObjectsRight` d5 · `Scan` a5), found via a `declared ∖ effective` sweep (banked as the `[proc.clobber-unexercised]` lint's regression seed). *(Second time the net converted anticipated byte-surgery into contract precision — cf. S2-D6 #3's stage-0.)* LICM hoist-hunt rejected (no frame-lag pressure; Parcel D carries the EV-ranked review candidates).
7. **Step-5 pass-3-adjacent riders** `[opt]` ⊘ **DISSOLVED (Parcel C stage-0, 2026-07-22).**
   **W022/W025 = 0 firings** (whole `.asm` corpus is s4lint-clean — pass-2 restructures + the port
   campaign dissolved their sites; census cross-ref confirms W022 was reclassified a future perf-lint).
   The **`move.l`-pairing** rider survives (tile_cache FillRow/collision + plane_buffer drain still
   `move.w`/`move.b`) but is non-lag-critical (≥35% idle); **folded into the 8b parcel** as its own
   bisectable commit(s) sharing 8b's ripple + PROVENANCE (logged slot, not dropped, per no-silent-skips).
   *Third net-conversion of anticipated byte-surgery into already-done/evaporated (after S2-D6 #3 stage-0
   + Parcel B hoists).*
   *(Note: "D7 deletions" is NOT here — it is its own byte-changing batch, Phase 2.5.)*
8. **Object/render candidates** `[opt]` — Tier-B — ⊘ **CLOSED-EARLY (2026-07-22): ZERO CUTS AFTER 8b.**
   sprites H1/H2/H3 · rings R2/R3 · entity_window #1/#3/#4 · core #1/#2 · animate A2/A3 · section H1/H3.
   > **HEADLINE:** the entire Tier-B census hotness was an **inclusive-artifact, queue-wide** — every
   > "hot" number (RunObjects 34.8%, section 6.3%, ew 2.9%, …) was inclusive of dispatched/fill/walker
   > work the proposed transforms don't touch; measured **plain-shape addressable SELF-time is sub-2%
   > for all 13 items**. And there is **no lag to relieve**: the pass-2 / unified-prefetch / free-lunch
   > arcs already ate it (VSync idle **48.8% max-H, 54.3% churn; Lag=0 all regimes**). Closed via 3 mini
   > design gates (core #1 #4, sprites #5) + one Bar-A census sweep (the remaining 10 rows); all surfaces
   > banked to the gap-ledger with reopen conditions. Notes: `2026-07-22-{core1-runobjects,sprites-h1h2h3,
   > barA-census-sweep}-design.md`.
   - **8b DONE** (FindStagedBlock memoize + 2 move.l riders + F2 census; aeon `5c975af`/sigil `4993b0b`).
   - **core #1 (RunObjects) ⊘ DISSOLVED-STAGE-0 (2026-07-22, dissolution #4 — hotness-measurement
     artifact).** Census "34.8%" was DEBUG *inclusive*; addressable machinery self ≈**5.75%** in a
     **54%-idle** plain frame. No parcel cut (byte-changing AND byte-neutral both declined). Two surfaces
     banked to the gap-ledger (`.culled_loop` declared∖effective sweep · branchless-abs cull math w/ `$8000`
     pin). **Reopen = a real-scene lag report** (54%-idle is churn-stressor-relative). Note:
     `2026-07-22-core1-runobjects-design.md`.
   - **sprites H1/H2/H3 ⊘ DISSOLVED-STAGE-0 (2026-07-22, dissolution #5 — value-vs-ceremony).** Ranking
     CONFIRMED (Render_Sprites self **8.9%** > RunObjects 5.75%) but no cut: scene-bias is **per-path** —
     churn over-counts H1's per-object lever (40/40 ≥ gameplay), so H1's **~0.5-1% is a CEILING**; no lag
     (54.3% idle), no paid ripple to ride → core-#1 branchless-abs shape. H2 evaporated (already
     comptime-unrolled); H3 non-binding VBlank DMA (PB1 satisfied). Three surfaces banked to gap-ledger.
     Reopen = real-scene lag OR elected headroom pass; H1's reopen first-gate = corpus-wide
     `mapping_frame`-drift writer sweep. Note: `2026-07-22-sprites-h1h2h3-design.md`.
   - **BAR-A CENSUS SWEEP — DONE, RULED-ACCEPTED (2026-07-22).** All 10 remaining rows measured
     plain-shape self-time and DISSOLVED (section H1 0.85% · ew #1 0.66% · core #2 1.9% in its own
     delete-storm vehicle · animate A2/A3 ~60c/~24c structural · rings R2/R3 0.7%/0.8% @ 13 rings). Rings
     harness-vs-close ruled **CLOSE both** (no realistic ring-saturation scene). Reopen conditions banked
     per row (core #2 = >4 deletes/f; rings = ring-heavy content + X=0-mask rider; rest = real-scene lag /
     elected headroom). Table: `2026-07-22-barA-census-sweep.md`.
   - **MILESTONE CHECKPOINT → Volence** (next-arc options): **t18 parallax port (overseer-recommended
     next)** · **VDP shared-module row 1073** (optional warm-up) · **H-streaming successor charter (rows
     1066/1074) — PARKED, and REQUIRES ITS OWN BAR-A STAGE-0 before any charter** (census numbers are
     inadmissible there too — same inclusive-artifact risk). The next arc opens on Volence's pick.

   > **STANDING BAR A (ratified at the core #1 gate, 2026-07-22).** Every remaining D-parcel stage-0 MUST
   > present **fresh PLAIN-SHAPE addressable SELF-time** (inclusive − children, decomposition shown).
   > **Census inclusive/DEBUG numbers are INADMISSIBLE as hotness evidence** — the whole D census is
   > suspect the way core #1 was. Applies to survivors AND parks.
   >
   > **STANDING BAR B (ratified 2026-07-22).** When the idle-margin frame-anchored A/B method is used, the
   > run MUST **record the lag-frame counter on both sides** — "no lag frame" is measured evidence, not an
   > assumption.
9. **Micro-batch riders** `[opt]` — aabb 3-piece split · vdp_init M1 · dplc D3 · VDP shared-module (row 1073, 2nd typed-VDP consumer).

---

## Phase 2.5 — D7 dead-code deletion batch  *(its own gated, byte-CHANGING batch)*

> **✅ CLOSED-MERGED 2026-07-22** — combined item-9 + Phase-2.5 arc (c1–c6 + MigrateMasks fix)
> merged: **aeon master `033865f` / sigil master `9b89d67`** (`pass3-phase25-item9` --no-ff).
> NEW CANONICAL: plain **`406c773b`**/421122 · debug **`5752c2e3`**/429107 (ASSEMBLED_LEN unchanged).
> Full paired strict on merged masters **2457/0/1**; overseer attack-the-diff PASS + re-attack PASS.
> **Cut:** Spawn_Count · CROSS_RESET (whole dead mechanism) · ess_*_left_idx (mid-struct) + the
> byte-neutral D7 const/flag purge. **PARKED (gate rulings, gap-ledger reopen conditions):**
> Hscroll_Dirty_* → t18 (parallax retires it free) · Tier-C movem (item 11, children.asm port
> adjudicates) · Tile_Cache_GetTile (next tile_cache byte-changing edit) · aabb#1 · dplc D3.
> **Caught:** a silent, byte-gate+boot-invisible MigrateMasks hand-stride bug (ledger blind-spot row).

10. **D7 dead-code / dead-symbol deletions** `[opt]`  *(ledger row 1091)*
    Deletes live RAM symbols / dead writers: `Spawn_Count`, `CROSS_RESET_MAGIC`, dead `ess_*` indices, `Hscroll_Dirty_*`, `Tile_Cache_GetTile` (zero callers), the wave-4 dead-constants list, dead `DEBUG_*` flags.
    **Byte-changing** (RAM layout shifts / removed stores) → moves the byte gates → needs both-shape lockstep + re-pin + provenance. Cannot ride a byte-neutral pass. Ratified scaffolding (e.g. `Plane_Buffer_Reset`'s reset hook) is **annotated, not deleted**.
11. **Tier-C movem deletions** `[opt]` — dplc / load_object / AllocDynamic; unblocked once the Phase-1 S2-D6 lint exists (per-caller union proven mechanically).

---

## Phase 3 — G5: type the now-stable register layout

12. **G5 — typed register slots (§7)** `[lang]`  *(ledger rows 1054, 1069)* — ✅ **DONE 2026-07-23** (merged; byte-neutral, canonical 406c773b/5752c2e3 UNCHANGED = the Phase-3 landing proof). Axis-split `GridX`/`GridY` + `SectionId` at the FlatIDXY/GetSecPtrXY seam; `out(dN: Type)` grammar + `as`-bless + the `[call.slot-type-mismatch]` strict-degrade reaching-def slice; swap class closed at every seam site. `FlatIDXY.d2` banked u8 (preserves-verifier limit — new ledger row). Spec+addendum: `2026-07-23-g5-typed-register-slots-spec.md`.
    *Why last:* G5 types the exact register slots pass-3 reshapes — type the **final** layout, not a moving target. **Byte-neutral**, so it lands on pass-3's settled canonical and proves it changed nothing. Demand step: run the `// In:`/`// Out:` proc-header census (row 1069) once signatures are stable.
13. **Prelude domain-type pass** `[lang]`  *(sibling / follow-on; "construct walk #3/#4")* — **WAVE 1 ✅ DONE, merged 2026-07-24** (aeon `9fb6fcb` / sigil `4f4f0e2`; byte-neutral, canonical `0bfa5b79`/`9d962703` UNCHANGED). Shipped `SongId`/`SfxId` · `AnimId`/`AnimFrame`/`MappingFrame` (`FrameId`→`MappingFrame`) · `VramTile`/`VramAddr` (comptime-first). Enforcement-tier finding: F1 live (register call-slot), F2/F3 documentary (SST-memory / comptime-fn substrate) with reopen markers ledgered. **Wave 2 A4-i-GATED** (Tile/Block/Chunk, Coord/Velocity — shift/add chains wait for arithmetic-preservation). Ratification `7b9afa6`; close packet `2026-07-24-item13-wave1-close-packet.md`.
    Populates G5's typed-register mechanism with the wider newtype family: `Angle` (partly shipped for GetSineCosine), `SoundId`/`SongId`/`SfxId`, `VramTile`, `AnimId`/`FrameId`, `Tile`/`Block`/`Chunk`. Volence-driven design.

---

## C · Trigger-keyed reopens  *(run these checks at the Phase-2 design gate; skip if the trigger doesn't bind)*

> **CHECKED 2026-07-21, overseer-CONFIRMED 2026-07-21 (oracle profiler, 3 regimes; outcomes + method in `2026-07-21-phase2-design-gate.md`).** Both triggers resolve to **skip**; the finding-#3 memoize is sanctioned as net-new item **8b** (keyed + per-axis; before Tier-B).

- **DMA-drain reopen** `[opt]` — only if the post-pass-2 worst-case VBlank audit still binds.
  → **DOES NOT FIRE.** Worst VBlank CPU = ~55% of the ~18.5k window (max-H); drain itself 1.1%;
  main loop ≥35% idle every regime. VBlank never binds. (The lag that occurs is the cold-crossing
  *producer* decompress spike, not VBlank.)
- **FindStagedBlock direct-mapped** `[opt]` — only if it's still hot after the pass-2 memoize.
  → **DOES NOT FIRE (direct-mapped).** Stale premise: the memoize **was never built** (no commit
  in aeon history). FindStagedBlock is at full pre-memoize cost (5.2% / 19 calls, max-V). Direct-
  mapped stays skipped (thrash risk + A/B, low value vs idle). **Net-new candidate surfaced: the
  behavior-preserving finding-#3 memoize** (safe, no thrash) → design-gate note item 8b.

---

## D · Backlog  *(any time; not gating t18)* — ✅ **ARC MERGED 2026-07-23 (attack-the-diff PASS; masters aeon `c39f308` / sigil `0c27746`; NEW CANONICAL plain `ab787bd1`/421122 · debug `6a19669f`/429165; paired strict 2484/0/1; close packet `2026-07-23-sectionD-backlog-arc-close-packet.md`)**

- ✅ **`Sound_PlayMusic.await_slot` DEBUG watchdog** `[bug]`  *(ledger row 1090)* — **DONE + reframed.** Stage-0 surfaced the real bug behind it: the H-1 repost gate never iterated (`startZ80`'s `move #$0000` clobbered the `tst` flag before the `bne` — the constant-flag-clobber class), and `[branch.condition-constant]` (item-4 rider) found a SECOND identical bug in `Sound_Init.wait_alive`. Fixed as capture-then-test (aeon **c1** `c0db661`, both twins) + the DEBUG bounded-spin watchdog on both spins (aeon **c2** `4b5a2c0`, `SPIN_WATCHDOG_LIMIT=$8000`). The now-working spin was live-single-step-proven (the `d4` watchdog counted `$8000→$7C64` before the slot cleared — the loop iterates). Plain + debug re-baselined (sigil `32bc836`).
- ✅ **Optional-param design** `[lang]` — **DESIGN-ONLY, ruled.** `AnimateSprite d3`: Option A (`?`-optional marker) ships *when animate is next touched*; Option D (type the animation-script duration byte — the enforcing successor) gated behind the item-13 domain-type family. No implementation this arc. (Stage-0 gate item 5.)
- ✅ **@scaffolding attribute** `[lang]` — **EVAPORATED** (shipped in G1 + already applied to the one keep, `Plane_Buffer_Reset`).
- ✅ **D11 name-linkage** `[lang]` — **RETIRED.** Its guard (`ensure(extern("X")==X)`) shipped + is a mandatory port-loop rule; its auto-detector's target population (seam mirrors) is a draining kill-list artifact. Thin intra-`.emp` residual logged as a low-priority link-warning idea, not a diagnostics-tier item.
- ✅ **s4lint absorption #1 — Z80-bus machine-state contract** `[lang]` — **BUILT (item-4 core, sigil `93a309f`).** `[bus.double-stop]`/`[bus.start-without-stop]`/`[bus.stopped-at-return]`/`[bus.vdp-write-unstopped]` (E011/E008/E007/E006) over the shared `flag_check::Cfg`; 3-point MUST lattice, zero-FP polarity. First-corpus-run 0/0 both shapes (the fixed corpus is clean; teeth sentinel-proven). E008 included per the gate; E006 largely inert (indirect `(a4)` VDP writes punted). **Remaining absorb candidates** (`2026-07-22-s4lint-absorption-census.md`): #2 W026 width-discipline dataflow (pairs with G5 width typing), #3 E010 SR coverage via §5, #4 debug-seam checks, then perf/peephole WARN tiers. End-state unchanged: the last `.asm` port retires s4lint — one tool, one truth.

---

## Bucket coverage check (the review's three outputs)

| Bucket | Status |
|---|---|
| **Optimization** `[opt]` | Tier-A done (pass-1/2); Tier-B = Phase 2; Tier-C = Phase 2.5 (gated on S2-D6); D7 batch = Phase 2.5. ✅ |
| **Bugs** `[bug]` | All confirmed bugs (sprites PB1/PB2 + wave-2 B1/H-1/C1/D1/E1) fixed+merged; one open rider = await_slot watchdog (§D). ✅ |
| **Language failings** `[lang]` | out()-verification + edge-sensitive + S2-D6 lint (Phase 1); G5 + domain-type pass (Phase 3); diagnostics remainder (§D). ✅ |

---

## Not a hard dependency

Parallax (t18) is **unported**, so it depends on *none* of Phase 2/2.5/3. If beating frame-lag is the priority, t18 can jump ahead of pass-3 — a deliberate call, not a forced sequence.

## Two spots to confirm before committing  *(summarized from dense notes)*

- **Item 8** — the Tier-B pass-3 candidate list: verify against the review doc.
- **§D diagnostics remainder** — verify exact scope against the v2 design doc.

## References

- Contract-grammar v2 design doc: `sigil/docs/superpowers/specs/2026-07-17-contract-grammar-v2-design.md` (the G1–G5 table is §"tiers"; G5 = §7).
- 4-wave optimization review: `aeon/docs/reviews/2026-07-16-emp-port-optimization-review.md`.
- Gap-ledger (row detail): `sigil/docs/superpowers/notes/campaign-gap-ledger.md` — rows 1023/1030/1062 (S2-D6), 1054/1069 (G5), 1090 (watchdog), 1091 (D7), 1092 (move.l pairing).
- Campaign log: `spec2-progress.md` (memory; newest at file tail).
