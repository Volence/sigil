pwd=/home/volence/sonic_hacks/sigil/.claude/worktrees/agent-a735fcd77afe45035 HEAD=f9505ad649c4cc5ffaab47de1412ced652dc95fc branch=parcel/z80-half-registers tracked-changes=0
CARGO_EXIT=0
binaries 170 passed 809 failed 0 ignored 1
FAILED tests: none
banner: PARTIAL RUN (SIGIL_ALLOW_PARTIAL is set). No reference tree is named, so 131 test binaries are reference-dependent and every row in them is left UNMEASURED. A green result from this run does NOT mean 
banner: PARTIAL RUN (SIGIL_ALLOW_PARTIAL is set). No reference tree is named, so 131 test binaries are reference-dependent and every row in them is left UNMEASURED. A green result from this run does NOT mean 
banner: PARTIAL RUN (SIGIL_ALLOW_PARTIAL is set). No reference tree is named, so 131 test binaries are reference-dependent and every row in them is left UNMEASURED. A green result from this run does NOT mean 
