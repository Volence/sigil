# Wave-B parcel: plane_buffer #2 — VInt_DrawLevel column drain move.l pairs (A/B)

**Parcel:** `.drain_col` `move.w/dbf` → `move.l` pairs (one move.l = two word writes,
each autoincremented $80) + an EXACT `btst`-guarded odd-word tail — an extra word past
row 63 would land at $E000 (Plane B), the named test vector. aeon `opt-wave-b`.
**Class PF (VBlank window).**

**step-0 re-confirm:** review item 1.2 vs `plane_buffer.emp` `.drain_col` — the
word-drain loop was live. OPEN confirmed.

## Profiler A/B — max-H drive, vs the pb#4 baseline

| proc | pb#4 base | pb#2 | delta |
|---|---|---|---|
| VInt_DrawLevel | 3380 | 2636 | **−744** |
| VInt_Level (VBlank total) | 10172 | 9432 | −740 |
| Draw_TileColumn (producer) | 5196 | 5196 | 0 |

**Threshold ruling (logged with numbers per OQ-6):** −744 cyc/f is under the general
~1k bar, but this parcel's budget is the VBLANK WINDOW (~18.5k cycles; `VInt_Level`
55% → 51% of it), and the saving scales per column entry (~370/entry): the 2-entry
steady drive understates the ~11-column worst case (~4k relief) where the window
actually binds. KEEP on the window rationale.

## PS state identity — scroll drive, plain shape, quantum anchors 240/420/700

OLD = the pb#4 golden (`4f5aa60c`), NEW = `1c37c489`; runs duplicated, determinism OK
both ROMs (emulated state; the racy GUI fb snapshot excluded — see the pb#4 note).

- **RAM byte-identical at fc 234/414** (including the exception stack); at fc 694 only
  the two established dead-stack signatures (the interrupted-PC and the word-straddled
  stale parallax return address, now shifted by this parcel's +0x10 packing delta).
- **CRAM/VSRAM identical everywhere.**
- VRAM: DIFF at fc 234 only, converged by fc 414 — the pb#4-classified drain-progress
  capture aliasing (the quantum lands mid-VBlank; the faster drain changes what's
  in-flight at the pause instant). The drain emits the byte-for-byte same word stream
  (pairing changes bus grouping, not content), and RAM identity rules out any producer
  divergence.
- VDP regs: DIFF at fc 414 only — the A3-classified mid-VBlank register-write-progress
  aliasing; identical at the other anchors.

**Verdict: PS bar MET (all deltas in the three established classified classes);
PF bar KEEP on the VBlank-window rationale with numbers.**

## Rider ruling recorded here: plane_buffer #3 = LOG-AND-SKIP

The ready-command-longword entry-header change saves ~26 cyc/entry in VBlank; at the
observed 2–3 entries/frame that is ~50–80 cyc/f (worst case ~290) — under every bar —
and it buys an entry-FORMAT change that re-opens the b96c861 tear invariant and the
section.emp reserve constants (cross-file risk). Skipped per the threshold rule;
numbers above are the record. Reopen only with a drive showing entry counts an order
of magnitude higher.
