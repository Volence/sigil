# Wave-B parcel: plane_buffer #4 — Draw_TileColumn wrap-check hoist (A/B evidence)

**Parcel:** `Draw_TileColumn`'s `.pA_data`/`.pB_data` per-iteration `cmpa.l/blo`
circular-wrap checks hoisted into `.copy_col_run` — a shared two-run local subroutine
driven by rows-before-wrap (from `Cache_Origin_Row`), each run a check-free
`move.w/dbf`. Riders (OQ-6, same file): the zero-fill loops' `clr.w (a2)+` (12c) →
`move.w a1,(a2)+` from a zeroed a1 (8c; a1 freed by the hoist — the old wrap
sentinel); `Draw_BG_TileColumn`'s `adda.w #128,a1` → `lea 128(a1),a1` (4c/iter, the
review §1.6 tail note). aeon branch `opt-wave-b`. **Class PF.**

**step-0 re-confirm:** review `aeon/docs/reviews/2026-07-16-emp-port-optimization-review.md`
item 1.4 vs `engine/level/plane_buffer.emp:132-139/174-181` — the per-iteration
`cmpa.l a1,a0 / blo` pair was live in both part loops. OPEN confirmed.

**The B-0 dependency (the parcel that forced the mechanism):** this +18-byte growth
was Wave B's first byte-size change and collided with `tile_cache`'s pin —
the packed-placement mechanism (B-0, `2026-07-31-waveb-b0-computed-placement.md`)
landed first; this parcel is its live t24: the build now shifts the engine run
downstream automatically (plain 412306→412328, debug 422147→422171; all placement
gates green).

## Profiler A/B (the PF bar) — max-H scroll drive, s4.debug shape

Drive: deterministic hold-right from reset (OJZ boot scene), profile window = 150
frames of steady 16 px/frame scroll (camera 2016→4416, well before the 5824 clamp),
`Lag_Frame_Count` 26→26 both builds (zero lag in window — the win banks headroom).
Runner: `golden/ab/waveb/profile_drive.py`; BEFORE/AFTER JSON committed beside it.

| proc | before cyc/f | after cyc/f | delta |
|---|---|---|---|
| Draw_TileColumn | 6944 | 5196 | **−1748** |
| Section_UpdateColumns (incl.) | 8036 | 6288 | −1748 |
| VInt_DrawLevel | 3380 | 3380 | 0 (untouched — pb#2's target) |
| GameState_OJZScroll_Update | 55983 | 54255 | −1728 |
| VSync_Wait (idle) | 61456 | 63184 | +1728 (the win, returned to idle) |

Threshold: **−1748 cyc/f ≥ the ~1k bar — PASS.**

## PS state-identity A/B — scroll drive, plain shape, quantum anchors 240/420/700

OLD = the committed golden (`8692e93a`), NEW = the parcel build (`4f5aa60c`); each ran
twice (determinism). Layout-shift note: RAM words holding ROM addresses legitimately
move by the section delta — the runner (`golden/ab/waveb/ab_pb4.py`) classifies a
differing aligned long as a RELOCATED ROM POINTER iff both values are ROM addresses
with 0 < delta ≤ 0x20; anything unclassified fails the bar. VRAM/CRAM/VSRAM and the
hashed framebuffer carry tile indexes/pixels (no addresses) and must be identical
outright.

RESULTS:
- **Determinism:** run1 ≡ run2 per ROM on all emulated state (RAM CRC + VRAM/CRAM/
  VSRAM/regs hashes) at every anchor. (The GUI's framebuffer-snapshot hash is
  wall-clock racy under scroll motion and is excluded; pixels are a pure function of
  the VDP state compared here.)
- **CRAM / VSRAM / VDP regs: identical** OLD vs NEW at every anchor.
- **Work RAM: byte-identical except two classified dead/exception-stack signatures**:
  0xFFFFFEF8 holds the VBlank-frame interrupted-PC inside the `VSync_Wait` poll
  (`22D4`→`22D0` — the intra-frame phase skew, the A3 signature at the same
  addresses); 0xFFFFFEB0 (below SP) holds a stale parallax-region return address
  shifted by the packing delta (+0x20) — the relocated-ROM-pointer class. Every
  streaming/cache cursor, counter, and buffer is identical.
- **VRAM: identical at anchors fc 414 / 694 (and 242+)**; at fc 234–238 exactly TWO
  plane-A cells differ (col 36, rows 35–36 — full-VRAM dump diff committed logic:
  `vram_dump.py`), converging by fc 242. Classification: the cells sit in an
  OFF-SCREEN stream column (visible window spanned world cols ≈[188,208]; col 36 maps
  to 164/228) and heal ≥15 frames before entering visibility; with ALL work RAM
  byte-identical throughout (no streaming decision diverged), this is pause-instant
  aliasing of in-flight plane writes under the ~1.7k-cycle skew (the A3 register-file
  capture class, at nametable granularity). OPEN NOTE: the exact micro-mechanism
  (VDP FIFO commit state vs drain progress at the frame-time quantum) is not pinned
  down; bounded by the RAM-identity + convergence + off-screen facts above.

**Verdict: PS bar MET** (state identity modulo classified capture aliasing +
relocation signatures), **PF bar MET** (−1748 cyc/f ≥ ~1k). Protocol note for the
wave: quantum captures alias in-flight VDP writes exactly as they alias mid-VBlank
register sequences — prefer code-point anchors when the oracle's breakpoint path is
healthy (it wedged twice tonight; see the oracle defect intel in the A3 note).

