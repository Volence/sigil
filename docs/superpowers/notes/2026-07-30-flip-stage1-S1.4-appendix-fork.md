# 2026-07-30 — FLIP STAGE 1 · S1.4 the deb2-appendix identity — INVESTIGATION + MANDATORY STOP

Status: **STOP — S1.4's literal bar (`native full file == asl full file == eff2396f/413577`
INCLUDING the deb2 appendix) is NOT achievable without a large symbol-NAME
reconciliation that spans the sigil compiler's label-naming model, the `.emp`
comptime-table DSL, and dozens of `.emp` local/global label names.** The appendix is a
debug symbol table (MD-Debugger) whose bytes are not a ROM-behavior property, and whose
golden is re-frozen to sigil's own output post-Stage-2 anyway. This is a design fork the
overseer must rule before S1.4 can land. Everything downstream (demo appendix, Config-A/B
goldens, the golden freeze) inherits the ruling.

The **assembled-ROM** identity is UNAFFECTED and remains green: `native_rom_{plain,debug}`
still byte-match `s4.bin`/`s4.debug.bin` over `[0, EndOfRom)` (eff2396f/413577 ·
1e9097bc/421579) via `assert_rom_matches_convsym`. The fork is ONLY the appendix bytes.

Baseline this session (overseer-verifiable): aeon `bcb8f64`, sigil branch `flip-stage1`
tip `72a921e`. Reference ROMs rebuilt with `SIGIL_EMIT=…/emit_sound_blob` and CRC-verified:
`s4.bin` eff2396f/413577 · `s4.debug.bin` 1e9097bc/421579 · `demo.bin` 2b71b37d/88738 ·
`demo.debug.bin` b0475a59/91584 — all four match the goldens. S1.1 `native_rom` gate: 2/2
pass. No source changed this session (investigation only; throwaway dump tests removed).

## What the pipeline is (verified empirically against the prebuilt `tools/convsym`)

`build.sh:169-175`: after `p2bin`, if `${ROM_NAME}.lst` exists,
`convsym ${ROM}.lst ${ROM}.bin -input as_lst -range 0 FFFFFF -exclude -filter "z[A-Z].+" -a`
APPENDS the MD-Debugger deb2 table at `EndOfRom` and bumps `$1A4` (ROM-end pointer); then
`fixheader` re-checksums `$18E`. The full file = assembled ROM + deb2 appendix.

**convsym `as_lst` reads the `Symbol Table (* = unused):` SECTION of the `.lst`** (not the
body listing lines — a body-only `.lst` fails `FATAL: Coudn't find symbols table`). From
that section it takes **only `C`-marked (code/address) rows** — equate (`-`) rows are
excluded, and the unused `*` flag is IRRELEVANT (both used and unused `C` symbols are
included). `-range 0 FFFFFF` then keeps only symbols with value ≤ `0xFFFFFF`, which
**excludes every RAM symbol** (`$FFFFxxxx`, shown by asl as `FFFFFFFFFFFF____`). The
`-exclude -filter "z[A-Z].+"` filter matches zero Aeon labels (confirmed) but is passed
verbatim. Output symbols are address-sorted inside the deb2 table.

Net: the appendix is a pure function of the **set of `C` symbols with address ≤ 0xFFFFFF —
their NAMES and ADDRESSES only**. To reproduce asl's appendix byte-for-byte, sigil's `.lst`
symbol-table section must present that exact name+address set.

### Positive-control CORRECTION (design §3.3 was wrong)

The deb2 header is `de b2` followed by **data-dependent** bytes — observed `de b2 04 02`
in the real ROMs but `de b2 00 1a` / `de b2 00 06` in synthetic runs (the 3rd/4th bytes
encode the packing/offset, not a fixed magic). **The assert-PRESENCE control must check
`de b2` (2 bytes) at `EndOfRom` + a non-trivial size band, NOT `de b2 04 02`.** A silent
convsym failure (`2>/dev/null || true`) still yields an appendix-less ROM = hard FAIL.

## The measurement — addresses PERFECT, names STRUCTURALLY divergent

Method: dumped sigil's joint link symbol table (AS residual + placed `.emp`) from the
all-gates-ON native build via `resolve_layout`; extracted asl's `C` symbols from
`s4.lst`/`s4.debug.lst`; restricted both to convsym's range (≤ 0xFFFFFF); translated
sigil's local mangling `$module$Parent$local` → `Parent.local`.

| shape | asl C (in range) | sig (in range) | addr mismatch | asl-only (rename / truly-absent) | sig-only (of which genuinely-extra addr) |
|---|---|---|---|---|---|
| plain | 1342 | 1387 | **0** | 140 (137 / 3) | 185 (50) |
| debug | 1376 | 1443 | **0** | 144 (141 / 3) | 211 (72) |

**ZERO address mismatches** — every symbol the two toolchains share sits at the identical
address (the M1 byte-identity result at the symbol level). The ONLY divergence is NAMES.
End-to-end proof: fed a sigil-derived `.lst` (translated names) through the real
`tools/convsym` over the native assembled ROM → appendix built cleanly (rc 0) but is
**3707 bytes smaller** than asl's (26,030 B vs 29,737 B) and byte-different — CRC 2f768db4,
not eff2396f. Confirms the names, not the pipeline, are the blocker.

### The name divergence is four structural classes (not a small patch)

1. **Inline-`asm{}`-block locals (the bulk).** sigil scopes locals inside inline asm blocks
   to a synthetic block counter: `asm14.abs`, `asm2.clamp`, `asm34.cell`. asl scopes local
   labels to the **enclosing global label**: `Air_LandState.abs`, `Camera_Update.clamp_x`,
   `Collision_ProbeLeft.cell`. The enclosing-global name is DISCARDED by sigil's mangling
   (unrecoverable in a listing-side translation), AND the leaf differs too (`clamp` vs
   `clamp_x`, `piece_loop` vs `piece_loop_yf`). Hundreds of labels across collision/player/
   camera/touch/DMA modules.
2. **Comptime-table-generated labels.** The anim-table DSL emits internal names
   `__offsets$games.sonic4.sonic_anims$Ani_Sonic$Balance` / `__align$…$0` for what asl's
   `.asm` calls flat `Ani_Sonic_Balance`, `Ani_Sonic_End`. Different naming model entirely.
3. **Global renames** — the `.emp` chose different names than the `.asm` twin:
   `Cold_Boot`→`EntryPoint` (+15 locals), `ErrorHandler`→`ErrorHandlerBlob`,
   `Ground_Move`→`Ground_Move_Cap` (+22 locals), `QueueDMATransfer`→`QueueDMA_Deferrable`,
   `Perform_DPLC_Deferrable`→`asm3`, `TestEmitter_Main.particle_desc`→`emitter_particle_desc`,
   `Warm_Boot.*`→`EntryPoint.*`, etc.
4. **~50–72 genuinely-extra sigil labels** asl never emits (`Process_DMA_Critical.drain_1..8`,
   `asmN.wait_z80`, `TestArt_Ring`, `EmitterPositions`) — these would ADD deb2 entries.
   Plus **3 truly-absent** asl locals sigil doesn't produce (`Perform_DPLC.done`,
   `Player_SlopeRepel.speed_abs`, `Process_DMA_Critical.drain_end`).

None of these is translatable listing-side: classes 1–2 need information sigil's naming
model throws away; classes 3–4 are genuine label-name choices. Matching asl exactly would
require (a) changing how sigil mangles inline-asm locals to asl's enclosing-global scheme
AND matching leaf names, (b) changing the anim-table DSL's label naming, and (c) dozens of
`.emp` global/local renames — a sigil-compiler + broad-aeon effort measured in weeks, all
to match a toolchain that LEAVES at Stage 2.

## The fork — for the overseer to rule

- **Option A (RECOMMENDED) — sigil-canonical appendix + re-frozen golden.** The appendix is
  MD-Debugger metadata, not ROM behavior. Redefine S1.4's bar: (i) assembled-ROM bytes ==
  asl (ALREADY proven, S1.1), (ii) sigil's listing → `tools/convsym` produces a VALID,
  self-consistent, deterministic appendix (assert-PRESENCE `de b2` + size band + Oracle
  `LoadFromAsListing` / `s4budget.py` consumers parse it), and (iii) the FROZEN GOLDEN is
  **sigil's own full file** (captured now under asl-live provenance, re-verified at Stage 2).
  The `native == asl` clause holds for the assembled ROM; the appendix golden is sigil's.
  This matches the design's own observation (§5/§6.2) that post-flip goldens are re-frozen
  to sigil, and it is a stable/reproducible target (convsym is deterministic).
- **Option B — reconcile names to asl.** Make sigil emit asl-identical symbol names:
  sigil-compiler inline-asm-local naming change + anim-table-DSL label naming + dozens of
  `.emp` renames. Byte-neutral to the ROM but large, touches the compiler, and is arguably
  pointless (asl departs). Not recommended.
- **Option C (rejected) — a convsym `-filter` restricting to the agreed set.** Not viable:
  the divergent set is a large regex-inexpressible fraction, and it still wouldn't match
  asl's appendix (different symbols packed).

Under Option A the rest of Stage 1 proceeds: wire `emit_listing` to derive the symbol set
from the link table + drive convsym + assert-PRESENCE (ruling-independent infrastructure),
then capture sigil's own full-file goldens for all six ROMs.

## Remaining Stage-1 work (unchanged by the ruling except where noted)

- **S1.2 map-manifest growth** — NOT started this session. Independent of the appendix
  fork; the design's DECLARE half (object-code-bank region + section ordering) + COMPUTE
  half (resume points as link outputs) still stands.
- **S1.4 infrastructure** — listing derivation + convsym driver + assert-PRESENCE control
  (corrected to `de b2`). Common to both options; the golden it compares against is the
  ruling's output (sigil's own file under A, asl's under B).
- **Demo lockstep · Config-A/B · golden freeze** — all inherit the appendix ruling for
  their full-file bars.

## RESOLUTION — Option A ruled; S1.4 LANDED (the split-golden model)

The overseer ruled **Option A** (sigil-canonical appendix): the appendix consumers care
that names resolve to correct addresses (proven — 0 address mismatches), the `.emp`
names ARE the source names going forward, and byte-imitating asl's departing name set
is pointless. S1.4 is built to the split-golden bar:

- **Assembled prefix `[0, EndOfRom)` == asl** — the correctness anchor, asl-witnessed,
  UNCHANGED (PRIMARY header-neutral e5765873/dab4f06c). Proven by `native_rom_{plain,debug}`
  and re-asserted (header-neutral) inside the new full-file gate.
- **Full file == sigil-canonical golden** — assembled + sigil's OWN deb2 appendix, built
  by `native::build_native_full_file` driving the REAL `tools/convsym … -output deb2 -a`
  (build.sh flags verbatim) + `tools/fixheader` over sigil's `emit_listing` output.
  Deterministic. CRC-pinned (`native_full_rom.rs`), blob-frozen in the golden-freeze stage.

**Explicit CRCs (aeon `bcb8f64`, sigil `flip-stage1`):**

| shape | assembled (== asl, header-neutral PRIMARY) | full file (sigil-canonical) | appendix | listing syms / kept by convsym |
|---|---|---|---|---|
| plain | 0x5DB60 / e5765873 | **2198deb2 / 395374** | 0x2D0E (11534 B) | 1585 / 676 |
| debug | 0x5F65A / dab4f06c | **1d895fcb / 402696** | 0x2EAE (11950 B) | 1727 / 676 |

**Functional truth gates (condition 2), all green:**
- (a) determinism — double-build byte-identical (asserted).
- (b) presence — `de b2` (2-byte magic) at EndOfRom + appendix size band (0x2000..0x10000);
  a collapsed/empty listing HARD-fails (convsym aborts on 0 symbols; a 3-symbol appendix
  = 0x3B B is rejected by the band). The design's `de b2 04 02` 4-byte control was WRONG
  (bytes 3–4 are data-dependent) — corrected to 2 bytes.
- (c) convsym rc 0 + the load-bearing spot-check: EntryPoint/GameLoop/BusError/
  Ground_Move_Cap/Section_Init/BG_Init/AnimateSprite/TouchResponse/Z80_Sound_Start all
  resolve to their exact per-shape addresses through the REAL `convsym -output log`
  consumer (the same parser that packs the deb2).
- (d) t24 both directions — a doctored symbol address IS reflected by convsym (the
  spot-check isn't vacuous); a collapsed listing IS rejected by the size band.

**Convsym-drops-`$`-names fact (feeds ledger row i):** convsym's `as_lst` parser keeps
only 676 of 1585 labels — every `$`-mangled synthetic local (906) is dropped ($ is not a
legal as-listing symbol char). The kept 676 are every named global proc — a serviceable
load-bearing table for the golden. The listing-side demangler (ledger row i) would lift
this to ~1394; deferred post-flip per the ruling.

Gates: `crates/sigil-cli/tests/native_full_rom.rs` (native_full_sonic4_{plain,debug} +
deb2_appendix_negative_controls). Machinery: `native::build_native_rom_with_listing`,
`build_native_full_file`, `append_deb2_appendix`, `convsym_resolve`, `crc32`. Strict
**2914/0 (1 ignored)** — +3 over 2911; asl default path unmoved.
