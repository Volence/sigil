# 2026-07-29 — t38 step-0 recon: P4 player_sensors (the player-cluster close)

Porter: Opus subagent (Fable-dispatched). Brief: `2026-07-29-t38-p4-sensors-brief.md`.
Masters: sigil `2187b7d` / aeon `0037db4`. Branches `port-tranche38` both repos,
worktrees `.worktrees/port-tranche38`. Editor rsync done; baseline CRCs verified
(plain **4b66cace/421041**, debug **1c256b3b/429102**); baseline strict **2827/0
(1 ignored)** with `AEON_DIR` = the aeon worktree.

STOP-on-contradiction survey: **no contradiction that blocks step 1.** ONE brief
premise is FALSE and is reported below (§4, the `_pl_*` guard-survival premise) — it is
a retirement-checklist condition-not-met, not a port blocker.

## 1. Region / pin / shape derivation (the engine-block location)

`games/sonic4/player/player_sensors.asm` (493 L) is the FIRST include in
`gameEngineBlockIncludes` (`games/sonic4/main.asm:20-35`), invoked by `engine.inc`
via the macro. It is NOT in the object bank — no `code_addr` entry points (a called
primitive; census-correct). Emitted layout (from `s4.lst` / `s4.debug.lst`):

| shape | first byte (`Collision_ProbeDown`) | resume (`game_debug.asm` start) | size |
|---|---|---|---|
| plain | **$50A8** | **$55A4** | **$4FC** |
| debug | **$5E40** | **$633C** | **$4FC** |

- **SHAPE-VARYING BASE, shape-INVARIANT OWN LAYOUT** (the psg/fm class, NOT the
  object-bank shape-invariant class): debug shifts the base by +$D98 (upstream
  `__DEBUG__` engine growth before the engine block), but the region's own length is
  $4FC in BOTH shapes. So the gate arm needs a **per-shape org** (`ifdef __DEBUG__`),
  like the engine.inc engine-block ports (boot/vdp_init/dma_queue), NOT the object-bank
  ONE-org form.
- `game_debug.asm` emits **ZERO bytes** canonically (whole-file `ifdef SOUND_DEBUG_HOTKEYS`,
  off in every shipped build), so the resume address IS player_sensors' end.
- **New region** `PLAYER_SENSORS` (start `Collision_ProbeDown`, end `game_debug`'s first
  emitted symbol — practically the resume org). **New gate** `SIGIL_EMP_PLAYER_SENSORS`.
- **Gate arm** (games/sonic4/main.asm, replacing the bare line-21 include):
  ```
      ifndef SIGIL_EMP_PLAYER_SENSORS
          include "games/sonic4/player/player_sensors.asm"
      else
          ; sigil mixed build: the sensor primitives come from player_sensors.emp,
          ; pinned by the sigil map at the engine-block region start. Shape-VARYING
          ; base (upstream __DEBUG__ growth), shape-invariant own layout → per-shape
          ; resume org. Resume lands on game_debug.asm's start (ZERO canonical bytes).
        ifdef __DEBUG__
          org     $633C
        else
          org     $55A4
        endif
      endif
  ```
- **repin.toml**: add a `[[region]]` `player_sensors` (gate `SIGIL_EMP_PLAYER_SENSORS`,
  tests the new t38 test file) — this is a NEW region (wave-ripple item 4 repin.toml
  clause fires). The end anchor is `Debug_MusicToggle`? No — game_debug emits 0 bytes,
  so the region end anchor is the first symbol AFTER the engine block's game_debug
  (verify the concrete `end =` label at gate-build; the resume org is the ground truth).
- Whole-file gate (row-79 class, NOT the row-72 internal-gate): player_sensors DEFINES
  no zero-byte header that a surviving-AS file needs at assembly time; its only
  surviving-AS consumers CALL its labels (resolved via the sigil map/symbol pins), they
  do not expand its macros. So a plain `ifndef` whole-file arm suffices.

## 2. Symbol-resolution plan (the ladder, per §3-item-8 preference order)

External symbols player_sensors references and their .emp homes:

| symbol(s) | kind | home | plan |
|---|---|---|---|
| SST_x_pos/y_pos/status/layer/width_pixels/height_pixels/angle | Sst fields | `engine.objects.sst` | `use engine.objects.sst.{Sst}` → `Sst.field(a0)` idiom |
| SST_interact ($4E) | tail custom-window offset, NOT a Sst field | structs.asm | `interact_off()` comptime-fn + drift guard (collision.emp precedent, lines 24-28) — replicate locally (collision's is not pub) |
| ST_ON_OBJECT, ST_XFLIP | engine const | `engine.constants` (pub) | `use engine.constants.{ST_ON_OBJECT, ST_XFLIP}` |
| PLAYER_X_RADIUS | engine const | `engine.constants` (pub, =9) | `use engine.constants.{PLAYER_X_RADIUS}` (feeds `LEDGE_PROBE_REACH`) |
| SOLID_TOP/LRB/ALL (=1/2/3) | engine const, NOT yet in constants.emp | engine/constants.asm | **file-local mirror + drift guard** (1st-consumer rung; PSTATE_* precedent). NOT hoisted to engine.constants — same blast-radius deferral the overseer ruled for the constants hoist (a standalone constants.emp compile would need extern("SOLID_TOP")). |
| Player_Quadrant | RAM byte | link symbol | bare link (Player_JumpBuffer precedent) |
| Collision_GetType | proc, surviving-AS `engine/level/collision_lookup.asm` | cross-seam | **NEW `extern proc` boundary decl** `clobbers(d0-d3/a0)` (die-at-port when collision_lookup ports) — NEW twin-scaffolding row |
| SolidityTable, AngleTable, HeightMaps, HeightMapsRot | ROM data, collision_lookup.asm | cross-seam | bare link symbols (`lea (Table).l, a1`) |

Internal (defined by player_sensors, NOT called cross-seam — comment-only mentions
outside): `Collision_ProbeDown/Up/Right/Left`, `Player_SensorPair`,
`Player_SensorSurface`. Public (called cross-seam, must stay `pub`): `Player_SensorFloor`,
`Player_SensorCeiling`, `Player_SensorWallDir`, `Player_SensorWallAt`, `Player_AtLedgeEdge`.

## 3. The 4 sensor contracts vs the t35 caller decls (+ AtLedgeEdge vs t34)

The callers' standing `extern proc` decls (to be deleted at P4) declare:

| callee | caller decl `clobbers` | player_sensors.asm header `Clobbers` | match |
|---|---|---|---|
| Player_SensorFloor (ground:29 / air:20 / spindash:21) | `d0-d7/a1-a2` | `d0-d7, a1-a2` (L222) | **EQUAL** |
| Player_SensorCeiling (ground:30 / air:21) | `d0-d7/a1-a2` | "same as Floor" (L250) | **EQUAL** |
| Player_SensorWallDir (ground:31) | `d0-d6/a1` | `d0-d6, a1` (L360) | **EQUAL** |
| Player_SensorWallAt (air:22) | `d0-d6/a1` | shim onto WallDir (L360 covers) | **EQUAL** |
| Player_AtLedgeEdge (player_common:35, t34) | `d0-d5/a1-a2` | `d0-d5, a1-a2` (L419) | **EQUAL** |

All 5 declared contracts match the `.asm` headers exactly. **Honest-contract derivation
at step 1** must VERIFY the actual closure (local writes ∪ callee clobbers, incl.
Collision_GetType `d0-d3/a0` and the internal SensorPair/SensorSurface) — the def may
clobber LESS (safe) but if it clobbers MORE than the decl that is the t36 68k drift
class (adjudicated finding). No mismatch anticipated (headers are the `.asm` truth), but
the closure gate is the arbiter, not the header prose.

## 4. Per-field PlayerV guard survival — grep of the WHOLE AS tree (THE FINDING)

**Brief §3-item-3 premise ("sensors is expected to be the LAST AS reader of `_pl_*`")
is FALSE.** `player_sensors.asm` reads **NO `_pl_*` field** — it reads only standard
`SST_` fields (status/x_pos/y_pos/layer/width_pixels/height_pixels/interact/angle).
`grep -n _pl_ player_sensors.asm` → empty.

Per-field readers across ALL `games/**/*.asm` (excluding the player_common.asm equate
DEFINITIONS):

| `_pl_*` field | surviving-AS readers after P4 | guard dies at P4? |
|---|---|---|
| `_pl_gsp` | player_ground.asm, player_air.asm, player_spindash.asm (row-79 twins) | **NO** |
| `_pl_state` | player_ground.asm (twin); config/game.asm mention is a COMMENT only | **NO** |
| `_pl_move_lock` | player_ground.asm (twin) | **NO** |
| `_pl_spindash` | player_spindash.asm (twin) | **NO** |
| `_pl_stick_convex` | player_ground.asm (twin) | **NO** |

The `_pl_*` readers are the **row-79 gate-off body twins** (player_{ground,air,spindash}.asm
— 32/8/7 references) + player_common.asm's own gated Player_Init. Those twins retire at
**Spec 5**, not P4. player_sensors is NOT a `_pl_*` reader, so porting it eliminates ZERO
readers. **All 5 row-74 guards SURVIVE. Row 74's P4-close clause condition is NOT MET.**

## 5. Retirement-checklist condition survey (§3 — execution is step-2+ after the (a) countersign)

1. **Row 80 (7 extern-proc sensor decls DELETE)** — CONDITION HOLDS. player_sensors ports
   → ground/air/spindash `.emp` calls to Floor/Ceiling/WallDir/WallAt become
   module-to-module. Delete all 7 decls (ground:29-31, air:20-22, spindash:21). The
   contract-closure gate must stay complete (player_sensors.emp provides the real defs).
   Also edits those 3 `.emp` files (in scope per §3-item-1). EXECUTE at step 2+.
2. **Row-32 `Player_AtLedgeEdge` boundary decl DIES** (player_common.emp:35) — CONDITION
   HOLDS (same mechanism). EXECUTE at step 2+. This is the corpus's SOLE `extern proc`
   toward the t28 zero-`extern proc` headline — **but a NEW `extern proc` is BORN this
   tranche** (Collision_GetType, §2), so the headline moves sideways, not to zero: the net
   is 8 `extern proc` decls deleted, 1 created. Report at close.
3. **5 guarded PlayerV fields** — **CONDITION NOT MET** (§4). Report condition-not-met;
   do NOT retire any guard. Row 74 stays open (its true kill is Spec 5 / the twins).
4. **offsets-construct adoption** (Player_States table, player_common.emp:501) — CONDITION
   HOLDS: all 7 terms (PState_Ground/Roll/Spindash/Air/Jump/RollJump/AirBall) are real
   `.emp` defs since t35. Adopt the `offsets` spelling (byte-neutral; gates prove). The
   two hook tables (PState_EnterHooks:532 / ExitHooks:543) reference PHook_* — verify
   their `.emp`-def status at step 2 before extending adoption to them. EXECUTE at step 2+.
5. **Row 81 P4 arm** (state files' PPHYS/game-config mirrors) — player_sensors uses NONE
   of PPHYS_* (physics-table offsets). Row 81's kill is "a game-constants `.emp` module is
   born; or P4/Spec 5 whichever first". Whether the P4 CLOSE alone (no game-constants
   module born) fires the migration is ambiguous in the row text — survey at step 2 and
   report executed/condition-not-met. player_sensors' OWN game/engine-const mirrors
   (SOLID_*) are a fresh 1st-consumer mirror, not a row-81 item.

- OUT of scope (confirmed): the G9 d7 fix (ledgered), harness T1 states, any Z80 file.

## 6. Gate / test scaffolding plan (step 1)

- Per the multi-file-tranche registry this is a SINGLE-file tranche → one gate
  `SIGIL_EMP_PLAYER_SENSORS`, one region pin, one test file `test_p4_player_sensors_port`
  (mirror `test_p2_player_states_port`: windowed region byte gate both shapes +
  positive control `*_undoctored_compile_equals_the_reference_window` + t24 negative
  probe `*_doctored_reference_diverges`).
- Whole-ROM gate: the combined-link PcRel8 finisher **HAS LANDED** (only the aeon-tree
  `subcommands.rs` test is `#[ignore]`d now; mixed_tranche34/35 whole-ROM tests run
  un-ignored) → add `mixed_tranche38_{,debug_}rom_matches_assembled_reference` (both
  shapes) building the whole ROM with SIGIL_EMP_PLAYER_SENSORS set + the .emp placed at
  PLAYER_SENSORS. NOTE: player_sensors has heavy duplicate local labels (`.cell`/`.cl_air`
  stamped 4× by probeCore; `.keep`/`.done`/`.single`) — the PcRel8 fix must cover them;
  verify at gate-build, the windowed gate is the fallback if the whole-ROM combined link
  chokes on a residual case.
- Ambient for the tests: types + sst + constants + coords + player_common import items
  (same set as tranche35's `ambient()`), plus whatever SOLID_* / interact_off needs.
- Cross-seam pins already present: Player_SensorFloor/Ceiling/WallDir/WallAt +
  Player_AtLedgeEdge. NOTE surviving-AS consumer `test_player.asm:221` (`jsr
  Player_SensorFloor`) persists past P4 → the pub export + symbol pin stay live (not a
  blocker; a fact for the close packet).
</content>
</invoke>
