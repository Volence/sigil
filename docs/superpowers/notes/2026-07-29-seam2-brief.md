# 2026-07-29 — seam-2 brief: the BANKED/DATA side + the generator conversion (DESIGN GATE FIRST)

Status: **DISPATCH BRIEF** (overseer: Fable; porter: Opus subagent, direct-dispatch).
Seam-2 = the sound stack's banked `$8000`-window data + the build generators — the
last work before the Spec-5 flip. It inherits seam-1's emit-tool pattern and executes
the **Volence-ratified convert-vs-embed principle** as its classifying lens. Sigil
master = THIS brief's commit; aeon master **`6c311b5`**.

## 0. Bars

- **PRIMARY provenance: the assembled ROM `e5765873` (plain) / `dab4f06c` (debug)** —
  the game bytes; any movement = STOP + report. Artifact CRCs (full-file
  `22f69f77`/414414 · `d4e8d043`/422466; blob `c7534c84`/`fd2a845d`; syms `87b87b1b`)
  drift only where a ruled retirement explains them. Strict baseline **2880/0
  (1 ignored)**; builds need `SIGIL_EMIT=<sigil>/target/release/emit_sound_blob`.
- Branches `seam2-banked-data` BOTH repos, worktrees `.worktrees/seam2-banked-data`;
  full standard rules (editor rsync, one shape per invocation w/ SIGIL_EMIT,
  cd-every-call — never compound-cd between repos, explicit paths, no `git add -u`,
  failures-first --no-fail-fast, keep commits small).
- **STOP 1 = the step-0 DESIGN GATE** (the seam-1 precedent): recon + classification
  + design, committed, then STOP for endorsement. Then (a)/(b)/(c) with the same
  twins-present-first dual-proof discipline seam-1 set wherever anything retires.

## 1. Step-0 deliverables (the design gate's cargo)

1. **The data census + convert-vs-embed classification.** Every file the sound bank
   build consumes (the ~29 `data/sound/*` incl. the `phase 08000h` bank head, song
   streams, patch banks, pitch tables, the 18 `sfx/sfx_*` blob+patch files,
   `sfx_table`, the soundBankHead placement tables w/ their LOAD-BEARING
   bank-alignment/no-straddle asserts; plus the still-AS `dac_sample_tab.asm` /
   `seq_opcode_tab.asm` / `sound_tables_z80.asm` carriers and the 11 equ-carrier
   LUTs seam-1 left) — classified per the ratified test:
   - **human-authored SEMANTIC → readable typed `.emp` data** (diffable, label-
     validated, typed where the newtype exists);
   - **tool-GENERATED → the generator emits sigil-consumable output, deterministic
     from tracked inputs** (the generator conversion, below);
   - **opaque BULK → `embed()`** (the t41 repo-root machinery).
   Name the classification PER FILE with the reason; borderline cases argued both
   ways for the gate.
2. **The generator conversion design.** The build's prebuild generators (survey
   build.sh + tools/: `ojz_entity_gen.py` and the sound-side generators) emit
   AS-syntax output today; design their sigil-consumable emission (the seam-1
   emit-tool architecture: deterministic artifacts + generated syms contracts where
   AS consumers survive) AND settle the open reproducibility ledger row (outputs
   deterministic from tracked inputs; the gitignored-input fallback incident is the
   cautionary case). Note: `ojz_entity_gen` feeds GAME data (not sound) — rule
   whether it rides seam-2 or the flip, with the reason.
3. **The link/emit mechanics at scale-2**, incl. the handoff's named complication
   (`db` can't carry a link symbol — the scale-2 class) and the bank-alignment/
   no-straddle assert story (those asserts are load-bearing; where do they live
   post-conversion — `ensure`s in the .emp data, the emitter, or both?).
4. **The retirement set**: which `.asm` data carriers die at seam-2 vs the flip;
   every kill-list row touched gets its planned disposition; the identity bar per
   retirement (the dual-proof discipline).
5. **Sequencing to the flip**: what remains for Spec-5 after seam-2 lands (expected:
   main/config + the 68k gate-off twins + sound_api's deferred rows + asl itself);
   confirm or correct.

## 2. After endorsement

Execution per the endorsed design with the seam-1 checkpoint discipline: every
retirement proven in the dual state first; the deletion commits standalone and
plain-spoken; kill/ledger rows same-commit; the loop + dry panel (C3 heavy — bank
asserts are hardware-adjacent) → (b); gate (c)/merge mine. Close packet: the census
classification table, the corrections list, THE FLIP'S FINAL INPUT SET.
