# 2026-07-29 — t30 step-0 design (G2: the effect/child-lifecycle trio)

Porter: Opus subagent (Fable-dispatched). Brief:
`2026-07-29-t30-g2-effect-objects-brief.md`. Bars re-verified at dispatch:
plain c51342d0/421041, debug 992d9e7d/429102 (both built from the worktree);
strict baseline 2691/0 (1 ignored). Masters aeon `c6a89a5` / sigil `4faba19`.

## Discrepancies caught at step 0 (on the record)

1. **Brief §0 says sigil master `e17b403`; the real master is `4faba19`** (the
   dispatch message's value). `4faba19` is the t30-brief commit itself. Branched
   port-tranche30 off `4faba19`. Flag only — no action.
2. **The G2 trio is NOT contiguous in the object bank.** `test_parent.asm` sits
   BETWEEN `test_emitter` (lane A) and `test_stress_emitter` (lane B). Bank order
   (main.asm:99-103): test_emitter, **test_parent**, test_stress_emitter,
   test_churn, path_swap. So lane A's org-resume lands on `TestParent` (not on a
   G2 file), and test_parent stays an AS include between the two gate blocks.
3. **row-63 hoist does NOT trip.** The brief flags "if any G2 file consumes
   vram_bytes → 2nd consumer → hoist." NONE of the three consume `vram_bytes`;
   all three consume `vram_art` (already shared in objdef.emp). test_animated
   remains the sole `vram_bytes` consumer → row 63 stays open, correctly.

## Region derivation (both shapes — bank shape-invariant, verified in BOTH listings)

TestEmitter/TestParent/TestStressEmitter/TestChurnObj/ObjDef_PathSwap land at
IDENTICAL addresses in s4.lst and s4.debug.lst → the object bank did not slide
(the $8000 bar did not trip). ONE org literal per arm serves both shapes.

| Lane | region | base (both) | end anchor | len | org resume |
|---|---|---|---|---|---|
| A test_emitter | TestEmitter | `$10FDC` | TestParent | `$E4` | `org $110C0` |
| B test_stress_emitter | TestStressEmitter | `$1115C` | TestChurnObj | `$5A` | `org $111B6` |
| C test_churn | TestChurnObj | `$111B6` | ObjDef_PathSwap | `$78` | `org $1122E` |

Content bytes track cross-seam operands per shape (CreateEffect_Normal/AllocDynamic/
DeleteObject/Draw_Sprite/PopulateSpawnedPieceCount move between shapes) → the
COMPILE-TWICE class (t29 clarification), NOT identical-bytes-both-shapes.

## Cross-seam symbols (VMAs both shapes — all resolve module-to-module; ZERO externs)

| symbol | plain | debug | source |
|---|---|---|---|
| Draw_Sprite | $2B30 | $310E | sprites.emp (pin) |
| CreateEffect_Normal | $3F72 | $4C4A | children.emp (NEW pin) |
| PopulateSpawnedPieceCount | $3D38 | $4906 | children.emp (= CHILDREN base pin) |
| AllocDynamic | $288A | $2A20 | core.emp (pin) |
| DeleteObject | $2908 | $2A9E | core.emp (pin) |
| Frame_Counter | $FFFF8002 | $FFFF8002 | RAM (pin) |
| TestParticle | $10F8A | $10F8A | test_particle (= TEST_PARTICLE base pin) |
| Map_TestObj | $256E0 | $25748 | data (pin) |
| ObjCodeBase | $10000 | $10000 | const |

All calls (jsr → abs.w in both shapes; targets < $8000) are byte-identical
mnemonic; only operand address differs per shape.

## Demanded features / choices (ship at step 1 per the demanded-features law)

- **`vram_art` ADOPTED** (`use engine.objects.objdef.{vram_art}`), one-arg form
  `vram_art(VRAM_TEST_OBJ)` (pal=pri=0 default) matching the test_animated
  sibling — byte-identical to the AS `vram_art(VRAM_TEST_OBJ,0,0)`.
- **`VRAM_TEST_OBJ: VramTile = $03E0` file-local mirror + drift guard** in each of
  the three files (row-62/54 game-config-mirror class; `VRAM_TEST_OBJ` survives
  AS-side in config/constants.asm, so `ensure(extern(...))` resolves). Three
  mirrors because no game-constants .emp module exists yet (hoist candidate,
  ledgered).
- **`vars` SST-overlay per file** — `vars TEmitterV: Sst.sst_custom { timer: u16 }`
  etc. Its always-on `[overlay.window-overflow]` check subsumes the AS
  `objvarsCheck`. **KEY DIFFERENCE from t29's DplcV:** the field-offset equates
  (`_emitter_timer`/`_stress_timer`/`_churn_life`) are defined ONLY in the
  gated-out .asm file — NO surviving AS truth (unlike DplcV, whose `_dplc_ptr`/
  `_art_base` survive in test_player.asm). So the overlay carries ZERO extern
  drift guards; its offset ($2E) is guaranteed by sst.emp's `SST_sst_custom`
  ensure. The overlay is self-contained twin scaffolding (gate-off body row).
- **Honest exhaustive `clobbers()`** (register-contract convention). Emitter +
  stress: AS header (`d0-d3/a1-a2`) is accurate (CreateEffect_Normal(d0-d2/a1-a2)
  ∪ Draw_Sprite(d0-d3/a1)). **test_churn: AS header (`d0-d1/a1-a2`) is WRONG** —
  a step-3(b) contract finding: Main tail-calls Draw_Sprite (d0-d3/a1), so Main
  clobbers `d0-d3/a1` (AS under-declares d2-d3, over-declares a2 — Main never
  writes a2); init (falls into Main) whole set = `d0-d3/a1-a2` (a2 via its
  `movea.l a0,a2`). Declare the honest license; note the callee union in a
  present-tense comment (test_particle precedent).
- **`objroutine()` INLINED** as `X - ObjCodeBase` (item-13 inline ruling; C1
  reversal is corpus-wide). Six new `- ObjCodeBase` sites (2/file): a code_addr
  store + a descriptor `dc.w` in emitter & stress; two code_addr stores in churn.
  item-13 objroutine demand 3 → 9.
- **Descriptor blob** — the emitter/stress `.particle_desc` is a proc-local label
  + `dc.w TestParticle - ObjCodeBase / dc.b 0,0 / dc.w 0` after the tail `jbra`,
  reached by `lea .particle_desc(pc), a1` (seq_opcode_tab raw-dc-in-proc + boot
  `lea Sym(pc)` precedents; TestParticle is a bare cross-module link symbol).

## $FF sentinel: N/A here (no prev_anim/prev_frame writes in these files).

## Harness plan (t29 STANDING PATTERN)

- Per-file gates `SIGIL_EMP_TEST_EMITTER` / `_TEST_STRESS_EMITTER` / `_TEST_CHURN`.
- ONE test file `test_g2_objects_port.rs`; ONE mixed fn `build_mixed_tranche30_rom`
  + `assemble_mixed_tranche30_as_side`.
- 3 region pins (repin.toml) + 1 new symbol pin CreateEffect_Normal; regenerate.
- main.asm: three `ifndef/include/else/org/endif` arms (lane A around test_parent).
- Ambient per file: sst (bundles types) + constants + objdef; cross-seam synthetic
  AS labels for the 6 callees + TestParticle + Map_TestObj + ObjCodeBase/VRAM equs.

## Kill rows (same-commit): gate-off body twins (row-60 class) for the three .asm
files + their org arms; three VRAM_TEST_OBJ mirrors (row-62 class); three `vars`
overlay twins (row-61 class, self-contained variant). row 63 NOT tripped.
