# 2026-07-29 — t36 step-1 status + transcription groundwork

Status: **CHECKPOINT (partial).** The DEMANDED FEATURE is COMPLETE + proven +
committed; the whole strict paired gate holds. The faithful `.emp`
transcription + the dual-shape windowed oracle (the mechanical bulk of step 1)
is scoped with all seam data extracted below, NOT yet transcribed.

Branch tips: sigil `684b567` (feature) / aeon `84d23c8` (unchanged — no aeon
edits yet; the `.asm` stays canonical, byte movement ZERO held).

## DONE — the `ex (sp),hl` trampoline feature (all three rulings)

Commit `684b567`. Verified: `sigil-frontend-emp` + `sigil-isa` **1804/0**; the
FULL strict paired gate `SIGIL_STRICT_GATE=1 AEON_DIR=<aeon-wt>` **2807/0
(1 ignored)** = the 2803 baseline + 4 new trampoline tests, zero regressions.

- **Ruling (i):** `CodeOperand::Z80IndSp` (distinct `(sp)` indirect) — value.rs;
  produced by the asm mapper (eval/asm.rs `"sp" =>`); lowers to the golden ISA
  `Pair(Sp)` arm → `$E3` (code.rs ×2, byte-neutral). `sp_hazard` now MATCHES
  `ex (sp),hl` via `Z80IndSp` and returns `true` — the Z80IndHl stub REPLACED,
  not kept.
- **Ruling (iii) — the mandatory soundness tightening:** `verify_z80_preserved`
  now takes `invariant_regs`; past `bailed_reached_return`, a unit is credited
  Verified ONLY if `!ever_clobbered[i] && invariant_units.contains(i)` — all
  others loudly `Unverifiable`. Closes the silent-Verified hole (a
  locally-untouched NON-invariant unit a dispatch target clobbers). Applied
  uniformly to ALL bail reasons (also closes the `ld sp` optimism). Threaded
  from proc.rs.
- **Tests (z80_contracts.rs):** `z80_ex_sp_hl_trampoline_bails_declared_preserve`
  (the operand form lands + bails), `z80_trampoline_untouched_noninvariant_preserve_is_unverifiable`
  (the tightening RED — **empirically proven RED**: reverting the verdict block
  makes `de` false-pass with no diagnostic; restored → GREEN),
  `z80_trampoline_module_invariant_survives_bail` (GREEN control: `ix` credited),
  `z80_trampoline_locally_written_invariant_still_fires` (`pop ix` still fails).
- **Spelling (ruling ii, OPTION A):** at transcription, the `.coord` site is
  `push hl` … `ex (sp), hl` / `ret` with the forced-spelling site comment
  `// [dispatch.trampoline: SeqOpcodeTable — module-internal targets; credit past bail = module invariant only]`.
  Option B (`as dispatch(...)` + table-membership closure) LEDGERED as the
  typed-dispatch grammar ask.

## REMAINING — the faithful `.emp` + dual-shape oracle (the mechanical port)

`sound_sequencer.emp` (from `sound_sequencer.asm`, 2091 L / 51 labels) +
`sound_sequencer_port.rs` (the psg-oracle template, BOTH shapes). Structural
facts fixed at step 0:
- **Windows:** BOTH shapes vma-base **$0565** (shape-INVARIANT base); plain
  window **$0565–$0CD7 = 1906 B** (no `DEBUG`), debug **$0565–$0D55 = 2032 B**
  (`DEBUG=1`, +$7E). The oracle `compile_emp(Plain)` defines nothing extra;
  `compile_emp(Debug)` passes `DEBUG=1` so the 16 `if DEBUG == 1` bodies emit;
  the AS twin defines `__DEBUG__` for the debug shape. SAME base for both (unlike
  psg) — the `link_seam` needs NO per-shape base for the internal targets, only
  the external callee addresses that sit AFTER the sequencer shift by +$7E in
  debug (Sfx_Frame and anything downstream; the resident callees BEFORE it —
  Fm_*/Psg_*/Snd_ChanClass at $08xx/$12xx/$16xx — are shape-invariant since the
  sequencer is the growth source).
- **First Z80 `if DEBUG == 1` bodies:** the 16 `ifdef __DEBUG__` sites
  (Seq_Trace body @ :1920 + 15 `call Seq_Trace` sites). The `.emp` carries
  `if DEBUG == 1 { … }`; the twin keeps `ifdef __DEBUG__` (shape-divergent
  relaxation class).
- **Trust surface → 0:** `Mod_ReArm`/`Mod_Advance` become CHECKED `pub proc`
  here (psg's last two `extern proc` decls convert to verification).
- **out(carry:) cross-proc LIVE:** the 4 `ret c` sites after
  `PsgVolEnv_Resolve` (:674/:704) / `FmVolEnv_Resolve` (:744/:781) exercise the
  `[call.flag-result-unused]` credit end-to-end (both procs are extern-declared
  from sound_psg.emp).

### Seam values (extracted from s4.lst — ready for const_seam/link_seam)

SeqChannel fields (hex offsets): sc_stream_ptr $00, sc_mod_ptr $02, sc_dur_count
$04, sc_dur_default $05, sc_patch $06, sc_last_patch $07, sc_volume $08, sc_note
$09, sc_flags $0A, sc_route $0B, sc_loop_ptr $0C, sc_repeat_ptr $0E,
sc_repeat_count $10, sc_tempo_mod $11, sc_tempo_accum $12, sc_pt_count $13,
sc_pt_cursor $14, sc_points $15, sc_transpose $1A, sc_pan $1B, sc_opbias $1C,
sc_porta_accum $20, sc_porta_incr $22, sc_last_pan $24, sc_fill_master $25,
sc_fill_count $26, sc_env/sc_psgenv $27, sc_env_cur/sc_psgenv_cur $28,
sc_env_out/sc_psgenv_out $29, sc_mod_ctrl $2A, sc_mod_wait $2B, sc_mod_speed $2C,
sc_mod_delta $2D, sc_mod_steps $2E, sc_mod_speed_raw $2F, sc_mod_step_raw $30,
sc_mod_wait_raw $31, sc_mod_delta_raw $32, sc_mod_accum $33, sc_base_freq $35,
sc_last_freq $37, sc_noise_mode $39, sc_detune $3A, sc_macro_active/sc_pad $3B,
sx_extend $42, sx_gain $40, sx_priority $39. SeqChannel_len (grep separately).

Flag bits: SCF_ACTIVE_B 0, SCF_KEYED_B 1, SCF_IS_FM_B 2, SCF_IS_PSG_B 3,
SCF_IS_DAC_B 4, SCF_REKEY_B 5, SCF_SFX_OVERRIDE 0x40 (SCF_SFX_OVERRIDE_B =
grep; bit 6), SCF_PITCH_CHROMATIC_B (grep).

MEV: VOL $E0, PATCH $E1, DAC $E2, NOTE_DUR $E3, PAN $E4, REPEAT_START $E5,
REPEAT_END $E6, NOTE_RAW $E7, PITCHENV $E8, OPBIAS $E9, REGDELTA $EA, PSGENV $EB,
MODSET $EC, NOTEFILL $ED, LOOP_POINT $EE, JUMP $EF, SPINREV $F0, PSGNOISE $F2,
TEMPO $F3, LFO $F4, PORTA $F5, DETUNE $F6, FMENV $F7, REGWRITE $F8, MACRO $F9,
END $FF; REST $80, NOTE_BASE $81, NOTE_MAX $DF.

Ctl/tag: PsgVolEnvCtl_Loop/Sustain/Rest + FmVolEnvCtl_* (grep — $80/$81/$83),
TAG_MAC_NEXT $E0/REG $E1/LOOP $E2/END $E3. FNUM_LO $284, FNUM_HI $508.

SEQEV (debug): NOTEON 1, NOTEOFF 2, VOL 3, PATCH 4, DAC 5, LOOP 6, JUMP 7,
END 8, RPT_START 9, RPT_END $A.

SND RAM/reg: SND_STAT_TICK $1F13, SND_SEQ_ACTIVE $1A04, SND_SEQ_CHCOUNT $1A01,
SND_SEQ_CHANNELS $1A08, SND_SEQ_BADOP $1A05, SND_SEQ_TRACE $1CAC,
SND_SEQ_TRACE_WR $1A06, SND_SEQ_TRACE_LEN $20, SND_TEMPO_CUR $1CD0,
SND_TEMPO_TARGET $1CD1, SND_TEMPO_BASE $1CD2, SND_MASTER_FADE $1CCC,
SND_FADE_TARGET $1CCD, SND_FADE_DELAY_CTR $1CCE, SND_FADE_DIRTY $1CCF,
SND_FADE_DELAY 1, SND_FADE_STEP 2, Snd_SongBase $1CA1, Snd_SpindashRev $1CA5,
SND_Z80_PSG $7F11, SND_Z80_YM_A0 $4000, SND_Z80_YM_A1 $4001, SND_PSG_SILENCE_T3
$DF, SND_FM_TL_MAX $7F, SND_REG_LFO $22, SND_REG_TIMER_A_HI $24,
SND_REG_TIMER_CTRL $27, SND_REG_KEY_ONOFF $28, SND_REG_DAC_DATA $2A,
SND_REG_DAC_ENABLE $2B, CHROUTE_PSGN $09. PSGVOLENV_COUNT/FMVOLENV_COUNT (grep;
psg oracle used $0B/3).

Extern proc addresses (link_seam; from s4.lst — the $0565-invariant procs are
shape-invariant, Sfx_Frame + downstream shift +$7E in debug): grep each label's
emission addr in BOTH listings at transcription time. Externs to declare with
their `preserves` sets: Tempo_Ramp, Fade_Ramp (defined HERE — not extern),
Sfx_Frame, Fm_FnumApplyDelta, Fm_WriteFreq, Fm_NoteFromTable, Fm_NoteOn,
Fm_NoteOnFreq, Fm_NoteOff, Fm_SetVolume, Fm_SetPan, Fm_PatchPtr, Fm_PatchLoad,
Fm_RegDelta, Fm_YmWrite, Fm_ReparkDac, Psg_SetVolume, Psg_NoteOn, Psg_NoteOff,
Psg_Noise, Psg_ApplyMod, Psg_EmitDivisor, Psg_SilenceAll, PsgVolEnv_Resolve,
FmVolEnv_Resolve, Snd_ChanClass, Snd_StartSample, Snd_DacLookup, SeqOpcodeTable
(banked link data). NOTE: Tempo_Ramp/Fade_Ramp/Tempo_WriteChanMods/ModUpdate/
Porta_Apply/PsgEnvUpdate/FmEnvUpdate/Mod_*/MacroTick/Seq_* are DEFINED here.

### Contract census (step-0, for the header set)
51 labels; 24 Clobbers / 19 Preserves / 2 Out(carry) headers; 13 `Preserves ix`
procs (the invariant(ix) candidate set — verify per-proc, esp. the
`push ix / pop de|hl` move-idiom sites at Seq_Op_PitchEnv :1455, Seq_Op_OpBias
:1536, ModUpdate.multipoint :594). Header-accuracy scoreboard inherits (t33: 6/36
over-claims, all safe-direction).
