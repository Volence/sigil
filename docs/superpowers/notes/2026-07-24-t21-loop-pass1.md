# t21 loop pass 1 — filled checklists (3(a) / 3(b) / 4 / 5), close-packet input

All edits this pass are ZERO-BYTE (dual rebuild reproduced `13dfdfc5`/421159 ·
`dbba0fc9`/429204 exactly); paired strict **2553/0** (+1 = probe P9).
Overseer rulings R1-R6 (checkpoint-(a) countersign) folded in where cited.

## Step 3(a) — language/format asks (every line run)

- **Ceremony scan:** BuildStaticDMA = 35 lines for seven records (7×5-line
  near-identical blocks) → DEFERRED on the t20 size bar (5-line bodies, 4 of
  5 lines are the varying terms; a template saves only the lea/jbsr framing
  and threads a `.build_entry` Label through every call — F2 splice-hygiene).
  Ledger row with demand data. Nothing else scores.
- **Comment-as-compensation:** the 3 mem-to-mem pinned-width what-comments
  (vblank) = `[lower.abs-sym-operand]` compensation; the 6 `SRC_*` equ site
  comments (buffers) = row-1004 comptime/link-boundary compensation. Both
  ledgered as demand increments (one combined row).
- **Escape-hatch census:** extern decl ×1 (Sound_DebugMirror — boundary
  class, kill row 42); equ escapes ×7 by shape (SND sum ×1 t19-row-1004
  standing + SRC_* ×6); ZERO call-expr escapes; drift ensures = standard twin
  tax. Recurring-shape rows updated.
- **Domain-type scan:** logged at step-2 item 6 (VramAddr-class A4-i;
  dbf counters; VInt_Ptr typed-cell → demand row this pass). No new
  newtype candidates beyond those.
- **Noticing:** ONE process proposal — the **off-canonical twin-parity gate**
  (vblank_port's sound-off/mirror arms: full AS-side assembly at the same
  defines as the oracle, region self-located by its own labels, cross-seam
  symbols fed from the same module's label table). Proposed as the standing
  gate-artifact class for comptime-shaped files whose arms have no reference
  ROM.

## Step 3(b) — reads-wrong (every line run)

- **Comment-claim audit:** (1) Enqueue header "Z80 already stopped" was
  STALE for the sound build (the MegaPCM-2 flag bracket leaves the Z80
  free) → FIXED both twins (accurate per-build wording). (2) ram.asm:334
  "set =1 by VInt_Lag" was FALSE — the store is tile_cache's trailing
  frame-gate (`tile_cache.emp:773`, Frame_Counter-delta detect; overseer
  R3 + row 1067 confirmed) → FIXED (zero-byte, explicit-path commit).
  (3) VSync_Wait's "~34-40 cy" mask-cost figure: static tables suggest
  ~46-50 cy for the 3-instruction bracket (14+16+16..20) — FLAGGED to lens
  C1 for the panel (the t20 "~64 cyc/entry" precedent); not hand-fixed on
  uncertain tables. (4) "after Init_DMA_Queue" (BuildStaticDMA) VERIFIED
  (boot.asm:149→155). (5) VInt_Lag torn-drain block's mechanism VERIFIED
  against plane_buffer.emp (drains on Plane_Buffer_Ptr!=0, no complete
  guard). (6) the new twin "-42 bytes" sound-off comment VERIFIED
  (2×34-byte bracket arms vs 18+8 plain fence).
- **Contract audit:** clobber unions re-derived (overseer countersigned);
  preserves() claims are all save/restore-ENFORCED (R4 bar): VBlank_Handler
  movem round-trip, VSync_Wait sr push/pop. Parallax_Active_Config's
  prose-only Z-fact PROMOTED to contract (below).
- **Name audit:** no renames (`.no_pal`/`.skip_pal3` adjacency kept per
  twin; `.hs_cell`/`.no_hscroll` say what they do).
- **Magic-number audit:** `#$2700` carries its mask comment (house
  spelling); `79-1` reads against the header's 80-entry chain description
  and the separate terminating entry — checked, no change; `640`/`112`/
  `896` are the twins' documented byte counts (H3 row owns the 640
  question). No naked literals without site meaning.
- **Cold-reader test:** one frame traced VBlank_Handler → VInt_Level →
  Enqueue → drains → controllers on headers alone; the one stop was the
  stale Z80 claim (fixed above).
- **Codename audit:** twin's `b96c861` (VSync_Wait comment) scrubbed to the
  behavioral reference ("the torn-drain hazard VInt_Lag's header
  describes") — zero-byte, matches the .emp wording.

## Step 3(c) — mirrors/gaps

Kill row 43 born (sr_masked, same commit as the build); ledger rows: PlaneMapToVRAM
keep+kill-condition (R2), demand-increment row (mem-to-mem ×3 · SRC_* ×6 ·
module-level decl-gating · typed RAM-cell decl), BuildStaticDMA deferral row;
SR-bracket row RESOLVED-PARTIAL.

## Step 4 — construct pass (verbs adjudicated)

- **BUILD: `sr_masked(code)`** in new pure-comptime `engine.irq` (R6 item 1).
  The vblank port added the cleanest PAIRED site; probe P9
  (`sr_masked_code_argument_matches_as`) proved the Code-typed fn argument
  as-found; census: adoptable-now = VSync_Wait (ADOPTED, byte-neutral,
  gates re-green) + Sound_PostByte + Sound_PlayMusic (paired label-free —
  step-6 sweep candidates). Unpaired/looped/long-span sites (dma_queue
  3-exit, Sound_Init re-mask loop, DrainSfxRing internal labels,
  section/bg long spans) stay inline BY DESIGN — module header names them.
  Kill row 43 same-commit. t19/t20 ledger row updated RESOLVED-PARTIAL.
- **BUILD: `out(d0, zero: inert)` on Parallax_Active_Config** (R5): `zero`
  is in VALID_FLAGS and out_verify accepts the move.l-credited Z on both
  paths; demanding sites cited (Vscroll_Write + Enqueue_Dirty_Buffers
  beq-after-jbsr). Zero-byte contract tightening on a shipped proc; both
  .emp consumers verified flag-consuming; d0 moved from clobbers() to
  out() (the `[proc.out-clobbers-overlap]` rule — a returned result is not
  scratch).
- **DEFER: BuildStaticDMA entry-block template** (size bar, ledgered).
- **LOG: VInt_Ptr typed RAM cell** (R6 item 2) — VBlankHandler type exists,
  the one read site is blessed; cell typing needs a typed extern-data decl
  class (no grammar) and the stores are .asm-side → documentary; demand
  row.
- **ENUMERATE: queue_static_dma back-prop** (R6 item 3) — corpus census:
  zero other enqueue-a-prebuilt-entry sites .emp-side or .asm-side
  (buffers.asm was the macro's only consumer) → NOT-AN-INSTANCE anywhere;
  no retrofit sites.
- **Delete verb:** PlaneMapToVRAM ruled KEEP (R2, forward-scaffolding;
  site comments + ledger row; Volence-override flag open).

## Step 5 — FULL interrogation (per hot proc; heat: VInt_Level every normal
frame, VInt_Lag lag frames, Enqueue_Dirty_Buffers per-frame from both,
VSync_Wait per-frame spin; Init_SpriteTable/BuildStaticDMA/PlaneMapToVRAM
boot/init-cold)

- **Invariant ladder:** VInt_Level/VInt_Lag are jbsr chains — nothing sits
  below its scope (the DMA_Budget reload is frame-scope at frame position).
  Enqueue_Dirty_Buffers: each queue_static_dma expansion re-reads/rewrites
  DMA_Critical_Slot (7 sites worst-case) — threading the slot cursor in a1
  across the blocks would save ~8-16 cy per enqueue after the first
  (≈50-100 cy/f worst, typical 1-2 enqueues ≈ 0-30 cy) AND break the
  template's self-contained carry protocol on every bcs path →
  **LOG-not-cut** (far under the ~1k bar; correctness surface large).
  VSync_Wait: nothing loop-invariant (the spin is the point).
  Init/Build/PlaneMap: cold, n/a.
- **Counter/cache audit:** Palette_Dirty bits + Sprite_Table_Dirty —
  clear-only-on-carry-clear on EVERY enqueue path ✓ (the retry protocol;
  producers set them elsewhere). VBlank_Flag/Ready writers/readers balanced
  across VSync_Wait/VBlank_Handler/VInt_* ✓ (the sr_masked pair is the
  atomicity guard). Frame_Counter: incremented on BOTH handler paths ✓
  (tile_cache's trailing-lag detect depends on exactly this — verified at
  the R3 fix). DMA_Budget_Remaining: reloaded before Important/Deferrable ✓,
  charged in the drain ✓ (t20 rows own the overshoot note).
  Lag_Frame_Count: debug-only writer VInt_Lag, emulator reader (the
  DMA_Overflow_Count verb-(d) shape) ✓.
- **Guard-coverage audit:** all 7 enqueue paths carry bcs drop-handling ✓;
  the HScroll length keys off Parallax_Active_Config (the mode-tear guard —
  now contract-backed by out(zero:)) ✓; VInt_Lag's plane-drain skip IS the
  torn-drain guard (comment block carried verbatim) ✓; no unguarded
  emission path found.
- **Hardware cross-check (C3 territory — panel lens mandatory):**
  flag-bracket ORDER verified static — raise precedes ANY VDP work
  (Flush_VDP_Shadow is the first post-bracket call in both handlers), clear
  follows the LAST DMA (after Process_DMA_Deferrable / after Vscroll_Write
  on the lag path) ✓; §4.6 VSRAM-after-HScroll ordering carried ✓ (comment
  at both sites); IRQ6 cannot nest inside VBlank_Handler (SR level from the
  vector entry) — VSync_Wait's mask covers the main-loop side ✓; the rte
  restores the stacked SR/CCR ✓ (t20 C2's carry-pin analysis class). NAMED
  for panel C3: the budget-model/window coupling stays the t20 ledgered
  fence (carried honest comments, no fix); the VSync_Wait mask-latency
  figure (C1 flag above).
- **Silent-tradeoff comments:** drop-retry (carried, both twins); fixed
  640-byte SAT DMA (H3 trigger-gated ledger row owns it); VInt_Lag's
  deliberate plane-drain skip (carried); the sound-off ifdef-width twin
  arm carries its reach reasoning. No uncommented compromise found.
- **Threshold ruling: NO CUT** — largest identified item (slot-cursor
  threading) ≈ 50-100 cy/f worst-case, an order of magnitude under the
  ~1k bar; the handler shells are call sequences with no algorithmic
  content of their own (the t20 root-cause note: the 72-cy VDP send floor
  lives in dma_queue, not here). No live A/B needed for any cut decision.
- **Type-layer rider:** no register reshuffles taken → no blessings moved;
  the one new blessing (`jsr (a0) as VBlankHandler`) sits on the producing
  dispatch.

## Named probe list for the overseer (oracle is overseer-only)

No cut decision needs live numbers (no candidate within reach of the bar) —
the list is INFORMATIONAL baseline only, run-if-cheap:
- **P-O1 (baseline, optional):** profiler self-times over a 600-frame OJZ
  scroll soak for `VInt_Level`, `VInt_Lag`, `Enqueue_Dirty_Buffers`,
  `VSync_Wait` — banks the per-frame VBlank-window numbers for the
  post-conversion budget parcel (t20 C3 row input).
- **P-O2 (behavioral spot-check, optional):** one lag-frame episode
  (Debug_Scene_Freeze technique) confirming Lag_Frame_Count increments and
  the plane buffer defers — shipped behavior, byte-identical port, so this
  is regression comfort, not a gate.

## Pass-1 outcome

Pass 1 was NON-EMPTY (3(b) comment fixes ×3 + codename scrub; step-4 builds
×2 + adoption; ledger/kill rows). Per the loop shape, a fresh full 3→4→5
pass must now come up empty before the dry claim + panel (C3 active).
Checkpoint (b) report goes to the overseer first.
