# 2026-07-30 — FLIP STAGE 2 · S1.2 the computed-resume mechanism — PROVEN + an approach simplification

Status: **the Stage-1-companion linchpin (computed resume orgs) is EMPIRICALLY
PROVEN, and it turns out to need ZERO aeon edits.** Driver-side address-order
`Chained` placement reproduces the canonical asl ROM byte-for-byte in BOTH shapes
with every resume-org VALUE discarded — so the ~30 hardcoded sonic4 resume orgs are
inert link outputs, and the demo + Config-A/B native drivers (blocked in Stage 1 on
exactly these literals) are unblocked WITHOUT the ~30-site aeon parameterization the
brief assumed. This is a materially simpler, lower-risk Stage-1 shape. Surfaced for
the overseer's ratification before the six-proof preamble is built on it (per the
valve's STOP-on-design-fork). No aeon edit made; asl default path trivially
byte-identical. Sigil branch `flip-stage2`; aeon read-only `bcb8f64`.

## Baseline verified (my environment, both repos at flip-stage2 fork of master)

- asl default path: `./build.sh` → `s4.bin` **eff2396f / 413577**;
  `DEBUG=1 ./build.sh` → `s4.debug.bin` **1e9097bc / 421579** — both == goldens.
- native gates (SIGIL_STRICT_GATE): `native_rom` 2/2, `native_full_rom` 3/3.
- the six frozen goldens re-CRC'd, all match the brief: s4 eff2396f/413577 ·
  s4.debug 1e9097bc/421579 · demo 18c64002/90776 · demo.debug b0475a59/91584 ·
  config_a b4a6756d/421898 · config_b 92776720/304961.

## The mechanism — Chained placement computes every resume point

The linker already supports two placements (`sigil_ir::SectionPlacement`): `Pinned`
(base = the section's baked `org`/lma) and `Chained` (base derived at link by packing
after the prior section in its `group`, `relax.rs::place_pass`). Stage 1's native
driver PINS everything: the AS residual at its `org` addresses (incl. the gate
else-arm resume orgs) and each `.emp` at its `pins` base. The resume orgs are all
canonical-sonic4 literals (`engine.inc`/`main.asm`, each with the in-tree NOTE "never
set for other games") — which is why demo/Config-A/B could not build natively
(`2026-07-30-flip-stage1-demo-config-native-blocked.md`).

**The proof (`crates/sigil-cli/tests/native_chained_resume.rs`, 2/2 green):** take the
combined (AS-residual ∪ natively-placed `.emp`) sections, sort the ROM (image-bearing)
sections by address, and re-tag every section that ABUTS its predecessor as `Chained`
into a shared group — base COMPUTED by the linker. To prove the address is genuinely
computed and not read back, ZERO each chained section's baked `lma` AND clear its
baked `vma_base` (so the computed lma is the sole address authority — an arbitrary
resume org would leave a stale VMA). Result, both shapes:

| shape | ROM sections | re-chained | header-neutral diffs over the assembled bar |
|---|---|---|---|
| plain | 66 | 16 | **0** over [0, 0x5DB60) |
| debug | 68 | 17 | **0** over [0, 0x5F65A) |

"Header-neutral" = the checksum `$18E` and ROM-end pointer `$1A4` zeroed — the only
bytes that differ, and exactly the full-file/appendix fields the assembled-bar
comparison already excludes (the golden's are computed by `fixheader` over the
appendix'd file; sigil's `emit_rom` folds its own over the assembled ROM). Every
CONTENT byte ≥ 0x200 is identical.

Confidence checks along the way (all resolved, none a real obstacle):
- A false "MOVED text −0x68" was a measurement artifact: there are **5 sections named
  `text`** (OBJDEFS's real data + 4 empty comptime-default `text` sections), so a
  by-name orig-lma map collided. The emitted content is identical; OBJDEFS's existing
  driver handling (the emp map region + the "exactly one non-empty `text`" guard)
  reproduces correctly under address-order chaining.
- Two object-bank code sections carry `reserved_span < placement_span` (Δ8 / Δ0x10),
  but they are Pinned anchors (object bank `org $10000`), so they do not perturb any
  chain. Noted for the production chainer: advance uses the stored `reserved_span`.

## THE APPROACH SIMPLIFICATION (the reason to checkpoint)

The brief's Stage-1 item 1 assumed retiring the ~30 resume arms means EDITING aeon
(parameterize/compute the else-arm orgs per game×config, keeping asl byte-identical
because the gates-ON arms are dead code for the default build). The proof shows a
cleaner path: **the driver computes the resume points; the aeon literals are simply
IGNORED (chaining overrides them).** So:

1. **No aeon edit is needed to unblock demo/Config native.** The demo/Config AS
   residual carries the SAME sonic4-valued resume orgs (they flow from `engine.inc`),
   but a chaining driver discards those values and computes each layout's resume
   points from ITS OWN `.emp` placement. The demo-config-blocked STOP dissolves.
2. **asl stays byte-identical for free** — zero aeon commits in the companion work
   (the strongest possible form of the constraint).
3. **The aeon resume-org literals die at THE FLIP COMMIT anyway** (not mine): brief
   §1 already deletes the gate wrappers/includes/org arms with the twins. Pre-flip
   they are harmless inert break-markers; the driver's computation is what matters.

What the driver-side chainer still owes (the real, bounded work):
- Make the chainer a first-class capability in `native.rs` (generalize the proof's
  address-order re-tag into `build_native_rom`'s placement path, or a sibling the
  demo/Config drivers call), preserving the OBJDEFS `text` guard and the drift-guard
  allowlist partition.
- The `$20000` object-bank budget as a map-region check (brief item 3) is the ONE
  piece that still wants the AS-residual section-split (S1.2-map-growth-finding): the
  object-bank code + level-data are one contiguous AS section crossing `$20000`, and
  a map region validates only whole-section containment. This is INDEPENDENT of the
  resume-org computation (which is now proven) and can be sequenced after the six
  proofs, or folded into the flip when the twins collapse the residual to islands.

## Consequence for the brief's stages

- **Stage-1 item 1 (retire resume arms → computed):** MECHANISM PROVEN; realized in
  the driver, no aeon edit. Kill rows 6/58 (`object_test_state` `$5E2DA`/`$5C7EC`,
  `sound_api` `$80E4`/`$6414`) close by the driver computing them — their literals
  join the flip-commit deletion, not a pre-flip aeon edit.
- **Stage-1 item 2 (split AS residual):** needed only for the `$20000` map-region
  budget (item 3), not for computed resume. Decoupled; sequence-flexible.
- **Stage-1 item 3 (pins→map):** the resume-point half is now "computed by the
  chainer" (done in principle); the declarative-geometry half rides item 2.
- **Stage-2 items 5–7 (demo + Config-A/B drivers, the 6×2 proofs):** UNBLOCKED. Each
  is a parameterized sibling driver (demo: engine-only registry, sound OFF, demo pin
  set from `demo.lst`; Config-A/B: the off-canonical defines + per-config drift-guard
  allowlists) that reuses the chainer + the S1.4 split-golden machinery. The demo
  driver is scoped in `2026-07-30-flip-stage1-demo-lockstep-scoping.md`; with chaining
  its pin set is a CROSS-CHECK, not a hard requirement (the layout falls out of
  placement), which further de-risks it.

## Honest scope

Stages 1–2 as briefed are a multi-session effort (the design's own estimate for a
SUBSET — Stage 1 alone — was 7–11 porter-days; my span adds three native drivers +
6×2 proofs + the functional gate family on top). This opening session:
1. Validated the green baseline (both repos, CRCs above).
2. PROVED the computed-resume linchpin (the highest-uncertainty piece) and committed
   it as a permanent regression gate (`native_chained_resume`, 2/2).
3. Found that the linchpin needs no aeon edit — a materially simpler Stage-1 shape —
   and surfaced it for ratification before the six-proof preamble builds on it.

## Sequenced remainder (proposed, pending the overseer's ratification of the approach)

1. Generalize the chainer into `native.rs` as the placement authority (replace the
   pinned resume dependence; keep pins as a cross-check). Re-prove sonic4 both shapes
   == golden (assembled + full-file). Row 6/58 literals marked inert (deleted at flip).
2. Demo native driver (engine-only registry, sound OFF, chainer computes the layout;
   `demo.lst` pins as cross-check). Prove demo plain/debug native == 18c64002/90776 ·
   b0475a59/91584 (assembled anchor + sigil-canonical full file), full functional gate
   family + t24.
3. Config-A / Config-B native drivers (the `mixed_offcanonical_rom` defines; per-config
   drift-guard allowlists, named + both-direction). Prove == b4a6756d/421898 ·
   92776720/304961.
4. The complete 6×2 matrix + strict counts → CHECKPOINT (the no-return commit waits).
5. (Sequence-flexible) AS-residual section-split + the `$20000` map-region budget +
   the declarative-geometry pins→map (item 2/3), if not deferred to the flip.

## Kill-list rows

- **Row 6/58 (resume-org literals):** mechanism to retire them PROVEN (computed by the
  chainer); the literals themselves delete at the flip commit with the gate arms. No
  pre-flip aeon edit — the row closes structurally.
- No new twin scaffolding added this session (the gate is a pure proof; the driver is
  unchanged).

## The valve

Additive only — no aeon edit, no change to the existing native driver or its 5 gates;
one new proof gate added. asl default path byte-identical (verified eff2396f/1e9097bc).
STOP-on-design-fork honored: the no-aeon-edit path is a materially different Stage-1
shape than the brief assumed, surfaced here rather than silently taken.

---

## RATIFICATION (overseer, 2026-07-30) + rulings incorporated

Driver-side chaining is RATIFIED as the Stage-1 realization of "retire the resume
arms" — no pre-flip aeon edit (the ~30 literals are scaffolding the flip commit
deletes; parameterizing them pre-flip is pin-tax in a corpse). The header-neutral
diffs=0 proof both shapes is accepted. Binding conditions/rulings for the next steps:

- **SOUNDNESS CONDITION on step 1 (declared ordering).** When the chainer moves into
  `native.rs` as the placement authority, the chain ORDER must come from an
  EXPLICITLY DECLARED ordering (the map/manifest section list — `sigil.map.toml`'s
  §1.2 ordering-manifest role, SPEC2:199), NOT from sorting baked addresses.
  Sort-by-address is valid ONLY where the baked addresses are known-correct
  (canonical sonic4 — this bootstrap proof). For demo/Config the baked values are
  WRONG sonic4 numbers, so inferring order from them is a silent dependency on their
  relative order surviving. The golden byte-gate would catch a mis-order loudly (so
  it is iteration-cost, not corruption risk), but the declared ordering is what the
  map is FOR. Canonical sonic4 re-proves against the DECLARED order (same 0-diff bar)
  — which doubles as the t24 control that the declaration matches reality.
  - Foundation verified this session: the AS `Module.sections` vec is in stable
    SOURCE-EMISSION order (not address order) — the ROM islands emit in include
    order: Checksum(header) → BootData → ObjCodeBase → TestPlayer → __BUDGET_DATA →
    OJZ level data → HeightMaps → SoundTablesZ80 → … → EndOfRom, EACH identified by a
    stable first-label. So a declared ordering can key AS islands by a contained
    anchor label (game/config-independent) rather than by their `sec<lma>` names or
    baked addresses — the robust build the condition asks for.
- **SCOPE RULING (a) — kill rows 6/58:** their closure EVENT is recorded as THE FLIP
  COMMIT (the literals physically die there; this chained proof is the evidence the
  closure is safe). Same-commit row updates ride the flip parcel — NOT a pre-flip
  aeon edit.
- **SCOPE RULING (b) — AS-residual section-split + `$20000` map-region budget:**
  FOLD INTO THE FLIP COMMIT PARCEL (it models post-flip geometry; not a preamble
  blocker). Removed from the pre-flip sequence.
- **Demo/Config provenance:** demo assembled anchors cfda98d3 (plain) · 20c5571d
  (debug); prove full files vs 18c64002/90776 · b0475a59/91584 with the
  sigil-canonical full files PINNED AS NEW PROVENANCE per the S1.4 split-golden
  pattern (assembled anchor = the cross-checked identity bar; full file = sigil's own).

## THE NESTED-CHECKOUT SCANNER FIX (overseer red run — Stage-2-critical, fixed)

The overseer's countersign strict run (AEON_DIR=the MAIN checkout, which now contains
this nested `.worktrees/flip-stage2`) went RED: 7 failures, all one diagnostic —
`module engine.s4lz declared twice (also at module #0)`, 83 dups = every `.emp` found
in BOTH the main tree and the nested worktree copy. `Manifest::scan`'s recursive walk
wandered into the nested checkout. My worktree AEON_DIR has no nested checkout, so my
runs passed — but both environments are legitimate and the SCANNER was wrong. This is
Stage-2-critical: post-flip, `build.sh` drives `sigil build` from the main checkout,
which routinely contains `.worktrees/` — the flipped build would break the first time
anyone keeps a worktree around.

FIX (`crates/sigil-frontend-emp/src/resolve/manifest.rs`, `collect_emp`): do not
descend into a NESTED CHECKOUT — a subdir named `.worktrees`, OR any subdir carrying
its own `.git` (file or dir — the general nested-clone/`git worktree` root signature).
Only CHILD dirs are tested, so the scan ROOT (legitimately a worktree with a `.git`
FILE) is never excluded. Byte-neutral for a clean tree (no `.emp` under those dirs).
t24 control `resolve_manifest::scan_ignores_nested_checkouts`: plants decoy duplicate
modules under BOTH a `.worktrees/<branch>/` copy AND a `nested_clone/` with its own
`.git`, and asserts the scan finds the module EXACTLY ONCE with zero errors — while
the root itself carries a `.git` file and IS scanned. The existing
`duplicate_module_id_is_an_error` still passes (a REAL dup still errors).

ACCEPTANCE (the new countersign bar): strict green from BOTH AEON_DIR=main-checkout
(nested worktrees present) AND AEON_DIR=worktree. The prefer-(a) declared-module-list
shape from the ratification will REINFORCE this in step 1 (a declared list never walks
into `.worktrees/` at all — discovery-by-scan reduces to validating declared paths);
the scanner exclusion is the immediate, general backstop.

## Updated sequenced remainder (post-ratification)

1. Generalize the chainer into `native.rs` under the DECLARED-ORDERING condition
   (AS islands keyed by anchor label + the `.emp` registry order = the ordering
   manifest). Re-prove canonical sonic4 both shapes == golden against the DECLARED
   order (0-diff bar = the t24 declaration-matches-reality control). The nested-
   checkout robustness composes here (prefer the declared list as the module source).
2. Demo native driver (engine-only registry, sound OFF, chainer computes the layout).
   Prove demo plain/debug native == 18c64002/90776 · b0475a59/91584 (assembled
   anchors cfda98d3 · 20c5571d; sigil-canonical full files pinned as new provenance).
3. Config-A / Config-B native drivers (off-canonical defines; per-config NAMED
   drift-guard allowlists, both-direction). Prove == b4a6756d/421898 · 92776720/304961.
4. The complete 6×2 matrix + strict counts → THE PRE-FLIP CHECKPOINT (the no-return
   commit waits for the overseer's go).
- FOLDED INTO THE FLIP PARCEL (not preamble): AS-residual section-split + `$20000`
  map-region budget + the row-6/58 literal deletions.
