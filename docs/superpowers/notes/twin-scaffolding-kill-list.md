# Twin-scaffolding kill list (living doc)

Volence's standing ask (2026-07-10): every twin-related TEMPORARY mirror —
a value declared on both sides of the .emp/AS seam with a drift guard
holding the copies together — gets a row HERE with its kill condition.
Nothing on this list is a bug; everything on it is scheduled demolition
the campaign must not forget.

**The rule:** a mirror exists only while the value's OWNER is still on the
AS side. Each kill is an ownership flip (the .emp side becomes the only
definition, exported as an equ to remaining AS readers — the reverse seam)
or a consolidation (per-file mirror moves into the shared constants twin).
When a row dies, delete its mirror consts AND its drift guards together.

**Cadence:** every port that adds a mirror adds a row; every checkpoint
packet reviews the list; the campaign-end sweep closes whatever survives.

## Live mirrors

| # | Mirror | Where | Guards | Kill condition |
|---|--------|-------|--------|----------------|
| 1 | `engine.constants` twin — `HW_PORT_1_DATA`, `HW_PORT_2_DATA`, `BUTTON_UP/DOWN/LEFT/RIGHT`, `CTYPE_AIR`, `VDP_Shadow_len` | `engine/system/constants.emp` mirroring `engine/constants.asm` (+ the struct-generated `VDP_Shadow_len`) | 8 ensures, riding every twin consumer's gate | `constants.asm` ports → ownership flip (twin becomes THE file, AS readers take exported equs). `VDP_Shadow_len` dies earlier if the `VDP_Shadow` struct ports with `vars`. |
| 2 | Animation block in the constants twin — `AF_END`, `AF_BACK`, `AF_DELETE`, `AF_SET_FIELD`, `DUR_DYNAMIC`, `OBJ_CODE_BANK`, `FRAME_PIECE_COUNT` (grew from the tranche-6 `AF_DELETE` consolidation; tranche 9 RE-HOMED the AS-side AF_* truth animate.asm → `engine/constants.asm` so script data files survive the `SIGIL_EMP_ANIMATE` gate — the row-13 lesson applied: the written kill ("animate.asm ports → flip") was unexecutable because pitcher_plant/anims.asm + the gate-off twins keep reading AF_* AS-side) | `engine/system/constants.emp` (truth: `engine/constants.asm` — ONE home now) | 7 ensures (in row 1's block, riding every twin consumer's gate) | `constants.asm` ports → flip (row 1's condition — this row fully merged into row 1's class at tranche 9). |
| 3 | ~~`AF_END`, `AF_BACK`, `DUR_DYNAMIC` local consts in sonic_anims.emp~~ CLOSED BY CONSOLIDATION (tranche 9): the constants twin's animation block absorbed all three; sonic_anims.emp now imports via `use engine.constants` (particle_anims precedent). The 3 local ensures died; the twin's ensures (row 2) guard the values. | — | — | — |
| 4 | `ANIM_*` ordinal guards ×12 | `sonic_anims.emp` checking `games/sonic4/config/constants.asm` | 12 ensures | TWO-STAGE (Volence-ratified 2026-07-10): **[1 DONE]** the flip mechanics proven (`reverse_seam_ordinals.rs` — .emp `equ` exports of ordinals, AS consumes through the link; D2.34); **[2 at Spec 5]** delete the config block + swap player code to the exports — the gate-off shape can't read .emp exports until the dual build dies, and the imm deferral needs `.b`/`.w` widths first (ledgered). |
| 5 | The AS twins themselves — `collision_lookup.asm`, `vdp_init.asm`, `hblank.asm`, `controllers.asm`, `math.asm`, `sonic_anims.asm`, `particle_anims.asm`, ~~`dac_samples.asm`~~ (RETIRED EARLY at seam-2 stage-2b: BINCLUDE'd from `dac_samples.emp`), ~~`mt_bank` block~~ (RETIRED at seam-2 stage-2c: the 7 MT `.asm` deleted; BINCLUDE'd from `mt_bank.emp` + `mt_syms.asm`), ~~`sfx` block~~ (RETIRED at seam-2 stage-2d: the 20 SFX `.asm` deleted — 18 `sfx_NN{,_patches}.asm` + `sfx_table.asm` + `sfx_blob_win_tab.asm`; BINCLUDE'd from `sfx_bank.emp` (body, `sfx_bank{,_debug}.bin`) + `sfx_blob_win_tab.emp` (head, `sfx_blob_win_tab{,_debug}.bin`, co-linked via `sfx_bank.emp`'s `SFX_WIN_*` equ layer); NO syms — no surviving AS reader of `SfxTable`; `sfx_transcode.py` moved from prebuild-auto to MANUAL), `rings.asm`, **`aabb.inc`** (row 13 consolidated here at the rings port — its only consumers are the gate-off twins collision.asm + rings.asm), `animate.asm` (tranche 9 — incl. the `reloadAnimTimer` macro, LOCKSTEP twin of animate.emp's `reload_anim_timer` comptime fn), **`plane_buffer.asm`** (tranche 17 — the deferred plane-buffer draw half; SHAPE-INVARIANT $29C both shapes), **`parallax.asm`** (tranche 18 — the §4.6 parallax pipeline; SHAPE-INVARIANT $556 both shapes), **`children.asm`** (tranche 24 — the parent/child + effect spawner; SHAPE-DEPENDENT after the step-5 wave: $29C plain / $34E debug, the surplus being the two chain-contract asserts + one `ifdef __DEBUG__` branch width) [NOTE: this enumeration lags — the load_object/entity_window/section/collision_lookup/tile_cache gate-off BODY twins (tranches 13–16) also belong on this row and were never appended; flagged for a backfill sweep] | every ported file's gate-off body, kept in LOCKSTEP with its .emp (the step-5 mechanics) | the byte gates ARE the guard | **Spec 5** (AS front-end deletion): each `ifndef SIGIL_EMP_*` gate collapses to the .emp include, the .asm twin is deleted. Until then every byte-changing edit pays double (edit both, re-pin). |
| 6 | Per-shape gate pins — the `org` resume addresses in `engine.inc`/`main.asm` + the region bases/sizes/windows in the sigil harness | ~18 engine regions + 2 game-data pairs (and growing) — tranche 17 adds the `PLANE_BUFFER` region pin (`repin.toml`/`pins.rs`) + the two `SIGIL_EMP_PLANE_BUFFER` resume orgs ($42FA plain / $4EC4 debug in `engine.inc`); tranche 24 adds the `CHILDREN` region pin + the two `SIGIL_EMP_CHILDREN` resume orgs ($4046 plain / $4C14 debug), whose values are the SHARED `Load_Object` anchor it must keep equal to `LOAD_OBJECT.plain_base`/`debug_base` (`children_port::children_region_pins_share_both_anchors`) | strict gates fail loud on drift; `pins_rs_is_current` guards the region pins | Same as row 5 — the pins exist only while the dual build exists. Every reference re-baseline re-derives them (the re-pin tax). |
| 7 | `Act`/`Sec` struct twins | `act_descriptor.emp` (truth: `engine/structs.asm`), layout-pinned via `sizeof() == extern("Act_len"/"Sec_len")` | 2 ensures | `structs.asm` ports (the vars-era struct story) → ownership flip; or consolidate into a shared game structs module when act 2 arrives. |
| 8 | `MAX_ACT_SECTIONS`, `SECTION_SIZE_SHIFT`, `EDGE_CLAMP` mirrors | `act_descriptor.emp` (truth: engine constants; mirrored so the grid/clamp checks run at COMPTIME, and EDGE_CLAMP because a link-folded extern can't range-prove into a u8 field) | 3 ensures | owning engine files port → flip; or the constants twin grows a level block (same consolidation class as rows 2/3). |
| 9 | sonic4's `gameDebugTick` EXPANSION (`jsr Debug_MusicToggle` under `SOUND_DEBUG_HOTKEYS && SOUND_DRIVER_ENABLED`) | `engine/system/game_loop.emp` mirroring `games/sonic4/config/game.asm`'s macro body (tranche-5 H2 ruling — the game-contract macro seam) | `game_loop_port.rs`'s combo matrix re-extracts the macro body from the REAL game.asm every run and byte-diffs all four define combos — drift fails the matrix; LOCKSTEP comment at the macro | a ratified .emp game-contract-hook mechanism (extern-macro or link-time hook — Spec-5 neighborhood, same era as row 4 stage 2), or GameLoop migrating game-side. First game-contract macro with a NON-TRIVIAL body to port is the construct's demand moment (ledgered). |
| 10 | sound_api immediate mirrors — `SND_ALIVE_MARKER`, `SND_MUSIC_STOP`, `SND_FADE_CMD_OUT/IN`, `SFX_RING_MASK`, `SFXID_RING_RIGHT/LEFT` | `engine/sound/sound_api.emp` (truth: `engine/sound_constants.asm` + `games/sonic4/config/sound_ids.asm`) — immediate-POSITION only; the slot ADDRESSES stay AS-owned as extern-equ sums (nothing to drift there) | 7 ensures, riding the thirteen-module gates | KILL CONDITION TRIPPED 2026-07-24 (t23 shipped the `.b` deferral; `.w` existed) — SPLIT OUTCOME per the sweep law: the 5 UNTYPED mirrors (SND_ALIVE_MARKER, SND_MUSIC_STOP, SND_FADE_CMD_OUT/IN, SFX_RING_MASK) retire at next sound_api touch (bare link names, byte-neutral); the 2 TYPED mirrors (SFXID_RING_RIGHT/LEFT: SfxId) are BLOCKED — they carry the F1 typed enforcement, which a bare link name cannot (typed-extern grammar is the remaining dependency). Or the owning constants files port → flip. |

| 11 | The `Sst` struct twin — the FULL production SST layout (29 fields, $50 bytes) | `engine/objects/sst.emp` (truth: `engine/structs.asm`'s `SST` struct), type-only/zero-byte; `@`-asserted offsets double-locked by per-field ensures over the struct-equ seam | 30 ensures (riding both object-module gates + the mixed gates) | `engine/structs.asm` ports → ownership flip (sst.emp BECOMES the definition; AS readers take exported equs until Spec 5). Construct-walk #3 retypes the fields in place first (annotation diff, not a layout change). |
| 12 | `RF_COORDMODE`, `RF_PRIORITY_SHIFT` render-flag mirrors | `engine/system/constants.emp` (truth: `engine/constants.asm:177-179`) | 2 ensures (in row 1's block) | `constants.asm` ports → flip (row 1's condition). |
| 13 | ~~`aabb_axis_test` template twin — `aabb.inc` vs `aabb.emp`~~ CLOSED BY CONSOLIDATION (tranche 8): the rings port made the gate-off AS twins (collision.asm + rings.asm) the .inc's ONLY consumers, so the written kill ("delete the .inc") was unexecutable before Spec 5 — the .inc is re-homed under row 5 as twin scaffolding (LOCKSTEP comments updated both files). The promised zero-disp collapse probe SHIPPED at the rings port and PASSED UNCHANGED (the F1 splice already rode the general `(0,An)`→`(An)` collapse). Process lesson recorded: kill conditions written before their port get re-verified against the gate-off shape's needs. | — | — |
| 14 | Collision-block constants twin — `NUM_PLAYERS/DYNAMIC/SYSTEM/EFFECTS`, `COLLISION_TOUCH`, `ST_IN_AIR/ON_OBJECT` (8 values; the two `ST_P1/P2_STANDING` bits DIED with their consumers in the tranche-7b interact-pointer fix — the AS-side `constants.asm` deleted them under a tombstone note, twin count 20→18) | `engine/system/constants.emp` (truth: `engine/constants.asm:140-197`) | 7 ensures (in row 1's block) | `constants.asm` ports → flip (row 1's condition). |
| 15 | `interact_off()` displacement mirror — `collision.emp`'s `comptime fn interact_off() -> int { sizeof(Sst) - 2 }` ($4E), the player-slot tail word that carries which solid claimed a player this pass | `engine/objects/collision.emp` (truth: `engine/structs.asm`'s `SST_interact = SST_sst_custom + SST_CUSTOM_SIZE - 2`); spelled as a call-expr because a bare const name in a displacement slot on a TYPED register is (correctly) closed to the field namespace | 1 ensure (drift-locks `$4E` against `extern("SST_interact")`, riding collision's gate + the mixed gates) | `engine/structs.asm` ports → flip (SST_interact becomes .emp-owned), OR `sst.emp` models the per-slot-type overlay windows (object slots keep the full 34-byte custom window; player slots overlay the tail word) — then `interact_off()` becomes a real typed field access. |

| 16 | ~~The `assert.b` expansion TRANSLITERATION — rings.emp's DEBUG block spells debugger.asm's `assert`→`RaiseError`→FSTRING macro-tower output as literal asm + `dc.b` data (incl. two parity-derived flag/pad constants)~~ **KILLED 2026-07-12** — the `.emp` diagnostics construct (`assert`/`raise_error`) landed (sigil `diag-construct`); rings+core retrofit (aeon `452c7c1`) replaces the transliterations with one-line asserts, byte-neutral. Superseded by row 21. | `engine/objects/rings.emp` + `core.emp` | — | KILLED (construct shipped). |
| 17 | ~~VDP sprite-table geometry mirrors — `MAX_VDP_SPRITES`, `VDP_SPRITE_X_OFFSET`, `VDP_SPRITE_Y_OFFSET`~~ HOISTED TO ROW-1 CLASS (tranche 11): the sprites.emp port FORCED the flip — the gate removes sprites.asm's defs but the gate-off rings.asm twin reads them in immediates (reverse-seam-in-immediate is the unshipped `.b`/`.w` imm-link deferral), so the three `=` defs moved sprites.asm → `engine/constants.asm` (the "constants hoist" alternate kill in the original row). They are now genuine constants.asm↔constants.emp mirrors — row-1 condition (constants.asm ports → flip). | `engine/system/constants.emp` (truth NOW: `engine/constants.asm`) | 3 ensures (in row 1's block) | `constants.asm` ports → flip (row 1's condition — same class as rows 1/12/14/19). |
| 18 | GAME-owned ring capacity/geometry mirrors — `MAX_RING_BUFFER`, `RING_BUFFER_ENTRY_SIZE`, `RING_WIDTH`, `VRAM_RING_PLACEHOLDER` (a placeholder art slot — the ensure makes the real-art change loud) | `engine/objects/rings.emp` module-local (truth: `games/sonic4/config/constants.asm:48-50,70` — engine.inc game-CONTRACT symbols; row 10's consumer-module-mirror class) | 4 ensures riding rings' gate + the mixed gates + a doctored-truth probe (`doctored_game_mirror_fires_its_guard`) | the `.b`/`.w` imm-link deferral lands (all four sit in immediate/moveq/addq positions), or the game config port — whichever first. Spec-5 reverse seam runs through the game contract surface. |
| 19 | Object-core constants twin — `NUM_TOTAL_SLOTS` (derived `= NUM_PLAYERS+NUM_DYNAMIC+NUM_SYSTEM+NUM_EFFECTS`), `CULL_DISTANCE_X` ($300), `CULL_DISTANCE_Y` ($200), `SLOT_TAG_UNTAGGED` ($FF) — the tranche-10 growth 30→34 | `engine/system/constants.emp` (truth: `engine/constants.asm:145,209-210,467`) | 4 ensures (in row 1's block, riding core's gate + the mixed gates); `test_support::engine_constant_equs()` carries them for the AS-twin seam | `constants.asm` ports → ownership flip (row 1's condition — same class as rows 1/12/14). |

| 20 | Sprite render/frame/screen constants twin — the tranche-11 growth 34→49: render-flag bits `RF_ONSCREEN/XFLIP/YFLIP/MULTISPRITE`, band geometry `PRIORITY_BANDS/SPRITES_PER_BAND/SCANLINE_BANDS/SCANLINE_SPRITE_LIMIT`, `SCREEN_WIDTH/HEIGHT`, frame-header offsets `FRAME_BBOX_{X,Y}_{MIN,MAX}/FRAME_PIECES` (sprites.emp is the first comptime consumer of these constants.asm values) | `engine/system/constants.emp` (truth: `engine/constants.asm`) | 15 ensures (in row 1's block, riding sprites' gate + the mixed gates); `test_support::engine_constant_equs()` carries them for the AS-twin seam | `constants.asm` ports → ownership flip (row 1's condition — same class as rows 1/12/14/19). |

| 21 | The diagnostics construct's TWIN-PARITY EMISSION — `assert`/`raise_error` desugar reproduces debugger.asm's exact expansion: the message FSTRING encoding ($E0/$E8/$EA/$EC control + arg descriptor bytes), the even-offset align-pad parity rule, the pinned `b<cond>.w` branch width, and this engine's debugger config (`EXTENSIONS__ENABLE=1` → `jmp PagesController` tail). Sourced from debugger.asm, hard-coded in the lowering. | `crates/sigil-frontend-emp/src/eval/diag.rs` + `eval/asm.rs` (truth: `engine/debug/debugger.asm`'s macro layer + the engine's debugger config) | the CLI acceptance vector (`diag_assert_vector.rs`) assembles the unmodified real macro through the AS front-end and byte-diffs it every run — macro/config drift fails the gate; the rings+core debug-shape port gates re-derive the expansion in-place | Spec 5 — when the `.asm` twins die, the message format and the `b<cond>.w` pin are freed (no reference to reproduce). A debugger.asm config change is a construct change until then (one engine, §7.5). |

| 22 | entity_window seam mirrors (tranche 12) — GAME-owned collected caps `COLLECTED_WINDOW_SLOTS`(9)/`COLLECTED_SLOT_SIZE`(34)/`COLLECTED_PARK_SLOTS`(4)/`COLLECTED_PARK_ENTRY_SIZE`(33) (row-18 class), engine consts not yet in constants.emp (`MAX_TRACKED_SECTIONS`, `MAX_LIST_ENTRIES`, `ENTITY_LOAD/DESPAWN_BUFFER(_Y)`, `ENTITY_LOADED_SLOT_SIZE`/`_OBJ_OFFSET`, `ENTITY_RESCAN_COARSE_MASK`, `COLLECTED`/`KILLED_BITMASK_OFFSET`, `COLLECTED_EMPTY_TAG`/`_MASK_BYTES`, `OEF_TYPE_SHIFT`/`_MASK`, `OBJ_ENTRY_SIZE`, `RING_BUFFER_ENTRY_SIZE`), and the `Sec`/`Act` field-offset mirrors (sst.emp SST_-offset class). `SECTION_SIZE`/`_SHIFT`/`SEC_VOID` CONSOLIDATED into `engine.constants` 2026-07-15 (tranche-15 section.emp port, the 2nd/3rd consumer trigger) — entity_window now `use`s them; local mirrors + 3 ensures deleted; `engine_constant_equs()` carries them. The `EntityScanState` field-offset mirrors ADOPTED into a file-local struct twin 2026-07-15 (sst-usability-batch item 2) — moved to row 25. | `engine/objects/entity_window.emp` module-local (truth: `games/sonic4/config/constants.asm` + `engine/constants.asm` + `engine/structs.asm`) | ~25 ensures riding entity_window's plain+debug byte gates; `entity_window_port.rs::entity_window_equs()` carries the remainder for the AS-twin seam | `.b`/`.w` imm-link deferral lands (all in immediate/displacement positions) OR the source ports to .emp — whichever first; the `Sec`/`Act` offsets die when their struct-twins adopt (still consumer-gated). |
| 23 | `clear_slot_bitmasks()` comptime-fn (tranche 12) — the AS twin spells the nine stores (`clr.b 1(a0)` pad + 8 mask `clr.l`) INLINE at each of 3 sites (Collected_Init/ClaimSlot/UpdateCenter); no AS `rept` (the `clear_longs` class) | `engine/objects/entity_window.emp` (the fn) vs `engine/objects/entity_window.asm` (3 inline blocks) | the entity_window_port plain+debug byte gates name the first diverging byte if only one side changes | Spec 5 — dies with the twin (the AS inline mirror has no reference once entity_window.asm dies); `clear_longs` class. |
| 24 | `stop_z80()`/`start_z80()` comptime-fn templates (retro-fix batch 2) — sound_api.emp spells `engine/macros.asm`'s `stopZ80`/`startZ80` macros as comptime-fn templates (each `stop_z80` site splices its own hygienic `.wait_z80` poll loop); the AS twin `sound_api.asm` invokes the real `stopZ80`/`startZ80` macros at every site (PostByte/Init/PlayMusic/DrainSfxRing) | `engine/sound/sound_api.emp` (the two fns) vs `engine/macros.asm` (the two macros, invoked from `sound_api.asm`) | the sound_api_port plain+debug byte gates name the first diverging byte if only one side changes (macro↔comptime-fn lockstep, the `reloadAnimTimer`/`reload_anim_timer` class under row 5) | Spec 5 — dies with the AS twin: once `sound_api.asm` is gone the macros have no 68k invoker to mirror against (the fns become the sole spelling). A `macros.asm` change to `stopZ80`/`startZ80` is a template change until then. |
| 25 | The `EntityScanState` struct twin (sst-usability-batch item 2) — the entity-window scan-state record (11 fields, $1A bytes), FILE-LOCAL (single consumer, unlike the shared `Sst`/`Act`/`Sec` twins); `@`-asserted offsets, adopted from t12's row-22 offset-const mirrors. Access via `EntityScanState.ess_*(aN)` + `sizeof`/`offsetof`. | `engine/objects/entity_window.emp` (truth: `engine/structs.asm`'s `EntityScanState` struct), type-only/zero-byte; 12 per-field ensures double-lock the `@` offsets over the struct-equ seam; `entity_window_port.rs::entity_window_equs()` carries the AS equs | 12 ensures (riding entity_window's plain+debug byte gates + the mixed gates) | `engine/structs.asm` ports → ownership flip (the twin BECOMES the definition; AS readers take exported equs until Spec 5). Same class as rows 7/11. |
| 26 | `coll_src_row_base()` comptime-fn (t16 step-1) — mirrors `engine/macros.asm`'s `collSrcRowBase` macro (`lsr #1` tile→coll row, `lsl #4` ×BLOCK_COLL_COLS); the AS twin keeps the `collSrcRowBase` MACRO invocation inline at 2 sites (CopyBlockColumn/FillRow). Macro-port class (rows 16/24). | `engine/level/tile_cache.emp` (the fn, file-local) vs `engine/level/tile_cache.asm` (2 `collSrcRowBase` macro calls) | `tile_cache_port` plain+debug byte gates name the first diverging byte if only one side changes; the fn's `ensure(BLOCK_COLL_COLS==16)` mirrors the macro's `if <> 16 / error` | Spec 5 — dies with the twin (tile_cache.asm retirement; the AS macro invocation has no reference once tile_cache.asm dies). |
| 27 | `decompose_block()` comptime-fn (t16 step-4) — world-tile (col,row) → sec_x=d0/sec_y=d1/block_index=d2; a PURE DEDUP helper (no AS macro — the donor spelled it inline, self-flagged "(same pattern as FillColumn/FillRow)"). The AS twin keeps the ~13-instr decompose INLINE at **4 sites** (FillColumn/FillRow + WarmupBelowRow + Fill's leftover-budget prefetch — the last two adopted it during Wave 2, superseding step-4's "2 sites"). `clear_slot_bitmasks`/`clear_longs` class (row 23). | `engine/level/tile_cache.emp` (the fn, file-local) vs `engine/level/tile_cache.asm` (4 inline decompose blocks) | `tile_cache_port` plain+debug byte gates name the first diverging byte if only one side changes | Spec 5 — dies with the twin (tile_cache.asm retirement). |
| 28 | `mul_cache_stride()` comptime-fn (t16 step-5 loop-until-dry) — multiplies a cache row by TILE_CACHE_STRIDE (×80) via shift-add `(row<<6)+(row<<4)`, the "form (a)" scratch-reg variant (gap-ledger row 1060, revisited post-step-5). A PURE DEDUP helper (no AS macro — donor spelled it inline). The AS twin keeps the 4-instr ×80 shift-add INLINE at 3 sites (GetTile/GetCollision/FillRow cache_row_offset). CopyBlockColumn's "form (b)" single-temp ×80 (×2) stays inline BOTH sides — a register-pressure variant this helper deliberately does not cover (row 1060). `clear_longs`/`decompose_block` class (rows 23/27). | `engine/level/tile_cache.emp` (the fn, file-local) vs `engine/level/tile_cache.asm` (3 inline ×80 blocks) | `tile_cache_port` plain+debug byte gates name the first diverging byte if only one side changes; the fn's `ensure(TILE_CACHE_STRIDE==80)` drift-locks the #6/#4 shifts | Spec 5 — dies with the twin (tile_cache.asm retirement). |
| 29 | ~~`extern proc VSync_Wait () clobbers(d0)` (contract-grammar v2 §3 boundary decl) — caller-side mirrors in game_loop.emp + load_art.emp~~ **KILLED 2026-07-24 (t21 vblank port)**: `VSync_Wait` is `engine.vblank`-owned (`pub proc VSync_Wait () clobbers(d0) preserves(sr)` — the pinned contract carried exactly; `preserves(sr)` is the sound_api-convention strengthening the lint mandates, surfaced in the t21 packet). BOTH decls deleted same-commit; artifacts: `game_loop_port::two_module_ownership_flip_{plain,debug}` + `load_art_port::two_module_ownership_flip_{plain,debug}` (each compiles caller + vblank.emp together, decl gone, both regions byte-gated vs the reference ROM). | — (was game_loop.emp + load_art.emp) | — | DONE. |
| 30 | ~~`extern proc S4LZ_DecompressDict (a0, a1, a4: *DictBase, d4) clobbers(d0-d3/a0/a2-a4) out(a1)` (§3) — the .asm S4LZ dict-decompressor's contract~~ **KILLED 2026-07-24 (t22 s4lz port)**: `S4LZ_DecompressDict` is `engine.s4lz`-owned (`pub proc ... clobbers(d0-d3/a0/a2-a4) out(a1) falls_into S4LZ_Decompress` — the decl's contract carried EXACTLY, incl. the row's history note: the .asm-to-the-letter license, not the spec §3 draft's under-read). Decl deleted same-commit; artifact: `tile_cache_port::two_module_tail_call_flip_{plain,debug}` (compiles tile_cache.emp + collision_lookup.emp + s4lz_decompress.emp together, carrier dropped, all three regions byte-gated vs the reference ROM). | — (was `engine/level/tile_cache.emp`) | — | DONE. |
| 31 | ~~`extern proc QueueDMA_Important (d1, d2, d3) clobbers(d0-d4/a1-a2)` (§3) — the .asm DMA-queue enqueue (Important priority)~~ **KILLED 2026-07-24 (tranche 20)** — dma_queue.emp owns QueueDMA_Important; dplc.emp's decl deleted same-commit; module-to-module resolution proven by `dplc_port::two_module_ownership_flip_{plain,debug}`. (Historical NOTE kept: census part (b) missed this + QueueDMA_Deferrable — they enter via `perform_dplc(QueueDMA_*)`, invisible to a text census; spec §3 erratum, 5 externs not 3.) | — | — | KILLED (owner ported). |
| 32 | ~~`extern proc QueueDMA_Deferrable (d1, d2, d3) clobbers(d0-d4/a1-a2)` (§3) — the .asm DMA-queue enqueue (Deferrable priority); second copy at t19 in bg_anim.emp (panel-adjudicated KEEP-duplicated)~~ **KILLED 2026-07-24 (tranche 20)** — dma_queue.emp owns QueueDMA_Deferrable; BOTH decls (dplc.emp + bg_anim.emp) deleted same-commit; flips proven by `dplc_port::two_module_ownership_flip_*` + `bg_anim_port::two_module_ownership_flip_*`. | — | — | KILLED (owner ported). |
| 33 | ~~`extern proc Debug_MusicToggle () clobbers(d0-d2/a0/a1)`~~ **KILLED (t26)** — the GAME-side debug hotkey handler's contract. | ~~`engine/system/game_loop.emp`~~ | — | **DONE:** `game_debug.emp` ports Debug_MusicToggle (t26 lane B); the extern decl deleted same commit — the call now resolves module-to-module (a decl would §11-Q4-collide with the proc). The honest clobbers turned out to be `d0-d4/a0-a1`, not `d0-d2/a0/a1`: the extern's narrow leaf contract had HIDDEN Sound_PlayMusic's d3/d4 leak — porting the body surfaced it (contract closure). Proven by `contract_closure_corpus` (no hole, no collision, no firing) + `game_debug_port::two_module_flip_resolves_debug_music_toggle`. |
| 34 | **asl listings as address ground truth** (`s4.lst`/`s4.debug.lst`) — repin's pin extraction (`repin.rs:57-58`), convsym debug symbols, profiler/s4budget symbol maps all read the asl reference's listing. Reference-era scaffolding: valid ONLY while asl is the byte-gate reference. KNOWN HAZARD (2026-07-22 Parcel-A escalation): the `.debug.*` pair exists only via a MANUAL post-build cp (build.sh emits `s4.bin`/`s4.lst` for both shapes), so a skipped lst copy leaves a coherent-looking STALE listing — poisoned repin's debug side + a section-base check for a full escalation round. Mitigations: repin listing-freshness proof (bytes-vs-ROM cross-check, Parcel-A rider) + shape-suffixed native emit in build.sh (post-Parcel-A toolchain parcel). | `sigil-harness` (repin/pins) + aeon `build.sh`/tools (truth: asl `-OLIST` output) | repin freshness gate (mechanical, once the rider lands); byte gates catch content divergence but NOT listing staleness | Spec-2 cutover retires asl as reference → sigil's own symbol table becomes address ground truth; repin's lst parsing + the cp/suffix machinery are DELETED with it. |

| 35 | **OJZ harness per-frame mode-register force-write** (`GameState_OJZScroll_Update` :234-273) — force-writes the VDP mode-set-3 shadow + reg $0B directly every frame from the active config (Target during transition, else Current), a WORKAROUND for `Parallax_StartTransition` writing the mode ONLY at frame 0 (its same-config short-circuit leaves the register stale across a transition). NOT a twin mirror — a test-harness compensation for an engine gap; tracked here at the gate's direction. **t41 ADJUDICATED: CARRY-AS-IS** — the row-35 survey (2026-07-29-t41-stop1-row35-survey-and-recon.md) proved the gap is structurally PRESENT but UN-EXERCISED by shipped content (all act sections resolve to ONE config → the transition never fires → the force-write is dead insurance today; the `ojz_default.asm` MISDIAGNOSIS note corroborates). The overseer ruled the block ports VERBATIM into `ojz_scroll_test.emp` (present-tense-commented, naming the compensation + the ledgered fix parcel), row 35 STAYS OPEN. | `games/sonic4/test/ojz_scroll_test.emp` :234-273-equivalent (ported verbatim at t41 + the `.asm` twin under row-5 lockstep; compensates for parallax StartTransition's frame-0-only mode write) | none mechanical (harness-only; the B2/parcel rig A/B is the behavioral check that the engine renders coherently without it) | **The parallax-hardening parcel closes the engine gap** (per-frame / on-active-change mode-register write in `Parallax_Update` from `Parallax_Active_Config`; the shadow-vs-direct-reg-$0B sub-question is its step-0 item; full wave + C1+C3 + oracle A/B with the ojz `Debug_Scene_Freeze` cache-fill soak explicitly in the A/B plan) → THEN the harness force-write DIES (a one-block deletion in `ojz_scroll_test.emp` + `.asm` under its own byte-changing mini-wave), OR Spec-5 carries it commented if the parcel never runs. Parcel LEDGERED 2026-07-29 (t41 §parallax-hardening); the fix-now-vs-later promotion is surfaced to Volence with carry-as-is as the executed default. |

| 36 | **`engine.z80_bus` shared templates** — `stop_z80()`/`start_z80()` comptime fns, the .emp counterparts of `engine/macros.asm`'s `stopZ80`/`startZ80` macros, hoisted to a shared module at t19 (bg = the 2nd consumer that tripped the tranche-5 demand gate). Consumers: bg.emp + sound_api.emp (both t19 — the step-6 sweep deleted sound_api's local copies, collapsing row 24's fn-side into this row; the macro↔template lockstep class lives HERE now). | `engine/z80_bus.emp` (truth: `engine/macros.asm`'s two macros, invoked by every gate-off AS twin site) | consumer byte gates (bg_port; sound_api_port after the retrofit) name the first diverging byte if either side changes | Spec 5 — dies with the AS twins (once no `.asm` invoker of the macros remains, the templates are the sole spelling). A `macros.asm` change to `stopZ80`/`startZ80` is a template change until then (row 24's condition, re-homed). |

| 37 | `clamp_camera_axis()` comptime-fn (t19 pass-1 step-4) — the continuous-scroll world clamp, one axis (13 instrs; terms: axis RAM cell / Act grid-dim offset / screen extent). A PURE DEDUP helper (no AS macro — the donor spelled it inline at both sites). The AS twin keeps the clamp INLINE at 2 sites (camera.asm `.x_done` X clamp + `.clamp_y` Y clamp). `clear_slot_bitmasks`/`decompose_block` class (rows 23/27). | `engine/level/camera.emp` (the fn, file-local) vs `engine/level/camera.asm` (2 inline clamp blocks) | `camera_port` plain+debug byte gates name the first diverging byte if only one side changes | Spec 5 — dies with the twin (camera.asm retirement). |
| 38 | ~~`extern proc S4LZ_Decompress (a0, a1) clobbers(d0-d3/a2-a3) out(a0, a1)` (§3) — the .asm blocking S4LZ decompressor's no-dict entry contract~~ **KILLED 2026-07-24 (t22 s4lz port)**: `S4LZ_Decompress` is `engine.s4lz`-owned (`pub proc S4LZ_Decompress (a0: *u8, a1: *u8) clobbers(d0-d3/a2-a3) out(a0, a1)` — the decl's contract carried EXACTLY; the a4/d4-untouched fact is contract-carried by their absence from the license). Decl deleted same-commit; artifact: `load_art_port::two_module_ownership_flip_{plain,debug}` (now compiles load_art.emp + vblank.emp + s4lz_decompress.emp together, carrier dropped, all three regions byte-gated vs the reference ROM). | — (was `engine/level/load_art.emp`) | — | DONE. |
| 39 | ~~`extern proc ZX0_Decompress (a0, a1) clobbers(d0-d1) out(a0, a1)` (§3) — the .asm ZX0 decompressor's contract~~ **KILLED 2026-07-24 (t22 zx0 port)**: `ZX0_Decompress` is `engine.zx0`-owned (`pub proc ZX0_Decompress (a0: *u8, a1: *u8) clobbers(d0-d1) out(a0, a1)` — the decl's contract carried EXACTLY; the a2/d2 movem round-trip is `preserves(d2/a2)`-ENFORCED (§5-verified once step 2's jbsr spelling landed — the step-1 `bsr.s` spelling defeated the proof via the CFG's bsr-as-branch heuristic; mechanism ledgered)). Decl deleted same-commit; artifact: `load_art_port::two_module_ownership_flip_{plain,debug}` (load_art.emp + vblank.emp + s4lz_decompress.emp + zx0_decompress.emp in one link, carrier dropped, all four regions byte-gated vs the reference ROM; load_art.emp now carries ZERO extern decls). | — (was `engine/level/load_art.emp`) | — | DONE. |
| 40 | dma_queue rept-family LOCKSTEP mirrors (t20) — `fill_slot_markers`/`jt_slot`/`jt_filler`/`dma_send_entry` comptime fns + the named-drain-label jump table spell what dma_queue.asm spells as `rept`/`set` unrolls and `bra.w .drain_end-.c*8` arithmetic; the two sides must move together on any queue-geometry or entry-layout change. | `engine/system/dma_queue.emp` (twin: `engine/system/dma_queue.asm`) | the dma_queue_port byte gates both shapes + `ensure(DMA_CRITICAL_SLOTS == 8)` + the DMAEntry drift wall | dma_queue.asm retires (Spec 5) → the LOCKSTEP comments die with it; until then the byte gates are the guard. |
| 41 | `queue_static_dma(entry)` comptime fn + the `SRC_*` dmaSource-equ family (t21) — buffers.emp's counterparts of `engine/macros.asm` `queueStaticDMA` (entry-only, Critical-bound interface; the slot/end pair lives once inside the fn) and of the `dmaSource` AS function over link-time RAM sources (`equ SRC_* = (extern(sym) >> 1) & $7FFFFF` — the row-1004 comptime/link boundary; comptime addrs use engine.vdp's `dma_source`). The two sides must move together on any queue-protocol or entry-layout change. | `engine/system/buffers.emp` (twin: `engine/system/buffers.asm` + `engine/macros.asm:292`) | the buffers_port byte gates both shapes + the tranche21 probe `global_symbol_arg_splice_with_ccr_consumer_matches_as` (macro-expansion parity) + `linkexpr_shift_mask_over_extern_matches_as` | buffers.asm/macros.asm retire (Spec 5) → dies with the twin; until then the byte gates are the guard. |
| 42 | ~~`extern proc Sound_DebugMirror () clobbers(d0-d1/a0-a1)` (§3 boundary decl) — vblank.emp's caller-side mirror of the Z80-state snapshot~~ **KILLED (t28 lane S)** — `sound_debug.emp` ports Sound_DebugMirror (engine.debug.sound_debug); the extern decl deleted same-commit (vblank.emp), the mirror-shape call resolves module-to-module (a decl + the `.emp`-owned proc would §11-Q4 collide, the kill-row-33 precedent). **This was the corpus's LAST `extern proc` — ZERO remain (the t26 census's final member closed).** The honest contract `clobbers(d0-d1/a0-a1)` (a LEAF, no transitive callees) carried exactly. Proven by: `vblank_port::vblank_mirror_shape_twin_parity` (region bytes, decl deleted) + `mixed_offcanonical_rom::mixed_combined_config_a_rom_matches_reference` (the byte-exact cross-seam call in the whole off-canonical ROM — the stronger flip artifact, since the harness section model blocks combining two separately-lowered `.emp` modules). | — | — | **DONE.** |
| 43 | `sr_masked(code)` comptime fn (t21 step 4, `engine/irq.emp`) — the paired SR save/mask/restore bracket template; a PURE DEDUP helper (no AS macro — the twins spell the bracket inline at every site). Consumers: vblank.emp VSync_Wait (in-tranche); sound_api's two paired label-free brackets are step-6 sweep candidates. Unpaired/looped/long-span sites stay inline BY DESIGN (module header names them). | `engine/irq.emp` (twins: inline brackets in vblank.asm, sound_api.asm, …) | `tranche21_spelling_probes::sr_masked_code_argument_matches_as` (byte parity vs the hand-spelled bracket) + the consumers' byte gates both shapes | Spec 5 — dies with the twins (inline spellings retire); until then the byte gates are the guard. |
| 44 | `const TILE_SIZE = 32` file-local mirror (t22, `engine/compression/s4lz_decompress.emp`) — the first .emp consumer of engine/constants.asm's `TILE_SIZE` (TileDelta_Undo's tile advance). File-local per the 1st-consumer rule. | `engine/compression/s4lz_decompress.emp` (truth: `engine/constants.asm:124`) | `ensure(extern("TILE_SIZE") == TILE_SIZE)` + negative probe `s4lz_port::doctored_tile_size_fires_its_guard` (fires NAMING the constant) | 2nd .emp consumer OF THE NAME appears → hoist to the shared engine.constants twin (row-1051 class); or constants.asm retires (Spec 5). NB (t22 panel B1): value-level 32-bytes-per-tile already rides in load_art.emp (`ART_POOL_PAGE_TILES * 32`), dplc.emp (`lsl #5`), tile_cache.emp — those sites feed the hoist trigger's judgment at next touch even though none mirrors the NAME. |
| 45 | **gameBootHook macro-mirror** (t23) — boot.emp's `if SOUND_DEBUG_HOTKEYS == 1 && SOUND_DRIVER_ENABLED == 1 { ... }` block mirrors sonic4's `gameBootHook` macro EXPANSION (ping + autoplay + `Dbg_Music_On`; empty in both canonical shapes and in demo). The game_loop.emp:33 `gameDebugTick` class — this file must follow that macro body; edit both together. P3 RESOLVED (overseer ruling 1, loop pass 1): moveq link-imm8 SHIPPED (`ImmSigned8` fixup, signed-8 window) — the hotkeys arm lowers and its drift matrix is LIVE. | `engine/system/boot.emp` (truth: `games/sonic4/config/game.asm` `gameBootHook`) | boot_port byte gates both canonical shapes (empty-arm parity) + `boot_port::boot_hotkeys_shape_twin_parity` (the (1,1) shape byte-matches the full AS oracle carrying the REAL macro expansion — a macro-body edit the mirror doesn't follow fails it) | `gameBootHook`'s body moves .emp-side (game-config ports) → the mirror IS the definition; or the macro empties permanently → arm deleted both sides. |
| 46 | **boot.emp ↔ boot_data.asm cursor contract** (t23, the brief's ruled seam) — the .emp walks the AS-side BootData table with ONE sequential `(a5)+` cursor (movem.w d5-d7 + movem.l a0-a4 head, 24 reg bytes, fill command, Z80 blob copy, PSG bytes, post-DMA commands); the code's read widths/order and the table's layout must move together. Enforcement is SPLIT: the AS-side assert wall in boot_data.asm (movem head 26 / blob at +54 / post-blob evenness / total lock — both assemblers run it) + the boot_port byte gates. | `engine/system/boot.emp` (cursor) vs `engine/system/boot_data.asm` (table + wall) | the boot_data.asm `if/fatal` wall (negative-fired at the split commit) + boot_port plain+debug byte gates + `mixed_tranche23_{rom,debug_rom}` full-ROM arms | a data-table DSL / typed-table import makes the layout single-sourced (the .emp declares the table, the walk derives from it); or boot_data.asm retires with the Z80 ladder rework (Volence-deferred). |
| 47 | `const PSG_PORT = $C00011` + `PSG_DATA_OFF = PSG_PORT - VDP_DATA` file-local mirror + derive (t23, boot.emp) — the first .emp consumer of constants.asm's `PSG_PORT`; needed at COMPTIME for the PSG-silence displacement off the VDP_DATA base (the engine.vdp consumer-side derive pattern). The VDP_MODE2_OFF half EXECUTED (overseer ruling 3, loop pass 1): the VDP_Shadow offset consts (hblank's VDP_MODE1_OFF/VDP_HINT_OFF + boot's VDP_MODE2_OFF) are HOISTED into engine.vdp's shadow-offset block with their ensures riding; both consumers import; byte-neutral (all gates green vs unchanged canonical). | `engine/system/boot.emp` (truths: `engine/constants.asm:14`, `engine/structs.asm:11`) | `ensure(extern("PSG_PORT") == PSG_PORT)` (boot.emp) + the engine.vdp shadow-offset ensures ×3 + negative probe `boot_port::doctored_psg_port_fires_its_guard` (fires NAMING the constant) | PSG_PORT: 2nd .emp consumer → shared twin (row-1051 class). VDP_MODE2_OFF: DONE — lives in engine.vdp; the engine.vdp block dies when structs.asm retires (Spec 5). |
| 48 | `vdp_reg()`/`bytes_to_lcnt()` comptime fns (t23, boot.emp) — file-local counterparts of `engine/macros.asm`'s `vdpReg`/`bytesToLcnt` AS functions (macro-port class, rows 16/24/26; interfaces kept minimal ints — step-4 re-adjudicates typing). The AS twin keeps the function invocations inline; vdp_init.emp's `VDP_REG_CMD`/`VDP_REG_STEP` consts spell the same $8000/$0100 truth — engine.vdp is the natural shared home. CENSUS CORRECTED (t23 panel B1): the /4-1 derivation already has 6+ inline corpus sites (core.emp:53, entity_window.emp ×4) — the 2nd-consumer clock was passed at this row's birth; adoption debt ledgered (shared home for bytes_to_lcnt is NOT engine.vdp — general memory-loop derivation). | `engine/system/boot.emp` (truth: `engine/macros.asm:12,24`) | boot_port plain+debug byte gates bind every fold (the fns feed immediates only) | 2nd .emp consumer → hoist into `engine.vdp` (row-1052 class); or macros.asm retires (Spec 5). |

| 49 | `pixels_to_coord(v)` comptime fn (t24 step-4, `engine/coords.emp` — a NEW shared template module) — the integer-pixels → 16.16 `Coord` promotion (`swap`/`clr.w`), the single most-repeated shape in the spawn and camera paths. A PURE NAMING/DEDUP helper (no AS macro — every twin spells the two instructions inline). Consumers: children.emp ×10 (in-tranche). Step-6 sweep candidates: camera.emp ×3, plus the `.asm` sites (load_object.asm, ojz_scroll_test.asm) that are twin-side only. | `engine/coords.emp` (the fn) vs every AS twin's inline `swap`/`clr.w` pair | the consumers' byte gates both shapes name the first diverging byte if either side changes | Spec 5 — dies with the twins (once no `.asm` spells the pair inline, the template is the sole spelling). |
| 50 | `refresh_piece_count(sst, ptr, scratch)` comptime fn (t24 step-4, `engine/objects/frames.emp`) — the SST `sprite_piece_count` recompute (mappings null-check → frame-table walk → header piece-count → store). Kills an EXACT 12-instruction duplicate that had been sitting in two engine files: `animate.emp`'s `RefreshSpritePieceCount` and `children.emp`'s `PopulateSpawnedPieceCount` are the same procedure with different registers. BOTH adopted in-tranche (byte-neutral); the AS twins (`animate.asm`, `children.asm`) keep the sequence inline. Its first line absorbed the older `frame_piece_count` template (still used by load_object.emp). | `engine/objects/frames.emp` (the fn) vs `animate.asm` + `children.asm` (2 inline copies) | `animate_port` + `children_port` byte gates both shapes name the first diverging byte if any side changes | Spec 5 — dies with the twins. |

| 51 | `CHILD_INHERITED_FLAGS` (t24 ruling wave) — the render_flags bits a child inherits from its parent, declared in BOTH twins (`children.emp` const / `children.asm` equ) because the `.emp` const cannot be seen by the gate-off AS build. Value today is `(1 << RF_COORDMODE)`; the priority-band half was reverted (a band is a 3-bit value and this composes with `or` — see the gap-ledger row). Both sides derive from `RF_COORDMODE`, which carries its own `extern()` drift ensure, so the VALUE cannot drift silently; what can drift is the two spellings of the POLICY. | `engine/objects/children.emp` (const) vs `engine/objects/children.asm` (equ) | the children_port byte gates both shapes — the mask is an immediate in the encoding, so a divergent policy changes bytes and fails the gate | Spec 5 — dies with the twin; or the mask moves into the shared constants twin if a second file ever inherits flags. |

## NOT on this list (deliberately)

- **Co-residency ensures** (mt_bank ×5, sfx_bank ×1): they check PLACEMENT
  facts across the seam, not duplicated values — they survive the campaign
  as permanent invariants (possibly re-expressed once both sides are .emp).
- **Module-local consts with no AS counterpart** (`VDP_REG_CMD`/`VDP_REG_STEP`
  in vdp_init.emp): single-definition, nothing to kill.
| 52 | **Numeric `ErrorHandler` equ in engine.inc's `SIGIL_EMP_ERROR_HANDLER` gate-ON arm** (t25) — every OTHER gate-else arm is `org`-only; this one additionally defines `ErrorHandler: equ $5CC0A/$5E704` (per shape) so `mddbg_symbols.asm`'s `MDDBG__*` equ table derives off a CONSTANT (folds at assemble time). Required because sigil does not resolve an equ off a link-EXTERNAL base (the `.emp`-owned ErrorHandler) — the numeric-org derivation is the workaround. The `.emp` blob proc is renamed `ErrorHandlerBlob` so it never collides with this equ at link. | `engine/engine.inc` gate-ON arm (numeric equ) vs the pure-AS `ErrorHandler` blob label (error_handler.asm) — same address, two spellings | `error_handler_port` (windowed) + `mixed_dac_rom::mixed_error_handler_*` (whole-ROM) byte gates both shapes: a wrong numeric equ shifts every MDDBG__ symbol and fails them | **`error_handler_port::derived_equ_off_external_base_is_unresolved_today`** flips (sigil gains link-time-equ-off-external-base) → the numeric pin retires to `equ ErrorHandler+$0` off the flipped `.emp` symbol; OR dies with the twins. |
| 54 | **game_debug's game-contract const surface** (t26 lane B, `games/sonic4/debug/game_debug.emp`) — 5 button masks (`BUTTON_UP/B/C/A/START`) + 3 song ids (`SONG_MOVINGTRUCKS/DRUMTEST/HCZ2`) + 8 sfx ids (`SFXID_*`) mirrored as file-local `const`s because the FIRST game-side `.emp` code module has no game-constants `.emp` home yet, and the `.emp` const cannot be seen by the gate-off AS build. The d-pad four already live in engine.constants.emp; A/B/C/START do not (a hoist is its own ripple — the pinned engine-constants count + every consumer's guard tally). | `games/sonic4/debug/game_debug.emp` (consts) vs `engine/constants.asm` (buttons) + `games/sonic4/config/sound_ids.asm` (songs/sfx) | 16× `ensure(extern("X") == X)` drift guards + negative probe `game_debug_port::doctored_extern_fires_drift_guard` (fires NAMING the drifted const) | a game-constants `.emp` module is born (SONG_*/SFXID_* move there) AND BUTTON_A/B/C/START hoist into engine.constants.emp → this block collapses into two `use`s; or config/sound_ids.asm + constants.asm retire (Spec 5). |
| 53 | **`raise_exception` is pinned to the donor debugger config `DEBUGGER__EXTENSIONS__ENABLE=1` + `DEBUGGER__SHOW_SR_USP=0`** (t25, C2 panel) — the construct hardcodes the extensions-ENABLED `__ErrorMessage` shape (flag byte `opts+_eh_return` + `jmp (pages).l`) and emits only the per-site `address_error`/`0` opts, dropping `_eh_default` (= `_eh_show_sr_usp` iff SHOW_SR_USP set). Byte-identical ONLY under the current config; a flip would diverge (ENABLE=0 drops `_eh_return`+the jmp; SHOW_SR_USP=1 adds `$02` to every opts). The region + mixed byte gates CATCH a flip as a failure, but nothing points the editor at the construct. | `raise_exception` lowering (diag.rs, the enabled-shape assumption) vs debugger.asm:22/25 (`DEBUGGER__EXTENSIONS__ENABLE`/`DEBUGGER__SHOW_SR_USP`) | the error_handler byte gates fail if either equate flips | RE-CHECK `raise_exception`'s shape if either donor equate changes; or the POST-TWIN-RETIREMENT `.emp`-native diagnostics runtime (which owns the format) subsumes the pin. |
| 57 | ~~**dac_sample_tab twin — `dac_sample_tab.asm` canonical body** (t27 lane C)~~ **CLOSED — DELETED at seam-2 stage-2b (Option Y).** The whole-ROM banked-head seam landed: `soundBankHead` now BINCLUDEs the co-linked `dac_sample_tab.bin` at VMA `$85AD` (aeon `sound_bank.inc`, unconditional). The scale-2 complication — a comptime SOURCE for the 30 SND_* — dissolved by CO-LINK: `dac_samples.emp`'s `SND_*` equs resolve CROSS-MODULE at link into the head's cells (no `-D`, no mirror). `dac_sample_tab.asm` + the SCALE(1) windowed oracle (`dac_sample_tab_port.rs`) are DELETED SAME-COMMIT (the oracle's twin is gone; the whole-ROM gate supersedes it). | (twin gone — `dac_sample_tab.emp` is the sole spelling; the head is BINCLUDE'd from the co-linked `.bin`) | `seam2_dac_head_colink::colinked_dac_head_matches_the_reference_rom_slice_both_shapes` (90 B head == reference, BOTH shapes) + `seam2_dac_rom::seam2_dac_rom_matches_reference_{plain,debug}` (whole-ROM byte gate) + `seam2_dac_rom::..._diverges_when_head_doctored` (t24 non-vacuity) | **DONE (seam-2 stage-2b).** |
| 56 | **✅ CLOSED (seam-2 stage-3): `seq_opcode_tab.asm` DELETED; `seq_opcode_tab.emp` is canonical, BINCLUDE'd (`seq_opcode_tab{,_debug}.bin`) in `soundBankHead` at VMA $856D.** The kill condition below ("the whole-ROM banked-head SEAM lands") IS this. The emit co-links the 32 `dc.w Seq_Op_*` cells against the REAL resident handler VMAs from the SAME seam-1 blob link the driver ships from (`seam2::emit_seq_opcode_tab` → `native_sound_blob().symbols`) — so the imports resolve to the exact VMAs `z80_sound_syms.asm` exported (design §2c). SHAPE-DEPENDENT (the handlers re-base after sound_sequencer's `if DEBUG==1` growth). The windowed oracle (`seq_opcode_tab_port`) RETIRED with the twin; the `SeqOpcodeTable`/`_End` labels + the `32*2` span guard move into the `soundBankHead` BINCLUDE bracket (a `fatal` now). Identity bar: `seam2_seq_colink` (64 B == reference BOTH shapes + shape-variance + determinism) + `seam2_sfx_rom::seam2_sfx_rom_diverges_when_seq_doctored` (whole-ROM t24 control) + the seam2 whole-ROM gates (BINCLUDE path). NOTE: `z80_sound_syms.asm`'s `Seq_Op_*` equs are now VESTIGIAL for the AS side (nothing references them after this) but the emit still writes them + boot_data.asm still includes them (byte-neutral — equs emit nothing); cleanup candidate. — **seq_opcode_tab twin — `seq_opcode_tab.asm` canonical body** (t27 lane B) — the sequencer coordination-opcode jump table (32 × `dc.w <resident-Z80-label>`), the FIRST real file to exercise the phase-1(c) resident-Z80-address `Value16Le` value cell. Proven at SCALE (1): a WINDOWED oracle byte-gates `seq_opcode_tab.emp` == `seq_opcode_tab.asm` (64 B), with the ~26 `Seq_Op_*`/`Seq_BadOpcode` handler addresses as equ carriers. NO build gate yet — the `.asm` stays CANONICAL (included in main.asm's `phase 08000h` head); the `.emp` is not wired into build.sh at this scale. The AS twin's `if (End-Start) <> 32*2 / error` span guard stays the canonical entry-count enforcer (no doctorable extern → no `.emp` comptime equivalent). | `engine/sound/seq_opcode_tab.emp` vs `engine/sound/seq_opcode_tab.asm` (canonical body) | `seq_opcode_tab_port::seq_opcode_tab_matches_as_twin` (windowed byte gate, 64 B) + `emp_diverges_from_doctored_twin` (t24 positive control, one doctored handler address) + `doctored_both_sides_stay_equal` | the whole-ROM banked-head SEAM lands (scale (2): gate the `soundBankHead` include `ifndef SIGIL_EMP_SEQ_OPCODE_TAB`, place the `.emp` at the phased head LMA, region-pin) — then this row gains its gate + resume-org pin like row 55; OR Spec 5 (the `.asm` twin retires). Until a scale-(2) seam exists, the oracle is the sole drift guard. |
| 55 | **z80_init twin — `z80_init.asm` gate-off body** (t27 lane A) — the FIRST Z80 CODE `.emp` port and first live `module (cpu: z80)`. `boot_data.asm`'s no-sound `else` arm gates `ifndef SIGIL_EMP_Z80_INIT` (AS include) `else` (deferred DEMAND-3 .emp placement). CANONICALLY EMPTY (both shipped shapes have sound ENABLED → the whole `else` is skipped), so the port is proven by the OFF-CANONICAL `z80_init_port` oracle, not a whole-ROM diff. The internal `Z80_IdleProgram_CodeEnd` is a trailing LOCAL label `.code_end` (no external consumer; `Z80_IDLE_SIZE = End - Start` is a separate AS-side boot-cursor equate the code bytes do not contain). | `engine/system/z80_init.emp` vs `engine/system/z80_init.asm` (gate-off body) + the boot_data.asm `SIGIL_EMP_Z80_INIT` gate | `z80_init_port::z80_init_matches_as_twin` (off-canonical byte gate, 38 B) + `emp_diverges_from_doctored_twin` (t24 positive control) + the dual-build byte-identity (c51342d0/992d9e7d unchanged with the gate); **t28: ADD `mixed_offcanonical_rom::mixed_z80_init_config_b_rom_matches_reference` (the whole-ROM no-sound Config-B gate, .emp placed at 0x3D8 == pure-asl)** | Spec 5 — dies with the twin. **The DEMAND-3 whole-ROM off-canonical no-sound machinery LANDED (t28)** — the no-sound full build IS now byte-gated against the .emp placement (per overseer ruling 2, the whole-ROM gate is ADDED WITHOUT retiring the `z80_init_port` window oracle, which keeps the doctored-twin falsification). The twin itself retires only at Spec 5. |
| 58 | **Off-canonical gate-arm org pins** (t28 lane M) — the `SIGIL_EMP_GAME_DEBUG` else-arm `org $6408` (main.asm), the `SIGIL_EMP_SOUND_DEBUG` else-arm `org $827C` (engine.inc), and the `SIGIL_EMP_Z80_INIT` else-arm `org $3FE` + `Z80_IDLE_SIZE = $3FE-$3D8` (boot_data.asm). Each is a CONFIG-specific sonic4-shape address (Config A hotkeys/mirror; Config B no-sound) — the region-end resume point the `.emp` placement fills up to. NOT in pins.rs (they are inline AS org literals); re-derive on any reference re-baseline. The gate defines must NEVER be set except by the `mixed_offcanonical_rom` gates at their config. | `games/sonic4/main.asm` / `engine/engine.inc` / `engine/system/boot_data.asm` (else-arm orgs) vs the pure-asl region emission | the three `mixed_offcanonical_rom::mixed_*` whole-ROM gates (a wrong org shifts every downstream byte and fails them) | Spec 5 — the gate arms collapse to the `.emp` include when the `.asm` twins retire; until then the orgs are re-pin-tracked scaffolding. |
| 59 | **sound_debug twin — `sound_debug.asm` gate-off body + the const mirrors** (t28 lane S) — the AS twin (`sound_debug.asm`) is the gate-off body under `SIGIL_EMP_SOUND_DEBUG` (row-5 class); PLUS `sound_debug.emp`'s `const SEQ_CHANNEL_LEN = 60` / `const SND_SEQ_TRACE_LEN = 32` mirrors of the AS-side comptime constants (the TILE_SIZE / row-44 class), drift-locked so the `(SeqChannel_len-SEQ_MIRROR_CHBYTES)(a0)` displacement + `#SND_SEQ_TRACE_LEN-1` moveq fold at comptime. | `engine/debug/sound_debug.emp` (const mirrors + gate-off twin) vs `engine/sound_constants.asm` (SeqChannel endstruct = 60; SND_SEQ_TRACE_LEN = 32) | `sound_debug.emp`'s 2 drift-guard `ensure(extern("X") == const)` (checked in the combined whole-ROM gate — a firing guard fires NAMING the drifted const; verified live by the `SEQ_CHANNEL_LEN=36` mis-guess during the port) + the `mixed_combined_config_a` byte gate | Spec 5 — dies with the twin; OR the 2nd `.emp` consumer of SeqChannel_len/SND_SEQ_TRACE_LEN hoists the mirror to a shared twin (row-1051 class). |

| 60 | **test_static.asm / test_animated.asm gate-off body twins** (t29 G1) — the two AS object files are the gate-off bodies under `SIGIL_EMP_TEST_STATIC` / `SIGIL_EMP_TEST_ANIMATED` (row-5 class), each an `ifndef/else org` arm in `games/sonic4/main.asm`. Bank windows are shape-invariant, so ONE `org` literal per arm serves both shapes (`org $10C6A` static / `org $10CC4` animated); the resume orgs are the row-6/row-58 class (inline AS org literals, NOT in pins.rs — re-derive on re-baseline). The region pins ARE in pins.rs (`TEST_STATIC`/`TEST_ANIMATED`, repin-generated). The gate defines must NEVER be set for other games (demo takes the includes). | `games/sonic4/objects/test_static.asm` + `test_animated.asm` (gate-off bodies) + `games/sonic4/main.asm` (gate arms) vs the `.emp` regions | `test_g1_objects_port::g1_objects_{,debug_}regions_match_reference` (windowed byte gates both shapes) + `g1_undoctored_compile_equals_the_reference_window` (positive control) + `g1_doctored_reference_diverges` (t24 negative probe) + gate-off dual-build byte-identity (c51342d0/992d9e7d unchanged) | Spec 5 — each `ifndef SIGIL_EMP_TEST_*` gate collapses to the `.emp` include, the `.asm` twin + the `org` arm are deleted. |
| 61 | **DplcV SST-overlay twin** (t29 G1) — test_animated.emp's `vars DplcV: Sst.sst_custom { dplc_ptr: u32, art_base: u32 }` overlay ($2E/$32) mirrors the AS-side `DplcV struct` + `_dplc_ptr`/`_art_base` equates. The AS truth SURVIVES in `test_player.asm` (the other `ifndef _dplc_ptr` DPLC user — when test_animated.asm is gated OUT, test_player.asm becomes the first-and-only definer of the identical equs), NOT in the gated-out test_animated.asm. The overlay's always-on `[overlay.window-overflow]` check subsumes the AS `objvarsCheck DplcV_len`. Row-25 EntityScanState / row-11 Sst class (first game-side SST-overlay twin — arrived in G1, ahead of the census's G3/test_parent prediction). | `games/sonic4/objects/test_animated.emp` (`vars` overlay) vs `games/sonic4/objects/test_player.asm` (surviving DplcV struct + `_dplc_ptr`/`_art_base`) | test_animated.emp's 2 drift guards `ensure(extern("_dplc_ptr"/"_art_base") == offsetof(Sst, sst_custom) + offsetof(DplcV, …))` (captured + pass in `test_g1_objects_port`) + the windowed byte gates (a divergent displacement fails them) | `test_player.asm` ports → ownership flip (the `.emp` overlay BECOMES the definition, or a SHARED DplcV twin is born for both DPLC users); or Spec 5. |
| 62 | **VRAM_TEST_SONIC game-config mirror** (t29 G1) — test_animated.emp's `const VRAM_TEST_SONIC: VramTile = $03C0` mirrors `games/sonic4/config/constants.asm:83` because the `.emp` const cannot be seen by the gate-off AS build (row-54 game-config-mirror class; typed VramTile — the item-13 tile-index vocabulary). | `games/sonic4/objects/test_animated.emp` (const) vs `games/sonic4/config/constants.asm` (`VRAM_TEST_SONIC = $03C0`) | `ensure(extern("VRAM_TEST_SONIC") == VRAM_TEST_SONIC)` (captured + pass) + the windowed byte gates (the value folds into the art_tile/vram_bytes immediates) | a game-constants `.emp` module is born (VRAM_TEST_* moves there); or config/constants.asm retires (Spec 5). |
| 63 | **vram_bytes comptime-fn twin** (t29 G1) — test_animated.emp's `comptime fn vram_bytes(tile: VramTile) -> VramAddr { tile << 5 }` is the `.emp` counterpart of `engine/macros.asm`'s `vram_bytes` AS function (macro-port class, rows 24/26/48). File-local first consumer (s4lz TILE_SIZE row-44 precedent); the AS twin keeps the `vram_bytes` function invocation inline (sonic.asm, test_player.asm — twin-side only today). The result folds to a comptime immediate. | `games/sonic4/objects/test_animated.emp` (the fn) vs `engine/macros.asm:18` (`vram_bytes function`) | `test_g1_objects_port` byte gates bind the fold (the fn feeds the Perform_DPLC d1 immediate) | 2nd `.emp` consumer → hoist to the shared VRAM-layout home ALONGSIDE `vram_art` (objdef.emp's anticipated shared home; the 2nd-consumer clock is sonic.asm/test_player); or macros.asm retires (Spec 5). |

| 64 | **test_emitter / test_stress_emitter / test_churn gate-off body twins** (t30 G2) — the three AS object files are the gate-off bodies under `SIGIL_EMP_TEST_EMITTER` / `_TEST_STRESS_EMITTER` / `_TEST_CHURN` (row-5 class), each an `ifndef/else org` arm in `games/sonic4/main.asm`. Bank windows shape-invariant → ONE `org` per arm both shapes (`org $11030` emitter [test_parent's FIRST label TestChildPart, NOT TestParent], `$111B6` stress, `$1122E` churn); resume orgs are inline AS literals (row-6/58 class, re-derive on re-baseline). Region pins ARE in pins.rs (`TEST_EMITTER`/`TEST_STRESS_EMITTER`/`TEST_CHURN`, repin-generated). Gate defines must never be set for other games. | the three `.asm` gate-off bodies + `games/sonic4/main.asm` (gate arms) vs the `.emp` regions | `test_g2_objects_port::g2_objects_{,debug_}regions_match_reference` (windowed both shapes) + `g2_undoctored_compile_equals_the_reference_window` (positive control) + `g2_doctored_reference_diverges` (t24 negative probe) + `mixed_dac_rom::mixed_tranche30_{,debug_}rom_matches_assembled_reference` (whole-ROM both shapes) + gate-off dual-build byte-identity (c51342d0/992d9e7d unchanged) | Spec 5 — each `ifndef SIGIL_EMP_TEST_*` gate collapses to the `.emp` include, the `.asm` twin + the `org` arm are deleted. |
| 65 | **VRAM_TEST_OBJ game-config mirror ×3** (t30 G2) — each of the three files carries `const VRAM_TEST_OBJ: VramTile = $03E0` mirroring `games/sonic4/config/constants.asm:67` (row-54/62 game-config-mirror class; VRAM_TEST_OBJ survives AS-side, unlike the overlay equates). Three mirrors because no game-constants `.emp` module exists yet. | the three `.emp` consts vs `games/sonic4/config/constants.asm` (`VRAM_TEST_OBJ = $03E0`) | 3× `ensure(extern("VRAM_TEST_OBJ") == VRAM_TEST_OBJ)` (captured + pass in `test_g2_objects_port` + `mixed_tranche30`) + the byte gates (the value folds into the vram_art art_tile immediate) | a game-constants `.emp` module is born (VRAM_TEST_* moves there, alongside row-62's VRAM_TEST_SONIC); or config/constants.asm retires (Spec 5). |
| 67 | **test_parent.asm gate-off body twin + org arm** (t31 G3) — the AS object file is the gate-off body under `SIGIL_EMP_TEST_PARENT` (row-5 class), an `ifndef/else org` arm in `games/sonic4/main.asm`. Bank window shape-invariant → ONE `org` both shapes (`org $1115C`, test_stress_emitter's first label TestStressEmitter; resume org is an inline AS literal, row-6/58 class, re-derive on re-baseline). The region pin IS in pins.rs (`TEST_PARENT` region, base $11030 = TestChildPart, len $12C, repin-generated); the `TestParent` LABEL symbol pin is renamed `TEST_PARENT_LABEL` to avoid the const collision. Gate define must never be set for other games. | `games/sonic4/objects/test_parent.asm` (gate-off body) + `games/sonic4/main.asm` (gate arm) vs the `.emp` region | `test_g3_objects_port::g3_objects_{,debug_}regions_match_reference` (windowed both shapes) + `g3_undoctored_compile_equals_the_reference_window` (positive control) + `g3_doctored_reference_diverges` (t24 negative probe) + `mixed_dac_rom::mixed_tranche31_{,debug_}rom_matches_assembled_reference` (whole-ROM both shapes) + gate-off dual-build byte-identity (c51342d0/992d9e7d unchanged) | Spec 5 — the `ifndef SIGIL_EMP_TEST_PARENT` gate collapses to the `.emp` include, the `.asm` twin + the `org` arm are deleted. |
| 68 | **TParentV / TOrbitChildV `vars` overlays** (t31 G3) — the parent state (`life_timer: u16 @0, swing_phase: u16 @2, x_dir: u8 @4` — swing_phase added by the Volence-ruled lifecycle fix 2026-07-29, life_timer now a pure countdown) and child orbit state (`orbit_angle: u8 @0, phase_offset: u8 @1, radius: u16 @2`) both overlay the SAME `sst_custom` region (parent and child never coexist in one slot). Like t30's TEmitterV (row 66) and UNLIKE t29's DplcV (row 61), the field equates live ONLY in the gated-out `.asm` (single consumer — grep of the whole AS `games/` tree finds NO surviving reader of `_parent_life_timer`/`_parent_x_dir`/`_child_angle`/`_child_phase_offset`/`_child_radius`), so the overlays carry ZERO extern drift guards; offset $2E is guaranteed by sst.emp's `SST_sst_custom` ensure. The two `[overlay.window-overflow]` checks subsume the two AS `objvarsCheck` calls (`> SST_CUSTOM_SIZE` → fatal) — the census PORTER-VERIFY resolved: subsumed, NO separate `ensure`. The `orbit_angle` field is `orbit_`-prefixed because a bare `angle` overlay field would shadow Sst's DIRECT `angle` field (`[overlay.shadows-field]`); single-consumer, so the rename carries no drift guard. | the two `.emp` overlays vs the gated-out `test_parent.asm` structs/equates (no surviving AS truth) | `test_g3_objects_port` + `mixed_tranche31` byte gates (the overlay offsets fold into displacements) | Spec 5 — die with the twin. |
| 69 | **`SpawnDesc` shared descriptor struct + its AS twins across THREE files** (t31 G3, the t30-ruled hoist) — `pub struct SpawnDesc (size: 4) { code: ObjRoutine @0, x_off: i8 @2, y_off: i8 @3 }` in `children.emp` (the format owner) is the `.emp` counterpart of the AS descriptor records the CreateChild_Normal / CreateEffect_Normal walks read. Its AS twins are the raw-`dc` descriptor blobs in THREE gated-out `.asm` files: `test_parent.asm` `.child_desc` (3 entries), `test_emitter.asm` `.particle_desc` (1 entry), `test_stress_emitter.asm` `.particle_desc` (1 entry) — each spelled as `dc.w objroutine(...)` + `dc.b x, y`. The `.emp` consumers spell `[SpawnDesc; N]` (or a bare `SpawnDesc`) + a SEPARATE `dc.w 0` terminator; this REPLACED the two emitters' fused 6-byte `EffectSpawn1` struct same-commit, byte-identical (the G2 gates prove it). SpawnDesc is a PURELY `.emp`/harness type — the real dual build uses the raw-`dc` twins and never sees it; the harness prepends it via a filtered `spawn_desc_ambient_items` (children.emp is code-bearing, so only the struct item rides). | `engine/objects/children.emp` (`SpawnDesc`) + the 3 `.emp` consumers vs the 3 gated-out `.asm` raw-`dc` descriptor blobs | `test_g2_objects_port` + `test_g3_objects_port` + `mixed_tranche30`/`mixed_tranche31` byte gates (SpawnDesc's `code` folds into each descriptor's objroutine word; a divergent field layout fails them) | Spec 5 — dies with the twins (the raw-`dc` blobs have no reference once the `.asm` files retire); OR the descriptor gains a data-table DSL that single-sources the layout. |
| 65b | **VRAM_TEST_OBJ game-config mirror — 4th consumer** (t31 G3) — test_parent.emp carries `const VRAM_TEST_OBJ: VramTile = $03E0` mirroring `games/sonic4/config/constants.asm:67` (row-65 class; the 4th mirror after the three t30 emitters, still no game-constants `.emp` module). | test_parent.emp's const vs `games/sonic4/config/constants.asm` | `ensure(extern("VRAM_TEST_OBJ") == VRAM_TEST_OBJ)` (captured + pass in `test_g3_objects_port` + `mixed_tranche31`) + the byte gates (folds into the vram_art art_tile immediate) | a game-constants `.emp` module is born (VRAM_TEST_* moves there); or config/constants.asm retires (Spec 5). Collapses WITH row 65. |
| 66 | **TEmitterV / TStressEmitterV / TChurnV `vars` overlays + EffectSpawn1 descriptor structs** (t30 G2) — each file's `vars TXV: Sst.sst_custom { timer/life: u16 }` mirrors the AS `TXV struct` + `_X_timer/_X_life = SST_sst_custom+...` equate. UNLIKE t29's DplcV (row 61), the field equates live ONLY in the gated-out `.asm` (single consumer, no surviving AS truth) → the overlays carry ZERO extern drift guards; the offset $2E is guaranteed by sst.emp's `SST_sst_custom` ensure. **EffectSpawn1 RETIRED at t31** — the two emitters' fused 6-byte `struct EffectSpawn1 (size:6)` was REPLACED by the shared 4-byte `SpawnDesc` (row 69) + a separate `dc.w 0` terminator, byte-identical; the descriptor-twin tracking moved to row 69. The overlays' `[overlay.window-overflow]` check subsumes the AS `objvarsCheck`. | the three `.emp` overlays vs the gated-out `.asm` structs/equates (no surviving AS truth); the descriptor structs → row 69 | `test_g2_objects_port` + `mixed_tranche30` byte gates (the overlay offsets fold into displacements) | Spec 5 — die with the twins (the AS structs/equates have no reference once the `.asm` files retire). **row 63 (vram_bytes hoist) did NOT trip: no G2 file consumes vram_bytes; all three consume vram_art (already shared).** |
| 70 | **✅ CLOSED (seam-1, THE FIRST TWIN DELETION — aeon `85ae87c`): `sound_psg.asm` DELETED; `sound_psg.emp` is the canonical source (sigil native-links it, asl BINCLUDEs the emitted blob); the windowed oracle transformed to a reference-slice gate.** — **sound_psg twin — `sound_psg.asm` canonical body** (t32, the rung-2 CONTRACT-model acceptance corpus) — the SN76489 PSG voice writer (15 procs), the FIRST resident-blob Z80 CODE port. Proven at SCALE (1): a WINDOWED oracle byte-gates `sound_psg.emp` == `sound_psg.asm` at BOTH shipped shapes (444 B / $1BC each): plain window $1660, debug window $16DE (upstream __DEBUG__ growth shifts the phase-0 base; psg's own layout is shape-invariant, so the shapes differ only in the internal call/jp absolute targets + the Snd_ChanClass address). The comptime constants ride as `-D` defines, the cross-seam symbols (banked tables PsgDivisorTableZ/PsgVolEnv_*/FmVolEnv_*, external calls Mod_ReArm/Mod_Advance/Snd_ChanClass) as equ carriers. NO build gate yet — the `.asm` stays CANONICAL (included in z80_sound_driver.asm's `phase 0` blob, 4th of 5); the `.emp` is not wired into build.sh at this scale (the resident-blob seam sub-tranche owns whole-ROM placement — the t27 seam-STOP family). The FULL rung-2 register contract set is LIVE + machine-checked (63 firings → 0 after the finisher wired the three under-built gaps, sigil `d1830ee`/`7441795`; §13.5): module-scope `invariant: preserves(ix)` inherited + verified on all 15 procs (over the push/pop stack model + the callee-preserves oracle), `clobbers(...)`/`out(<reg>)` on Z80 registers (incl. the `af`→`out(a) clobbers(f)` result/flag split), `out(hl, carry: found)` (Psg/FmVolEnv_Resolve), `falls_into` (Psg_ApplyMod→EmitDivisor→EmitDivisorTo), and 3 `extern proc` callee contracts (Snd_ChanClass/Mod_ReArm/Mod_Advance) feeding the oracle. The acceptance corpus caught THREE 15-year-old header over-claims, now machine-corrected (EnvCursorReset `af`; Psg/FmVolEnv_Resolve `bc`→only `b`; EmitDivisor `af,bc,de`→`de`/`b` survive). | `engine/sound/sound_psg.emp` vs `engine/sound/sound_psg.asm` (canonical body) | `sound_psg_port::sound_psg_matches_as_twin_{plain,debug}` (windowed byte gates, 444 B both shapes) + `plain_and_debug_shapes_differ` (shape-variance) + `emp_diverges_from_doctored_twin` (t24 positive control, moved PsgDivisorTableZ) + `doctored_both_sides_stay_equal` | the whole-ROM resident-blob SEAM lands (scale (2): the `.emp` placed at the phase-0 window inside the blob, blob-precedes-engine byte check); OR Spec 5 (the `.asm` twin retires). Until scale (2), the oracle is the sole drift guard. |
| 71 | **✅ CLOSED (seam-1, THE FIRST TWIN DELETION — aeon `85ae87c`): `sound_fm.asm` DELETED; `sound_fm.emp` is the canonical source; the windowed oracle transformed to a reference-slice gate.** — **sound_fm twin — `sound_fm.asm` canonical body** (t33, the rung-2 CONTRACT-model INVARIANT-HEAVY corpus) — the YM2612 FM voice writer + patch load (21 labels/procs incl. the RegDeltaGroupBase data table + the Fm_NoteOnFreqExact alias entry), the SECOND resident-blob Z80 CODE port. Proven at SCALE (1): a WINDOWED oracle byte-gates `sound_fm.emp` == `sound_fm.asm` at BOTH shipped shapes (925 B / $39D each): plain window $12C3, debug window $1341 (+$7E upstream __DEBUG__ growth; fm's OWN layout is shape-invariant — every inter-label delta is the constant +$7E). fm's ENTIRE external link seam is shape-INVARIANT (Mod_ReArm $86F + the four banked tables FmPitchTableZ/LogVolumeLutZ/CarrierMaskTableZ/SndDefaultPitchTable), because fm now DEFINES Snd_ChanClass itself — psg's shape-varying extern trust CONVERTS TO A CHECKED DEFINITION here, at the EXACT $12EE/$136C addresses psg's oracle trusted (the trust surface's first conversion to verification). So the two shapes differ ONLY in the section's own re-based internal call/jp targets. Comptime constants ride as `-D` defines; the cross-seam symbols (the four banked tables + the one external call Mod_ReArm) as equ carriers. NO build gate yet — the `.asm` stays CANONICAL (included in z80_sound_driver.asm's `phase 0` blob, 3rd of 5); the `.emp` is not wired into build.sh at this scale (the resident-blob seam sub-tranche owns whole-ROM placement — the t27 seam-STOP family). The FULL rung-2 register contract set is LIVE + machine-checked (0 firings, contract gate proven non-vacuous by an injected false `preserves(hl)` on Snd_ChanClass firing `[proc.preserves-unverifiable]`): module-scope `invariant: preserves(ix)` inherited + verified on all procs over the DEEP push/pop LIFO nesting (Fm_PatchTlGroup reaches stack depth 4; Fm_PatchOpGroup depth 3 — psg's deepest was 1), the callee-preserves oracle, and the `push ix / pop hl|bc` MOVE idiom; `clobbers(...)`/`out(<reg>)` on Z80 registers (the `af`→`out(a) clobbers(f)` split on Fm_ChSel, plus the multi-out `out(b, c)` Fm_RoutePart / `out(d, e)` Fm_FnumApplyDelta / `out(hl, carry: music)` Snd_ChanClass — the first IN-FILE carry producer+consumer, consumed by Fm_PatchPtr/Fm_SetVolume); `falls_into` (Fm_NoteOn→Fm_NoteOnFreq→Fm_NoteOnFreqExact, a THREE-proc chain); 1 `extern proc` (Mod_ReArm). The corpus caught TWO `.asm` header over-claims, machine-corrected: Fm_NoteOff `Clobbers af,bc,de`→de survives; Fm_WriteFreq `Clobbers af,bc,de,hl`→de survives (both preserve de BY CONSTRUCTION — never loaded with a port address; push/pop-bracketed across callees that all preserve de). | `engine/sound/sound_fm.emp` vs `engine/sound/sound_fm.asm` (canonical body) | `sound_fm_port::sound_fm_matches_as_twin_{plain,debug}` (windowed byte gates, 925 B both shapes) + `plain_and_debug_shapes_differ` (shape-variance) + `emp_diverges_from_doctored_twin` (t24 positive control, moved FmPitchTableZ) + `doctored_both_sides_stay_equal` (Mod_ReArm) | the whole-ROM resident-blob SEAM lands (scale (2): the `.emp` placed at the phase-0 window inside the blob, blob-precedes-engine byte check); OR Spec 5 (the `.asm` twin retires). Until scale (2), the oracle is the sole drift guard. |
| 72 | **player_common INTERNAL gate + gate-off code twin + org arm** (t34 P1 keystone) — player_common.asm keeps its zero-byte HEADER (the `PlayerV struct`, `_pl_*`/`PPHYS_*` equates, `BUTTON_JUMP_MASK`, the 4 macros) ALWAYS-EMITTED so the surviving player_ground/air/spindash resolve it; only the CODE (`Player_Init`…`Player_DebugMove`) sits under `ifndef SIGIL_EMP_PLAYER_COMMON … else org $10448 … endif`. The row-58 inline-org class, INSIDE the file (deviation from the main.asm-gate pattern, justified by the shared-header requirement). | `games/sonic4/player/player_common.asm` (gated code + header) vs `games/sonic4/player/player_common.emp` | `test_p1_player_port` windowed byte gates (both shapes) + gate-off dual-build identity + the org-arm downstream alignment (whole-ROM canaries) | **P4 close: when player_ground/air/spindash/sensors all port, the header's surviving AS readers vanish → the header equates become fully `.emp`-owned and the internal gate collapses to a whole-file gate; or Spec 5 (dual build dies).** |
| 73 | **sonic whole-file gate + org arm** (t34 P1 keystone) — `ifndef SIGIL_EMP_SONIC / include sonic.asm / else / org $10C54 / endif` (main.asm). sonic has NO surviving-AS consumer of its symbols (PhysTable_Sonic/Sonic_InitAssets/LoadArt read only by player_common, which ports too) → the clean row-60 whole-file gate. | `games/sonic4/player/sonic.asm` vs `games/sonic4/player/sonic.emp` | `test_p1_player_port` windowed byte gates (both shapes) + gate-off dual-build identity | Spec 5 (the dual build dies — no surviving AS consumer, so nothing to flip earlier). |
| 74 | **PlayerV 13-field GUARDED overlay + 5 drift guards** (t34 P1 keystone, row-61 DplcV class) — player_common.emp's `vars PlayerV: Sst.sst_custom { 13 fields }` reproduces the AS `PlayerV struct` + `_pl_*` equates that SURVIVE in player_common.asm's always-emitted header for player_ground/air/spindash. 5 guards on the fields a surviving file reads (gsp/state/move_lock/spindash/stick_convex); the other 8 covered by the region gate. The overlay's always-on `[overlay.window-overflow]` check subsumes the AS `objvarsCheck PlayerV_len` + the SST_interact collision `if`. | `games/sonic4/player/player_common.emp` (`vars` overlay) vs player_common.asm's surviving `PlayerV struct` + `_pl_*` equates | 5× `ensure(extern("_pl_X") == offsetof(Sst, sst_custom) + offsetof(PlayerV, field))` (captured + pass in `p1_drift_guards_all_pass`) + the windowed byte gates | **P4 close: player_ground/air/spindash/sensors port → the surviving `_pl_*` readers vanish → the overlay becomes single-consumer `.emp`-owned (drops to the row-66 zero-guard class); or Spec 5.** |
| 75 | **The 4 macro→comptime-fn templates** (t34 P1 keystone, macro-port class rows 24/26/48) — `set_standing_size`/`set_ball_size` (closed pair), `mask_opposing_lr` (hygienic-label; defined for P2/P3 consumers, player_common does NOT expand it), `dist_to_fix` (ADOPTS `pixels_to_coord`, kill row 49; the leading ext.l DIED in the t34 wave). The AS twins keep the `setStandingSize`/`setBallSize`/`maskOpposingLR`/`distToFix` macros inline in the always-emitted header. | `games/sonic4/player/player_common.emp` (4 comptime fns) vs player_common.asm's 4 AS macros | the windowed byte gates bind each expansion's emitted bytes (player_common ×1 dist_to_fix + the two size setters; player_air ×4 distToFix as the surviving expander) | **P4 close: the surviving AS expanders (maskOpposingLR in ground/air, distToFix in air) port → the templates become `.emp`-only; or macros.asm retires (Spec 5).** |
| 76 | **player_common game-config mirrors** (t34 P1 keystone, row-54/row-1 mirror class) — REDUCED at the ambient-hoist parcel (2026-07-29): the engine-truth block (PHYS_SKID_MIN/JUMP_BUFFER/FALL_CAP / player+ball radii / CURL_Y_SHIFT / ST_ROLLING/PUSHING / COLLISION_NONE / EDGE_* / BUTTON_A/C / BUTTON_*_BIT) **HOISTED to the `engine.constants` twin** (2nd consumer trip discharged); player_common now `use engine.constants.{...}`. SURVIVING file-local: PSTATE_* ×8 / ANIM_* ×12 / VRAM_TEST_MARKER (typed VramTile) / SFXID_SKID (typed SfxId, DEBUG-gated) mirror config/constants.asm + config/sound_ids.asm (game-config, NOT this parcel), + BUTTON_JUMP_MASK (game-truth player_common.asm, derived from the hoisted BUTTON_A/C). | `games/sonic4/player/player_common.emp` (the surviving consts) vs `games/sonic4/config/constants.asm` + `config/sound_ids.asm` + `player_common.asm` (BUTTON_JUMP_MASK) | the surviving drift-guard `ensure`s (captured + pass in `p1_drift_guards_all_pass`) + the byte gates (values fold into immediates); the hoisted block's guards now live in `engine.constants` (rides every consumer's gate) | a game-constants `.emp` module is born (PSTATE_*/ANIM_*/VRAM_TEST_* move there); or config/constants.asm retires (Spec 5). |
| 77 | **sonic game-config mirror** (t34 P1 keystone, row-54/row-1 class) — REDUCED at the ambient-hoist parcel (2026-07-29): the 8 PhysTable_Sonic values (PHYS_ACCEL/DECEL/FRICTION/TOP_SPEED/GRAVITY/JUMP_FORCE/AIR_ACCEL/JUMP_RELEASE_CAP) **HOISTED to the `engine.constants` twin**; sonic.emp now `use engine.constants.{PHYS_*}`. SURVIVING file-local: VRAM_TEST_SONIC (typed VramTile) mirrors config/constants.asm (game-config). | `games/sonic4/player/sonic.emp` (VRAM_TEST_SONIC) vs `games/sonic4/config/constants.asm` | the VRAM_TEST_SONIC drift guard + the `PhysTable_Sonic` byte gate + `ensure(8*2 == Player_Phys_End - Player_Phys)` (the physics values now fold from `engine.constants`) | game-constants `.emp` module born (VRAM_TEST_SONIC); or config retires (Spec 5). |
| 78 | **✅ CLOSED (seam-1, THE FIRST TWIN DELETION — aeon `85ae87c`): `sound_sequencer.asm` DELETED; `sound_sequencer.emp` is the canonical source; its 26 opcode-handler labels export via the generated `z80_sound_syms.asm` (the seq_opcode_tab contract); the windowed oracle transformed to a reference-slice gate.** — **sound_sequencer twin — `sound_sequencer.asm` canonical body** (t36 rung-3a interpreter) — the Z80 music-event-list interpreter (the biggest resident Z80 file). The `.asm` stays CANONICAL in the build (Z80-blob-precedes-engine: one byte slides the whole corpus); `sound_sequencer.emp` is proven by the SCALE-1 dual-shape windowed oracle (`sound_sequencer_port`, plain $0565–$0CD7 = 1906 B / debug $0565–$0D55 = 2032 B), NOT wired into build.sh. FIRST resident Z80 `.emp` with internal `if DEBUG == 1` bodies (16 blocks → the +$7E debug growth). KILL = the seam sub-tranche retires the `.asm` when the sound files link natively (also closes row-70/71 psg/fm twins + the extern-decl-vs-def drift ledger row). |
| 79 | **player_ground / player_air / player_spindash gate-off body twins + org arms** (t35 P2/P3) — the three AS state-machine files are the gate-off bodies under `SIGIL_EMP_PLAYER_GROUND` / `_AIR` / `_SPINDASH` (row-5 class), each an `ifndef/else org` arm in `games/sonic4/main.asm` (plain-code gates, NOT the row-72 internal-gate — these files have NO surviving-AS header consumers, they are pure code reached only through the Player_States offset table). Bank windows shape-invariant → ONE `org` per arm both shapes: ground→`$108A2` (PState_Air), air→`$10B74` (PState_Spindash), spindash→`$10C14` (Sonic_InitAssets); the resume orgs are inline AS literals (row-6/58 class, re-derive on re-baseline). Region pins ARE in pins.rs (`PLAYER_GROUND` $10448/$45A, `PLAYER_AIR` $108A2/$2D2, `PLAYER_SPINDASH` $10B74/$A0, repin-generated). Gate defines must never be set for other games. | the three `.asm` gate-off bodies + `games/sonic4/main.asm` (gate arms) vs the `.emp` regions | `test_p2_player_states_port::p2_{spindash,air,ground}_region_matches_reference` + `..._debug_region_...` (windowed both shapes) + `p2_*_undoctored_compile_equals_the_reference_window` (positive controls) + `p2_*_doctored_reference_diverges` (t24 negative probes) + `mixed_dac_rom::mixed_tranche35_{,debug_}rom_matches_assembled_reference` (whole-ROM both shapes) + gate-off dual-build byte-identity (37dd2bb2/bbb822f6 unchanged) | Spec 5 — each `ifndef SIGIL_EMP_PLAYER_*` gate collapses to the `.emp` include, the `.asm` twin + the `org` arm are deleted. |
| 80 | **player_sensors `extern proc` boundary decls — 4 distinct sensors, 7 decl sites** (t35 P2/P3, the `Player_AtLedgeEdge` row-32-class boundary) — the state files call the surviving-AS player_sensors primitives cross-seam; each caller module declares an `extern proc` contract so the corpus contract-closure gate is complete: `Player_SensorFloor` (all 3 files), `Player_SensorCeiling` (ground+air), `Player_SensorWallDir` (ground), `Player_SensorWallAt` (air) = 7 decl SITES over 4 DISTINCT callees. `clobbers`: Floor/Ceiling `d0-d7/a1-a2`, WallAt/WallDir `d0-d6/a1` (matching the `.asm` headers; over-claim-safe). The `extern proc` form is the resolution-ladder's die-at-port exception (player_sensors ports at P4). | the 7 `extern proc` decls in `player_{ground,air,spindash}.emp` + the 8th `Player_AtLedgeEdge` decl in `player_common.emp` (t34 row-32-class) vs `games/sonic4/player/player_sensors.asm` (the callees) | `contract_closure_corpus::corpus_closure_residue_is_empty_the_error_gate` (the closure is complete — no extern holes) + the windowed/mixed byte gates | **KILLED at P4 (t38, player_sensors ported).** All 8 68k `extern proc` boundary decls DELETED (7 sensor decls in ground/air/spindash + `Player_AtLedgeEdge` in player_common); the calls resolve module-to-module by bare link. **NO new 68k `extern proc` born** — `Collision_GetType` bare-links to the already-ported `engine.collision_lookup` (recon predicted a new decl; the actual port needed none). The 68k game side is now ZERO `extern proc` (the t28 headline achieved for 68k; every remaining `extern proc` in the corpus is a Z80 sound seam — rows 70/71/78, die at Spec 5). The 4 sensor callees now carry honest `preserves(a0)` (verified by the t38 immediate-sp-cleanup model), so no a0 leaks past them. Closure residue: **10 firings → 0**. |
| 81 | **player_ground/air/spindash file-local PPHYS + game-config mirrors** (t35 P2/P3, row-54/row-1 mirror class) — REDUCED at the ambient-hoist parcel (2026-07-29): the engine-truth block (`PHYS_*` slope/roll/slip/gsp-cap/headroom/move-lock, radii/CURL/PUSH, `ST_XFLIP`/`ST_ON_OBJECT`/`ST_PUSHING`, `BUTTON_LEFT/RIGHT`, `BUTTON_A/C`, `BUTTON_*_BIT`) **HOISTED to the `engine.constants` twin** (the 2nd-consumer trip discharged); the three state files now `use engine.constants.{...}`. SURVIVING file-local: `PPHYS_*` physics-table field offsets + `BUTTON_JUMP_MASK` (GAME-truth — defined in `player_common.asm`, NOT engine/constants.asm; the brief's PPHYS→engine.constants was CORRECTED at recon, see the ledger row: hoisting a game-truth symbol into engine.constants would invert engine→game layering and force every constants-consuming mixed build to link player_common.asm) + `PSTATE_*`/`SFXID_*`/`SPINDASH_*` game-config. | the three `.emp` files' surviving PPHYS_*/game-config mirrors vs `player_common.asm` (`PPHYS_*`/`BUTTON_JUMP_MASK`) + `games/sonic4/config/constants.asm` + `config/sound_ids.asm` | the surviving file-local `ensure(extern("NAME") == NAME)` drift guards (captured + pass in `test_p2_player_states_port` + `mixed_tranche35`); the hoisted block's guards now live in `engine.constants` | a game-constants `.emp` module is born (PPHYS_*/PSTATE_*/SFXID_*/SPINDASH_*/BUTTON_JUMP_MASK move there — the natural home is player_common.emp, `pub const`, once the state files' cross-module import is cheap); or P4/Spec 5 — whichever fires first. |
| 82 | **`abs_w` comptime-fn template** (t35 P2/P3 step-4 build; RE-HOMED to `engine.coords` at the ambient-hoist parcel 2026-07-29) — `pub comptime fn abs_w(v: Reg)` emits the `bpl .abs / neg.w {v}` in-place absolute-value pair with a hygienic `.abs` label per expansion. HOISTED from `player_common.emp` (game) to `engine.coords.emp` (beside `pixels_to_coord`) — a corpus-wide ENGINE idiom, the "first-consumer wrong-home" case corrected. Consumers `use engine.coords.{abs_w}`: player_ground ×6 + player_air ×3 (the original 9) + player_common ×3 (t35 step-6 folds) + **the 6 engine folds NOW DONE**: entity_window ×2 (`Collected_UpdateCenter` |slot-center|), collision ×2 (`Touch_Solid` AABB |delta|), core ×2 (`RunObjects` |pos-camera| cull). The AS twins keep the `bpl/neg.w` INLINE (byte-level lockstep). Caller must have v's sign live in CC. | `engine/coords.emp` (the fn) + all 20 `.emp` expansion sites vs the AS twins' inline `bpl/neg.w` | the windowed byte gates (`test_p2_player_states_port`, `entity_window_port`, `collision_port`, `core_port`) + `mixed_tranche35` + `mixed_dac_rom` collision arm; the 5 abs_w-blast-radius tests gained a `coords` ambient (entity_window/collision/core port tests + core_negative_probes + tranche7_negative_probes) | Spec 5 — the AS twins retire (nothing left to lockstep; the engine-hoist + 6-fold sweep is COMPLETE). |
| 83 | **✅ CLOSED (seam-1, THE FIRST TWIN DELETION — aeon `85ae87c`): `sound_sfx.asm` DELETED; `sound_sfx.emp` is the canonical source; the sfx build-time helpers (SFX_WIN_MASK/BASE + sfx_winptr/sfx_bankid) it hosted for the banked sfx_blob_win_tab.asm now live in boot_data.asm's arm; the windowed oracle transformed to a reference-slice gate.** — **sound_sfx twin — `sound_sfx.asm` canonical body** (t37 rung-3b, the Out-heavy SFX-engine mirror) — the Phase-5a SFX interpreter (17 code procs + 3 data tables), the FOURTH resident Z80 CODE port and the last interpreter before the driver top (rung 4). The `.asm` stays CANONICAL in the build (Z80-blob-precedes-engine: one byte slides the whole corpus); `sound_sfx.emp` is proven by the SCALE-1 dual-shape windowed oracle, NOT wired into build.sh. The PSG t32 CASE: shape-VARIANT base (plain $0CD7 / debug $0D55, +$7E from the sequencer's `if DEBUG==1` growth that PRECEDES this window), shape-INVARIANT layout (1516 B / $5EC BOTH shapes — sfx has NO own `__DEBUG__` blocks; only the game-config `if SFXID_REV_LOOP>=0`, always-on). 13 `extern proc` decls: 11 co-resident (ModUpdate/Sequencer_Channel ← sound_sequencer.emp; Fm_* ← sound_fm.emp; Psg_* ← sound_psg.emp — verified by-hand conservative-subset, the t36 extern-decl-vs-def drift class) + 2 driver DIE-AT-PORT boundary externs (SndDrv_SetBank/Snd_RouteClassFlags). NO module `invariant: preserves(ix)` — Sfx_Frame is the SFX-side channel walker (the t36 §3.4 mirror, ledger row 1777's 2nd ix-reliance site); per-proc `preserves(ix)` on the verifiable leaves. | `engine/sound/sound_sfx.emp` vs `engine/sound/sound_sfx.asm` (canonical body) | `sound_sfx_port::sound_sfx_matches_as_twin_{plain,debug}` (windowed byte gates, 1516 B both shapes) + `plain_and_debug_shapes_differ` (shape-variance) + `emp_diverges_from_doctored_twin` (t24 positive control, moved SfxBlobWinTab) + `doctored_both_sides_stay_equal` (ModUpdate) | the seam sub-tranche retires the `.asm` when the sound files link natively (also closes rows 70/71/78 psg/fm/sequencer twins + the 2 driver die-at-port externs when rung 4 ports + the extern-decl-vs-def and transitive-clobbers-completeness ledger rows); OR Spec 5 (the `.asm` twin retires). Until then the oracle is the sole drift guard. |
| 84 | **test_player.asm INTERNAL-gate scaffolding** (t39) — the AS file keeps its ZERO-BYTE header (DplcV `struct` under `ifndef _dplc_ptr`, TPlayerV `struct`, `_debug_flag`/physics/`STUB_FLOOR_Y` equates, `objvarsCheck`) ALWAYS-emitted (feeds test_animated.emp's `_dplc_ptr`/`_art_base` guards + object_test_state.asm's `STUB_FLOOR_Y`); only the `TestPlayer`/`TestPlayer_Main`/`TestPlayer_Debug` CODE is gated under `SIGIL_EMP_TEST_PLAYER` with an `else org $10F02` (test_enemy's first label TestEnemy_Init; shape-invariant, one org). The `.emp` owns the TPlayerV overlay + the code; the AS twin is the lockstep body (row-5 class) + the surviving header (row-34/keystone class). Also mirrors VRAM_TEST_SONIC (row-62 class). Gate define must never be set for other games. | `games/sonic4/objects/test_player.asm` (header ALWAYS + gate-off body) vs `test_player.emp` region | `test_g4_final_objects_port::g4_objects_{,debug_}regions_match_reference` (windowed both shapes) + `g4_undoctored`/`g4_doctored` (positive control + t24 probe) + `mixed_dac_rom::mixed_tranche39_{,debug_}rom_matches_assembled_reference` (whole-ROM both shapes) + the 4 drift guards | Spec 5 (the gate-off AS build dies; the header's equates flip to `.emp`-owned `pub const`, DplcV/TPlayerV move to game-side `.emp`). |
| 85 | **test_enemy.asm INTERNAL-gate scaffolding** (t39) — the AS file keeps its ZERO-BYTE header (TEnemyV `struct`, `_enemy_*` equates, `objvarsCheck`, `ENEMY_PATROL_SPEED`/`ENEMY_PATROL_RANGE`) ALWAYS-emitted (feeds test_objects.emp's `ENEMY_PATROL_SPEED` objdef guard); only the `TestEnemy_Init`/`TestEnemy_Main` CODE is gated under `SIGIL_EMP_TEST_ENEMY` with an `else org $10F4A` (test_solid's first label TestSolid_Init; shape-invariant, one org). The `.emp` owns the UNGUARDED TEnemyV overlay + the code. Gate define must never be set for other games. | `games/sonic4/objects/test_enemy.asm` (header ALWAYS + gate-off body) vs `test_enemy.emp` region | `test_g4_final_objects_port::g4_objects_{,debug_}regions_match_reference` (windowed both shapes) + `mixed_tranche39_{,debug_}` (whole-ROM both shapes) | Spec 5 (the ENEMY_PATROL_SPEED / overlay equates flip to `.emp`-owned). |
| 86 | **path_swap.asm gate-off body + PER-SHAPE org arm** (t39) — the AS object file is the whole-file gate-off body under `SIGIL_EMP_PATH_SWAP` (row-5 class), a `ifndef/else` arm in `games/sonic4/main.asm` with **PER-SHAPE org** (`ifdef __DEBUG__ org $112F4 / else org $1128C`, DeformTable_Zero — gameDataIncludes' first label). The object bank's FIRST shape-dependent gate (2 `__DEBUG__` blocks, debug +$68); a wave that moves path_swap's bytes re-derives BOTH orgs (gap-ledger rider). The `.emp` owns the inline `ObjDef_PathSwap` objdef + the UNGUARDED PathSwapV overlay + the two procs; the `dc.l ObjDef_PathSwap` AS consumers flip to the `.emp` label. Also mirrors VRAM_TEST_OBJ (row-65 class, now ×4). Gate define must never be set for other games. | `games/sonic4/objects/path_swap.asm` (gate-off body) + `games/sonic4/main.asm` (per-shape gate arm) vs `path_swap.emp` region (shape-dependent, $92 plain / $FA debug) | `test_g4_final_objects_port::g4_objects_{,debug_}regions_match_reference` (windowed, per-shape lengths) + `g4_undoctored`/`g4_doctored` (positive control + t24 probe, on the shape-dependent plain window) + `mixed_tranche39_{,debug_}` (whole-ROM, per-shape resume proof) + the VRAM_TEST_OBJ guard | Spec 5 (the gate-off AS build dies; ObjDef_PathSwap + VRAM_TEST_OBJ + overlay flip to `.emp`-owned). |
| 87 | **✅ CLOSED (seam-1, THE FIRST TWIN DELETION — aeon `85ae87c`): `z80_sound_driver.asm` DELETED; `z80_sound_driver.emp` is the canonical source (the blob FRONT / phase-0 origin); its reset-vector entry renamed `Z80_Sound_Start`→`Z80_Sound_Entry` (the 68k LMA symbol `Z80_Sound_Start`=$3DE is owned by the permanent boot_data.asm arm); the windowed oracle transformed to a reference-slice gate.** — **z80_sound_driver twin — `z80_sound_driver.asm` canonical body** (t40 rung-4, the DAC-streaming driver — the blob FRONT and the LAST resident Z80 CODE port) — the Z80-autonomous DAC streaming driver (20 procs + the framing origin) that `save/cpu z80/phase 0`s the blob and DEFINES the $0000 origin every downstream sound file's base is measured from. The `.asm` stays CANONICAL in the build (Z80-blob-precedes-engine: one byte here slides the WHOLE corpus — the driver is the front); `z80_sound_driver.emp` is proven by the SCALE-1 dual-shape windowed oracle, NOT wired into build.sh. Window $0000..$0565 = 1381 B, SAME SIZE + POSITION both shapes (zero internal `__DEBUG__` content) but NOT byte-identical: five `call` sites target the three after-sequencer callees (Sequencer_StopAll/Sfx_StopAll/SfxDispatch) that shift +$7E in the debug shape → 9 operand bytes differ (Sequencer_Frame $0565 is the one shape-invariant target). NO module `invariant: preserves(ix)` — the sequencer per-proc shape (the driver clobbers ix in Sample/TimerATick/LoadSong); per-proc `preserves(ix)` only where verifiable. FOUR INBOUND trust conversions became checked defs here (sfx's SndDrv_SetBank/Snd_RouteClassFlags + sequencer's Snd_StartSample/Snd_DacLookup — all conservative subsets); FOUR OUTBOUND `extern proc` decls (Sequencer_Frame preserves(iy); Sequencer_StopAll/Sfx_StopAll/SfxDispatch bare). The rung-4 T-state capability (`cycles()` + `pad_to_cycles`) is LIVE: the three cycle-balance ensures verify FILL 195 / DRAIN 195 / DRAINING 194 on the real spans, and the two DAC timing pads are DERIVED (`pad_to_cycles`), byte-identical to the literal `rept 19/21`. | `engine/sound/z80_sound_driver.emp` vs `engine/sound/z80_sound_driver.asm` (canonical body, sliced after `phase 0` before the engine includes) | `z80_sound_driver_port::z80_sound_driver_matches_as_twin_{plain,debug}` (windowed byte gates, 1381 B both shapes) + `plain_and_debug_shapes_differ` (9-byte shape-variance) + `both_shapes_same_size` + `emp_diverges_from_doctored_twin` (t24 positive, moved Sfx_StopAll) + `emp_diverges_from_doctored_const_twin` (moved SND_STAT_TICK) + `doctored_both_sides_stay_equal` (SfxDispatch) | the seam sub-tranche retires the `.asm` when the sound files link natively (with t40 the seam now holds ALL the resident sound twins — rows 70/71/78/83/84 psg/fm/sequencer/sfx/driver — and closes the 4 inbound + 4 outbound driver externs + the extern-decl-vs-def and transitive-clobbers-completeness ledger rows); OR Spec 5 (the `.asm` twin retires). Until then the oracle is the sole drift guard. |
| 88 | **object_test_state.asm gate-off body + PER-SHAPE org arm** (t41 T1) — the AS game-state file is the whole-file gate-off body under `SIGIL_EMP_OBJECT_TEST_STATE` (row-5 class), an `ifndef/else` arm in `games/sonic4/main.asm`'s `gameStatesIncludes` with **PER-SHAPE org** (`ifdef __DEBUG__ org $5E2DA / else org $5C7EC` = ojz's first label GameState_OJZScroll_Init). SHAPE-DEPENDENT ($5BC plain / $658 debug — the `if DEBUG==1` profiling block grows the region +$9C; re-derive BOTH orgs + the region pins on re-baseline). The `.emp` owns GameState_ObjectTest{,_Init}/ObjectTestChurn{,_Init} + the `ObjSpawn`-typed `TestObjectList` + `TestArt` (embeds ring_art.bin + sonic.bin from the repo root) + `TestPalette`. Mirrors VRAM_TEST_OBJ (row-65 class) + STUB_FLOOR_Y + VDP_HV_COUNTER (new local-const mirrors, each drift-guarded). ojz reads the `.emp`-owned `TestArt` cross-module. Gate define must never be set for other games (demo takes the include). | `games/sonic4/test/object_test_state.asm` (gate-off body) + `games/sonic4/main.asm` (per-shape gate arm) vs `object_test_state.emp` region | `test_t1_harness_states_port::t1_{,debug_}regions_match_reference` (windowed both shapes) + `t1_undoctored`/`t1_doctored` (positive control + t24 probe) + `mixed_dac_rom::mixed_tranche41_{,debug_}rom_matches_assembled_reference` (whole-ROM both shapes) + gate-off dual-build byte-identity (4b66cace/1c256b3b unchanged) | Spec 5 — the gate collapses to the `.emp` include; the `.asm` twin + the per-shape `org` arm delete; VRAM_TEST_OBJ/STUB_FLOOR_Y/VDP_HV_COUNTER flip to `.emp`-owned (or VDP_HV_COUNTER hoists into engine.vdp). |
| 89 | **ojz_scroll_test.asm gate-off body + PER-SHAPE org arm** (t41 T1, the game ENTRY state) — the AS game-state file is the whole-file gate-off body under `SIGIL_EMP_OJZ_SCROLL_TEST` (row-5 class), an `ifndef/else` arm in `gameStatesIncludes` with **PER-SHAPE org** (`ifdef __DEBUG__ org $5E5A8 / else org $5CAAE` = NullInterrupt, the post-gameStates level-1 stub — a 2-byte `NullInterrupt:rte` sits between ojz's PlayerMarkerTile tail and error_handler's BusError). SHAPE-DEPENDENT ($2C2 plain / $2CE debug — the two Debug_Scene_Freeze skip blocks grow the region +$C). The `.emp` owns GameState_OJZScroll_Init (= Game_Entry) / _Update + OJZ_SectionMarkerColors + PlayerMarkerTile. Mirrors VRAM_TEST_OBJ + VRAM_TEST_MARKER + CAM_SCREEN_HALF_W/H (local-const mirrors, drift-guarded); the Player_1-spawn writes use the extern-equ-sum idiom (PLAYER_1_X_POS = extern("Player_1")+extern("SST_x_pos")). **Kill row 35 STAYS OPEN** (the per-frame mode-set-3 force-write ported verbatim — overseer-adjudicated CARRY-AS-IS). Gate define must never be set for other games. | `games/sonic4/test/ojz_scroll_test.asm` (gate-off body) + `games/sonic4/main.asm` (per-shape gate arm) vs `ojz_scroll_test.emp` region | `test_t1_harness_states_port::t1_{,debug_}regions_match_reference` + `t1_undoctored`/`t1_doctored` + `mixed_tranche41_{,debug_}` (whole-ROM both shapes) | Spec 5 — the gate collapses; the `.asm` twin + per-shape `org` arm delete; VRAM/CAM mirrors flip to `.emp`-owned (row 35's parallax-hardening parcel is independent). |
| 90 | **config/game.asm gate-aware Game_Entry numeric equ** (t41, the error_handler ErrorHandler-equ class) — under `SIGIL_EMP_OJZ_SCROLL_TEST` the `Game_Entry` equ folds to the PER-SHAPE numeric ojz region base (`ifdef __DEBUG__ $5E2DA / else $5C7EC`) instead of `= GameState_OJZScroll_Init` (a link extern when ojz is `.emp`-owned), so boot.asm's `move.l #Game_Entry` resolves at assembly (a link-time-equ-off-external-base is unavailable — the engine.inc ErrorHandler-equ precedent). Byte-neutral in the gate-off build (the `ifdef` false-arm keeps the label form). Re-pin BOTH numeric values on re-baseline. Gate define must never be set for other games. | `games/sonic4/config/game.asm` (the gate-aware `Game_Entry` equ) vs the `.emp`-owned GameState_OJZScroll_Init | `mixed_tranche41_{,debug_}rom_matches_assembled_reference` (the whole-ROM gate — boot's `#Game_Entry` must resolve to the ojz base for byte-identity) + gate-off dual-build byte-identity | Spec 5 — config/game.asm flips; Game_Entry resolves to the `.emp` export natively (or a link-time-equ-off-external-base capability lands and the numeric arm deletes). |
| 91 | **`SIGIL_EMP_*_BODY_STUB` — the DSM mixed-harness in-memory bank-composition entries** (`SIGIL_EMP_DAC_BODY_STUB` seam-2 stage-2b `cd31928`/`6de01ac`; **`SIGIL_EMP_MT_BODY_STUB` seam-2 stage-2c `cfe2abd`/`10eb2ce`**; **`SIGIL_EMP_SFX_BODY_STUB` seam-2 stage-2d** — IDENTICAL pattern: the SFX arm is `ifdef SIGIL_EMP_SFX_BODY_STUB org … else <BINCLUDE sfx_bank{,_debug}.bin>` (NO syms — no AS reader of `SfxTable`); the SfxBlobWinTab HEAD is BINCLUDE'd UNCONDITIONALLY in `soundBankHead` (`sfx_blob_win_tab{,_debug}.bin`, co-linked) — it can't be stubbed (the phase-block PC must advance past it), like the DAC head; the 10 SFX-and-downstream DSM helpers set it; kill condition below applies per-stage) — after the DAC deletion the aeon `gameSoundDataIncludes` body is `ifdef SIGIL_EMP_DAC_BODY_STUB org $58000 else <BINCLUDE the two banks>`. The stub `org` arm exists ONLY so the DSM `mixed_dac_rom` tranches can keep composing `dac_samples.emp`'s bank sections in-memory (the dac_port.rs pipeline, pinned $48000/$50000) INSIDE the whole ROM — the real build + `seam2_dac_rom` take the BINCLUDE arm. The 12 sound DSM helpers (dac/mt/sfx/hblank/tranche2-9 in `sigil-harness/src/lib.rs`) set the define. (Also: `SIGIL_EMP_DAC` — and now `SIGIL_EMP_SFX` — are VESTIGIAL: nothing in aeon reads them after the gate collapse (the SFX arm keys on `SIGIL_EMP_SFX_BODY_STUB`); the DSM helpers + `assemble_seam2_{dac,sfx}_rom_as_side` still set them as no-ops. Cleanup candidate.) | `games/sonic4/main.asm` (the `ifdef SIGIL_EMP_DAC_BODY_STUB` stub arm) vs the BINCLUDE arm; the define lives in `crates/sigil-harness/src/lib.rs` | `seam2_dac_rom::seam2_dac_rom_matches_reference_{plain,debug}` (the BINCLUDE path) + `mixed_dac_rom::*` tranches (the composed path) — BOTH == the reference, proving the two paths are byte-identical | the DSM `mixed_dac_rom` DAC-composition tranches retire OR convert to the BINCLUDE path (dropping the `dac_samples.emp` placement) — then the stub arm + the define die and the body collapses to a bare unconditional BINCLUDE, and the vestigial `SIGIL_EMP_DAC` is dropped from the helpers. |
