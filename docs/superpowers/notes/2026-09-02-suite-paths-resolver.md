# SUITE-PATHS-MIGRATION + D18-REFUSE-BARE-RUN — the resolver and the refusal

*In flight. This file is the packet for `parcel/suite-paths-resolver` and
`parcel/d18-refuse-bare-run`; it is committed early so the work has a durable
home from its first commit.*

## The contract being implemented

`empyrean` `contract/SUITE_PATHS.md`, read at `origin/main` =
`82982b7ff3c057f347d538fcf61b7c62b18ee813`.
